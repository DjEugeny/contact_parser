# 📊 План реализации Mini-CRM MVP — 2025-09-28

## 🎯 Цель
Обновить конвейер Mini-CRM до состояния MVP согласно документации `mini_crm_prd/`, обеспечить строгий JSON-ответ LLM, дедупликацию и обогащение данных, экспорт в Google Sheets и запуск минимального FastAPI+React PWA.

## 🔧 Основные этапы и шаги
1. **Инициализация и синхронизация**
   - Провести инвентаризацию текущего кода (fetcher, OCR, extractor, postprocessing, main_new).
   - Актуализировать `memory-bank/tasks.md` под данный план и зафиксировать зависимости.
   - Проверить структуру `data/` и наличие каталогов для артефактов (логи, llm_results, crm.db).

2. **Промпт и валидатор LLM**
   - Обновить `prompts/unified_contact_extraction_structured.txt` под шаблон из `09_PROMPTS_AND_VALIDATION.md` (organizations/contacts/commercial_offers/interactions/summary/key_points).
   - Обновить `src/core/extractor.py` и связанные валидаторы, добавить поддержку `role_in_message`, `interactions[]`, `confidence`, фильтрацию КП.
   - Настроить повторные запросы с подсказками при ошибках схемы.

3. **Нормализация, дедуп и обогащение**
   - Реализовать правила из `11_DEDUP_ENRICH_RULES.md` с переиспользованием модулей `src/postprocessing`.
   - Добавить scoring контактов (score ≥ 4), маппинг локальных `organization_id` → глобальные.
   - Настроить fuzzy-мэтчинг организаций (название, ИНН, домен, город) и модерационный Inbox.

4. **Рефакторинг пайплайна и CLI**
   - Превратить `api_pipeline_validator.py` в тонкий слой над `main_new.py`, добавив режимы `--mode first10|batch|range` и `--dry-run`.
   - Гарантировать сохранение артефактов (`crm.db`, `data/llm_results/DATE/*.json|*.md`, логи) и идемпотентность.
   - Обеспечить повторное использование общей бизнес-логики без дублирования кода.

5. **Прогоны качества и отчётность**
   - Выполнить прогон `first10`, собрать статистику, создать отчёт в `memory-bank/reports/` и обновить `index.md`.
   - После исправлений запустить `batch`, повторить отчёт.
   - Вести лог изменений в `memory-bank/tasks.md`.

6. **Основной конвейер (`main_new.py`)**
   - Актуализировать режимы `interactive/test/full-pipeline/async-*` под обновлённый extractor.
   - Убедиться, что `main_new.py` использует те же модули постобработки и записи в БД.

7. **Экспорт в Google Sheets и локальные таблицы**
   - Реализовать экспорт по `10_GOOGLE_SHEETS_INTERIM.md`: контакты, организации, КП, история, Inbox.
   - Добавить ссылки на .eml/JSON/attachments; предусмотреть локальный CSV/XLSX fallback.

8. **FastAPI + React PWA (минимальный UI)**
   - Создать REST-эндпойнты (поиск, карточки, реестр КП, модерация, «Открыть письмо»).
   - Подготовить фронтенд (Vite + Tailwind): поиск, карточки организации/контакта, реестр КП, модерация, просмотр письма.
   - Реализовать напоминания по `valid_until` внутри UI.

9. **Финализация и backlog**
   - Проверить бэкапы и структуру каталогов данных.
   - Добавить карточку backlog: опция сохранения `.eml` в fetcher.
   - Подготовить итоговый отчёт по завершению MVP.

## 🧪 Контрольные точки качества
- Доля валидных JSON ≥ 99% на выборке `first10` и `batch`.
- Отсутствие дублей в таблицах `organizations`, `contacts`, `commercial_offers` после повторных запусков.
- Наличие модерационного Inbox с сомнительными записями.
- Работа экспортов и UI (ручная проверка).

## 🚀 Следующие шаги
1. Обновить `memory-bank/tasks.md` с чек-листом этапов.
2. Начать выполнение с блока «Промпт и валидатор LLM».

---
*Отчет создан: 2025-09-28*  
*Статус: в работе*
