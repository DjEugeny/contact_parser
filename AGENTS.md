# AGENTS.md — инструкция для агента разработки (RU)

# Инструкция для AI-ассистента: Проект "Mini-CRM"

## Твоя роль и задача

Ты — мой AI-ассистент, эксперт в разработке на Python (FastAPI, Pydantic) и React. Твоя задача — помогать мне в разработке проекта "Mini-CRM", следуя изложенным ниже архитектуре, технологическому стеку и плану. Ты должен писать чистый, эффективный и хорошо документированный код.

## Обзор проекта

Мы создаем "Mini-CRM" для одного пользователя. Система анализирует архив электронной почты (~19 000 писем), извлекает с помощью LLM контакты, организации и коммерческие предложения, обрабатывает их и сохраняет в базу данных SQLite. Результаты доступны пользователю через веб-интерфейс на React.

## rules
- Отвечай на русском языке, если не требуется код.
- Внутренние размышления (reasoning) показывай на русском.
- Код всегда оставляй на исходном языке.

## Документация
Основной пакет документации находится в папке `memory-bank/mini_crm_prd/`:
- 01_PRD_OVERVIEW.md — цели и рамки.
- 02_REQUIREMENTS.md — детальные требования.
- 07_FRONTEND_UI.md — экраны и UX.
- 08_MVP_PLAN.md — фазы реализации.
- 09_PROMPTS_AND_VALIDATION.md — промпт и валидация.
- 10_GOOGLE_SHEETS_INTERIM.md — временные таблицы.
- 11_DEDUP_ENRICH_RULES.md — дедуп/маппинг/обогащение.
- 12_API_PIPELINE_VALIDATOR.md — сквозной тест-раннер.
- 13_STEP_BY_STEP_PLAN.md — пошаговый план.
- 14_TECHNICAL_SPECIFICATION.md — архитектурный дизайн, стек, схема БД, API, конвейер.

Рабочие исходники: `advanced_email_fetcher.py`, `ocr_processor.py`, `extractor.py`, `api_pipeline_validator.py`, `optimized_api_pipeline_validator.py`, `main_new.py` и все файлы в папке src, тесты в папке tests, данные в папке data

## Обязательные артефакты и отчётность
- После **каждой важной стадии имплементации** создавай отчёт:
  - Папка: `contact_parser/memory-bank/reports/`
  - Файл: `contact_parser/memory-bank/reports/README.md` — описывает как создавать отчёты.
- Веди файл задач (единый список):
  - Путь: `contact_parser/memory-bank/tasks.md`
  - Формат: чек‑лист (todo / in‑progress / done), ссылки на PR/коммиты/артефакты, краткие заметки по рискам.

## Основные цели
1) Сквозной конвейер: Fetcher → OCR → LLM → Validation/Normalize/Dedup/Enrich → SQLite (`crm.db`).  
2) Экспорт в Google Sheets/локальные таблицы.  
3) Минимальный FastAPI + React PWA (поиск, карточки, реестр КП, модерация, просмотр письма).  
4) Напоминания по срокам КП (UI).  
5) Backlog: добавить в fetcher опцию сохранения `.eml` (низкий приоритет).

## Ожидаемый результат
- Файл `crm.db` с таблицами: organizations, contacts, contact_phones, commercial_offers, interactions, moderation_inbox.  
- Папка `data/llm_results/DATE/*.json` — сырые ответы LLM. 
- Папка `data/llm_results/DATE/*.md` — отчёты по каждому письму для быстрого просмотра результатов (временно для стадии api_pipeline_validator.py). 
- Экспорт таблиц (CSV/XLSX/Sheets).  
- Минимальный UI (поиск, карточки, реестр КП, модерация, просмотр письма).

## Технические детали
- Язык бэкенда: **Python 3.11 (FastAPI)**.  
- БД: **SQLite** (миграция на Postgres позже без смены моделей домена).  
- Фронтенд: **React + Vite + Tailwind**, PWA.  
- Валидация JSON: **Pydantic/jsonschema**.  
- OCR: локальные экстракторы + Google Vision fallback.

## Стиль кодирования
naming: "snake_case для функций, PascalCase для классов"
organization: "Один класс = один файл, промпты отдельно"

principles:
    - "Всегда добавляй типизацию (typing) к функциям"
    - "Используй docstrings с эмодзи для описания функций"
    - "Включай обработку ошибок с информативными сообщениями"
    - "Логируй операции с эмодзи (🔍, ✅, ❌, 🔧, 📊)"
    - "Создавай fallback системы для критических операций"

## Переменные окружения (.env)
- IMAP_HOST, IMAP_USER, IMAP_PASS  
- DB_URL (sqlite:///./crm.db)  
- LLM_API_KEY_* (провайдеры + fallback)  
- GOOGLE_VISION_KEY  
- SHEETS_CREDENTIALS (по необходимости)

## Качество и логи
- Логи в `data/logs/` с уровнями INFO/WARN/ERROR.  
- Отчёт по качеству: доля валидных JSON, число сущностей, размер Inbox.  
- Idempotency: повторный запуск не должен плодить дубли.

## Процедура запуска (тонкий слой → основной пайплайн)
1) Запусти `api_pipeline_validator.py` (см. 12_API_PIPELINE_VALIDATOR.md).  
2) Проведи 1–2 итерации качества, оформи отчёты в `contact_parser/memory-bank/reports/`.  
3) После стабилизации — **перейди на `main_new.py`** и используй его режимы (`interactive/test/full-pipeline/async-*`) для полноценной обработки.  
4) Убедись, что ядро конвейера не дублируется, а импортируется общими модулями.

## Инструкция по созданию отчёта (contact_parser/memory-bank/reports/README.md)
А также важное:
- Дата/время:
- Версия кода (коммит/ветка):
- Режим запуска и параметры:
- Итоги: кол-во писем, валидных JSON, новых организаций/контактов/КП, записей interactions, размер Inbox.
- Замечания: где падали валидации, какие поля пустые, что улучшить в промпте/правилах.
- Ссылки: путь к `crm.db`, к `data/llm_results/...`, к логам.

## UX — открытие письма
- Кнопка **«Открыть письмо»** (основная): `.eml` (если есть) → встроенный просмотрщик по JSON → копирование темы/отправителя.  
- Кнопки **«Скачать .eml»/«Скачать JSON»** — только в меню администратора.

## Стиль PR
- Небольшие атомарные коммиты, понятные сообщения.
- Покрытие критических блоков юнит‑тестами (валидатор/нормализатор/дедуп).

## Правила взаимодействия
Задавай вопросы: Если задача не ясна, задавай уточняющие вопросы.

Объясняй свой код: Кратко поясняй, что делает предложенный тобой код и почему ты выбрал именно такое решение.
Следуй инструкциям: Всегда опирайся на информацию из этого документа.