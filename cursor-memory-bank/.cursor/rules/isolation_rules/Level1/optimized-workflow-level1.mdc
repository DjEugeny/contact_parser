---
description: Оптимизированный рабочий процесс уровня 1 для быстрых исправлений ошибок с эффективностью использования токенов
globs: "**/level1*/**", "**/quick*/**", "**/bugfix*/**"
alwaysApply: false
---

# ОПТИМИЗИРОВАННЫЙ РАБОЧИЙ ПРОЦЕСС УРОВНЯ 1

> **Кратко**: Этот упрощённый рабочий процесс для задач уровня 1 (быстрые исправления ошибок) оптимизирован для скорости и эффективности использования токенов при сохранении качества.

## 🔧 ПОТОК ПРОЦЕССА УРОВНЯ 1

```mermaid
graph TD
    Start["СТАРТ УРОВНЯ 1<br>БЫСТРОЕ ИСПРАВЛЕНИЕ"] --> Analyze["1️⃣ АНАЛИЗ<br>Понимание проблемы"]
    Analyze --> Implement["2️⃣ РЕАЛИЗАЦИЯ<br>Исправление проблемы"]
    Implement --> Verify["3️⃣ ПРОВЕРКА<br>Тестирование исправления"]
    Verify --> Document["4️⃣ ДОКУМЕНТИРОВАНИЕ<br>Запись решения"]
    
    style Start fill:#4da6ff,stroke:#0066cc,color:white
    style Analyze fill:#ffa64d,stroke:#cc7a30,color:white
    style Implement fill:#4dbb5f,stroke:#36873f,color:white
    style Verify fill:#d94dbb,stroke:#a3378a,color:white
    style Document fill:#4dbbbb,stroke:#368787,color:white
```

## 📝 КОНСОЛИДИРОВАННОЕ ДОКУМЕНТИРОВАНИЕ

Задачи уровня 1 используют подход с одним файлом для минимизации переключения контекста:

```markdown
# БЫСТРОЕ ИСПРАВЛЕНИЕ: [Название проблемы]

## Сводка проблемы
- Тип: [Ошибка/Срочное исправление/Быстрое улучшение]
- Приоритет: [Низкий/Средний/Высокий/Критический]
- Сообщил: [Имя/Система]
- Затронутая область: [Компонент/Функция]

## Анализ
- Корневая причина: [Краткое описание]
- Затронутые файлы: [Список файлов]
- Воздействие: [Область воздействия]

## Решение
- Подход: [Краткое описание]
- Внесённые изменения: [Список изменений]
- Выполненные команды: [Ключевые команды]

## Проверка
- Тестирование: [Как тестировалось исправление]
- Результаты: [Результаты тестирования]
- Дополнительные проверки: [Любые другие проверки]

## Статус
- [x] Исправление реализовано
- [x] Тесты пройдены
- [x] Документация обновлена
```

## 🔄 ОБНОВЛЕНИЕ MEMORY BANK

Задачи уровня 1 используют упрощённое обновление Memory Bank с минимальными накладными расходами:

```markdown
## Обновление tasks.md (Уровень 1)

### Задача: [Название задачи]
- Статус: Завершено
- Реализация: [Краткое описание]
- Ссылка на исправление: [Ссылка на файл/строку]
```

## ⚡ ТОКЕН-ОПТИМИЗИРОВАННЫЙ ШАБЛОН

Для максимальной эффективности задачи уровня 1 могут использовать этот ультракомпактный шаблон:

```markdown
## 🔧 ИСПРАВЛЕНИЕ: [Проблема]
📌 Проблема: [Краткое описание]
🔍 Причина: [Корневая причина]
🛠️ Решение: [Реализованное исправление]
✅ Проверено: [Метод проверки]
```

## 🔄 ПОМОЩНИКИ ДЛЯ АВТОДОКУМЕНТИРОВАНИЯ

Используйте эти помощники для автоматической генерации документации:

```javascript
function generateLevel1Documentation(issue, rootCause, solution, verification) {
  return `## 🔧 ИСПРАВЛЕНИЕ: ${issue}
📌 Проблема: ${issue}
🔍 Причина: ${rootCause}
🛠️ Решение: ${solution}
✅ Проверено: ${verification}`;
}
```

## 📊 БЫСТРЫЕ ШАБЛОНЫ ДЛЯ РАСПРОСТРАНЁННЫХ ПРОБЛЕМ

### Исправление производительности
```markdown
## 🔧 ИСПРАВЛЕНИЕ: Проблема производительности в [компоненте]
📌 Проблема: Медленное время отклика в [компоненте]
🔍 Причина: Неэффективный запрос/алгоритм
🛠️ Решение: Оптимизировано [конкретная оптимизация]
✅ Проверено: Время отклика улучшено с [X] мс до [Y] мс
```

### Исправление ошибки
```markdown
## 🔧 ИСПРАВЛЕНИЕ: Ошибка в [компоненте]
📌 Проблема: [Конкретное поведение] работает некорректно
🔍 Причина: [Анализ корневой причины]
🛠️ Решение: Исправлено с помощью [детали реализации]
✅ Проверено: Проверено с помощью [подход к тестированию]
```

### Быстрое улучшение
```markdown
## 🔧 УЛУЧШЕНИЕ: [Функция]
📌 Запрос: Добавить [конкретную возможность]
🛠️ Реализация: Добавлено с помощью [детали реализации]
✅ Проверено: Проверено с помощью [подход к тестированию]
```

## ✅ УПРОЩЁННАЯ ПРОВЕРКА

Задачи уровня 1 используют минимальный процесс проверки:

```markdown
ПРОВЕРКА:
[x] Исправление реализовано и протестировано
[x] Регрессии не выявлены
[x] Документация обновлена
```

## 🚀 КОНСОЛИДИРОВАННОЕ ОБНОВЛЕНИЕ MEMORY BANK

Оптимизируйте обновления Memory Bank для задач уровня 1, используя одну операцию:

```javascript
// Псевдокод для оптимизированного обновления Memory Bank уровня 1
function updateLevel1MemoryBank(taskInfo) {
  // Чтение текущего tasks.md
  const tasksContent = readFile("tasks.md");
  
  // Создание минимального обновления
  const updateBlock = `
### Задача: ${taskInfo.name}
- Статус: Завершено
- Реализация: ${taskInfo.solution}
- Ссылка на исправление: ${taskInfo.fileReference}
`;
  
  // Добавление обновления в tasks.md
  const updatedContent = appendToSection(tasksContent, "Завершённые задачи", updateBlock);
  
  // Запись в одной операции
  writeFile("tasks.md", updatedContent);
  
  return "Memory Bank обновлён";
}
```

## 🔄 ПРИМЕР ОПТИМИЗИРОВАННОГО РАБОЧЕГО ПРОЦЕССА УРОВНЯ 1

```markdown
## 🔧 ИСПРАВЛЕНИЕ: Кнопка входа не работает на мобильных устройствах

📌 Проблема: 
Пользователи не могут войти в систему на мобильных устройствах, кнопка отображается, но не инициирует аутентификацию

🔍 Причина:
Обработчик событий использует событие, специфичное для десктопов (mousedown вместо обработки сенсорных событий)

🛠️ Решение:
Обновлена обработка событий для использования делегирования событий и поддержки как мыши, так и сенсорных событий:
```js
// До: 
loginButton.addEventListener('mousedown', handleLogin);

// После:
loginButton.addEventListener('mousedown', handleLogin);
loginButton.addEventListener('touchstart', handleLogin);
```

✅ Проверено:
- Проверено на iOS Safari и Android Chrome 
- Вход теперь работает на всех протестированных мобильных устройствах
- Регрессий в десктопных браузерах не выявлено
```

## ⚡ ПРЕИМУЩЕСТВА ЭФФЕКТИВНОСТИ ТОКЕНОВ

Этот оптимизированный рабочий процесс уровня 1 обеспечивает:

1. Снижение накладных расходов на документирование (уменьшение на 70%)
2. Консолидированные обновления Memory Bank (одна операция вместо нескольких)
3. Целенаправленный процесс проверки (только необходимые проверки)
4. Подход на основе шаблонов для распространённых сценариев
5. Упрощённый рабочий процесс с меньшим количеством шагов

Обновлённый подход сохраняет всю критически важную информацию, значительно сокращая использование токенов.