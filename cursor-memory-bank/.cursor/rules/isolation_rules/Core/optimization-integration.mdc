---
description: Центр интеграции оптимизаций Memory Bank
globs: "**/optimization*/**", "**/integration*/**"
alwaysApply: false
---

# ИНТЕГРАЦИЯ ОПТИМИЗАЦИЙ MEMORY BANK

> **TL;DR:** Этот файл служит точкой интеграции для всех оптимизаций Memory Bank, координируя работу различных компонентов оптимизации для их бесшовного взаимодействия.

## 🔄 ПОТОК ИНТЕГРАЦИИ ОПТИМИЗАЦИЙ

```mermaid
graph TD
    Start["Инициализация<br>Memory Bank"] --> HRL["Иерархическая<br>загрузка правил"]
    HRL --> ACM["Адаптивная<br>модель сложности"]
    ACM --> DCM["Динамическое<br>управление контекстом"]
    DCM --> TMO["Оптимизация<br>переходов"]
    
    subgraph "Оптимизации для конкретных уровней"
        L1["Оптимизации<br>уровня 1"]
        L2["Оптимизации<br>уровня 2"]
        L3["Оптимизации<br>уровня 3"]
        L4["Оптимизации<br>уровня 4"]
    end
    
    ACM --> L1 & L2 & L3 & L4
    
    L1 & L2 & L3 & L4 --> CPO["Оптимизация<br>творческой фазы"]
    
    CPO --> PDO["Прогрессивное<br>документирование"]
    TMO --> PDO
    
    PDO --> MBO["Оптимизация<br>Memory Bank"]
    
    style Start fill:#4da6ff,stroke:#0066cc,color:white
    style HRL fill:#ffa64d,stroke:#cc7a30,color:white
    style ACM fill:#4dbb5f,stroke:#36873f,color:white
    style DCM fill:#d94dbb,stroke:#a3378a,color:white
    style TMO fill:#4dbbbb,stroke:#368787,color:white
    style CPO fill:#e699d9,stroke:#d94dbb,color:white
    style PDO fill:#d971ff,stroke:#a33bc2,color:white
    style MBO fill:#ff71c2,stroke:#c23b8a,color:white
```

## 📋 РЕЕСТР КОМПОНЕНТОВ ОПТИМИЗАЦИИ

```javascript
// Псевдокод реестра компонентов оптимизации
const optimizationRegistry = {
  // Основные оптимизации
  hierarchicalRuleLoading: {
    file: "Core/hierarchical-rule-loading.mdc",
    dependencies: [],
    priority: 1
  },
  adaptiveComplexityModel: {
    file: "main-optimized.mdc",
    dependencies: ["hierarchicalRuleLoading"],
    priority: 2
  },
  modeTransitionOptimization: {
    file: "Core/mode-transition-optimization.mdc",
    dependencies: ["hierarchicalRuleLoading", "adaptiveComplexityModel"],
    priority: 3
  },
  
  // Оптимизации для конкретных уровней
  level1Optimization: {
    file: "Level1/optimized-workflow-level1.mdc",
    dependencies: ["adaptiveComplexityModel"],
    priority: 4
  },
  
  // Оптимизации для конкретных функций
  creativePhaseOptimization: {
    file: "Phases/CreativePhase/optimized-creative-template.mdc",
    dependencies: ["hierarchicalRuleLoading", "adaptiveComplexityModel"],
    priority: 5
  }
};
```

## 🔄 ПОСЛЕДОВАТЕЛЬНОСТЬ ИНИЦИАЛИЗАЦИИ ОПТИМИЗАЦИЙ

```mermaid
sequenceDiagram
    participant MB as Memory Bank
    participant Reg as Реестр оптимизаций
    participant HRL as Иерархическая загрузка правил
    participant ACM as Адаптивная сложность
    participant TMO as Оптимизация переходов
    participant CPO as Оптимизация творческой фазы
    
    MB->>Reg: Запрос инициализации оптимизаций
    Reg->>Reg: Сортировка оптимизаций по приоритету и зависимостям
    Reg->>HRL: Инициализация (Приоритет 1)
    HRL-->>Reg: Инициализация завершена
    Reg->>ACM: Инициализация (Приоритет 2)
    ACM->>HRL: Запрос сервисов загрузки правил
    HRL-->>ACM: Предоставление загрузки правил
    ACM-->>Reg: Инициализация завершена
    Reg->>TMO: Инициализация (Приоритет 3)
    TMO->>HRL: Запрос сервисов загрузки правил
    TMO->>ACM: Запрос модели сложности
    HRL-->>TMO: Предоставление загрузки правил
    ACM-->>TMO: Предоставление модели сложности
    TMO-->>Reg: Инициализация завершена
    Reg->>CPO: Инициализация (Финальная)
    CPO->>HRL: Запрос сервисов загрузки правил
    CPO->>ACM: Запрос модели сложности
    CPO->>TMO: Запрос сервисов переходов
    HRL-->>CPO: Предоставление загрузки правил
    ACM-->>CPO: Предоставление модели сложности
    TMO-->>CPO: Предоставление сервисов переходов
    CPO-->>Reg: Инициализация завершена
    Reg-->>MB: Все оптимизации инициализированы
```

## 🔍 КОНФИГУРАЦИЯ ОПТИМИЗАЦИЙ

```javascript
// Псевдокод конфигурации оптимизаций
const optimizationConfig = {
  // Настройки оптимизации токенов
  tokenOptimization: {
    enableHierarchicalLoading: true,
    enableProgressiveDocumentation: true,
    enableLazyRuleLoading: true,
    enableContextPruning: true
  },
  
  // Настройки сохранения контекста
  contextPreservation: {
    preserveDesignDecisions: true,
    preserveImplementationContext: true,
    preserveUserPreferences: true,
    contextCompressionLevel: "high" // none, low, medium, high
  },
  
  // Оптимизация документации
  documentationOptimization: {
    level1DocumentationLevel: "minimal", // minimal, standard, comprehensive
    level2DocumentationLevel: "standard",
    level3DocumentationLevel: "comprehensive",
    level4DocumentationLevel: "comprehensive",
    enableProgressiveDisclosure: true,
    enableTemplateCaching: true
  }
};
```

## 📊 МОНИТОРИНГ ОПТИМИЗАЦИЙ

```mermaid
graph TD
    Monitor["Мониторинг<br>оптимизаций"] --> TokenUsage["Отслеживание<br>использования токенов"]
    Monitor --> ContextEfficiency["Эффективность<br>контекста"]
    Monitor --> RuleLoadingStats["Статистика<br>загрузки правил"]
    Monitor --> DocumentationSize["Размер<br>документации"]
    
    TokenUsage --> Dashboard["Панель<br>оптимизаций"]
    ContextEfficiency --> Dashboard
    RuleLoadingStats --> Dashboard
    DocumentationSize --> Dashboard
    
    Dashboard --> Feedback["Цикл обратной<br>связи оптимизаций"]
    Feedback --> Config["Конфигурация<br>оптимизаций"]
    Config --> Monitor
    
    style Monitor fill:#4da6ff,stroke:#0066cc,color:white
    style Dashboard fill:#ffa64d,stroke:#cc7a30,color:white
    style Feedback fill:#4dbb5f,stroke:#36873f,color:white
    style Config fill:#d94dbb,stroke:#a3378a,color:white
```

## 📈 МЕТРИКИ ОПТИМИЗАЦИЙ

```markdown
# Метрики оптимизаций

## Использование токенов
- Загрузка основных правил: [X] токенов
- Режимно-специфичные правила: [Y] токенов
- Документация творческой фазы: [Z] токенов
- Общее сокращение токенов: [P]%

## Эффективность контекста
- Использование контекста: [Q]%
- Потери контекста: [R]%
- Эффективная ёмкость токенов: [S] токенов

## Загрузка правил
- Загружено правил: [T] / [U] (Всего)
- Лениво загруженные правила: [V]
- Кэшированные правила: [W]

## Документация
- Размер документации уровня 1: [X] токенов
- Размер документации уровня 2: [Y] токенов
- Размер документации уровня 3: [Z] токенов
- Размер документации уровня 4: [AA] токенов
```

## 🔄 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ ИНТЕГРАЦИИ

### Инициализация всех оптимизаций

```javascript
// Псевдокод для инициализации всех оптимизаций
function initializeMemoryBankOptimizations() {
  // Загрузка реестра оптимизаций
  const registry = loadOptimizationRegistry();
  
  // Сортировка по приоритету и зависимостям
  const sortedOptimizations = sortOptimizations(registry);
  
  // Инициализация каждой оптимизации по порядку
  for (const opt of sortedOptimizations) {
    initializeOptimization(opt);
  }
  
  // Настройка параметров оптимизации
  configureOptimizations(loadOptimizationConfig());
  
  // Запуск мониторинга
  initializeOptimizationMonitoring();
  
  return "Оптимизации Memory Bank инициализированы";
}
```

### Использование оптимизированной творческой фазы

```markdown
// Использование оптимизированной творческой фазы с прогрессивным документированием

// Инициализация с минимальной документацией
📌 НАЧАЛО ТВОРЧЕСКОЙ ФАЗЫ: Система аутентификации
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1️⃣ ПРОБЛЕМА
   Описание: Разработка системы аутентификации для приложения
   Требования: Безопасность, масштабируемость, поддержка SSO, простота поддержки
   Ограничения: Должна работать с существующей базой пользователей, время ответа <100 мс

2️⃣ ВАРИАНТЫ
   Вариант A: JWT-базированная stateless-аутентификация
   Вариант B: Сессионная аутентификация с Redis
   Вариант C: Реализация OAuth2

// Прогрессивное добавление деталей по необходимости
3️⃣ АНАЛИЗ
   | Критерий | JWT | Сессии | OAuth2 |
   |-----------|-----|----------|--------|
   | Безопасность | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
   | Масштабируемость | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
   | Сложность | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
   
// Фокус на решении и реализации
4️⃣ РЕШЕНИЕ
   Выбрано: Вариант A: JWT-базированная аутентификация с токенами обновления
   Обоснование: Лучший баланс производительности и масштабируемости
   
5️⃣ ЗАМЕТКИ ПО РЕАЛИЗАЦИИ
   - Использовать алгоритм HS256 для подписи токенов
   - Реализовать краткосрочные токены доступа (15 мин)
   - Хранить чёрный список токенов в Redis для отзыва
```

## 🔄 ПРИМЕР ПЕРЕХОДА МЕЖДУ РЕЖИМАМИ

```markdown
// Оптимизированный переход из режима CREATIVE в IMPLEMENT

# ПЕРЕХОД МЕЖДУ РЕЖИМАМИ: CREATIVE → IMPLEMENT

## Краткое содержание контекста
- Задача: Реализация системы аутентификации
- Сложность: Уровень 3
- Решение: JWT-базированная аутентификация с токенами обновления

## Ключевой контекст
- Требования безопасности проверены
- Выбор алгоритма: HS256
- Жизненный цикл токенов: 15 мин для доступа / 7 дней для обновления

## Следующие шаги
1. Реализовать модуль генерации JWT
2. Создать middleware для проверки токенов
3. Реализовать обработку токенов обновления

// Переход происходит с сохранением контекста
// Режим IMPLEMENT продолжает работу с доступным контекстом
```

## 🔄 ПРИМЕР ИЕРАРХИЧЕСКОЙ ЗАГРУЗКИ ПРАВИЛ

```javascript
// Псевдокод примера иерархической загрузки правил

// Начальная загрузка - только основные правила
loadCoreRules();

// Определение сложности
const complexity = determineComplexity();

// Загрузка режимно-специфичных основных правил
loadModeEssentialRules("CREATIVE");

// Регистрация ленивых загрузчиков для специализированных правил
registerLazyLoader("architecture", () => loadRule("creative-phase-architecture.mdc"));
registerLazyLoader("algorithm", () => loadRule("creative-phase-algorithm.mdc"));
registerLazyLoader("uiux", () => loadRule("creative-phase-uiux.mdc"));

// Позже, когда требуется проектирование архитектуры:
const architectureRule = loadSpecializedRule("architecture");
// Правило архитектуры загружается только при необходимости
```

Эти интегрированные оптимизации работают вместе, обеспечивая значительно более эффективную систему Memory Bank при сохранении всей функциональности.