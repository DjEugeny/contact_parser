---
description: Визуальная карта процесса для режима VAN QA (Техническая точка входа валидации)
globs: van-qa-main.mdc
alwaysApply: false
---
# РЕЖИМ VAN: ТЕХНИЧЕСКАЯ ВАЛИДАЦИЯ QA (Основная точка входа)

> **КРАТКО:** Это точка входа для процесса валидации QA, который выполняется *после* режима CREATIVE и *перед* режимом BUILD. Он обеспечивает выполнение технических требований перед началом реализации.

## 📣 КАК ИСПОЛЬЗОВАТЬ ЭТИ ПРАВИЛА QA

Для доступа к любому правилу или компоненту валидации QA используйте инструмент `fetch_rules` с точными именами правил:

```
// КРИТИЧНО: Всегда используйте fetch_rules для загрузки компонентов валидации
// Для подробных примеров и руководства загрузите:
// isolation_rules/visual-maps/van-qa-utils/rule-calling-guide
```

## 🚀 АКТИВАЦИЯ РЕЖИМА VAN QA

После завершения режима CREATIVE, когда пользователь вводит "VAN QA", отвечайте:

```mermaid
graph TD
    UserQA["Пользователь вводит: QA"] --> HighPriority["⚠️ КОМАНДА ВЫСШЕГО ПРИОРИТЕТА"]
    HighPriority --> CurrentTask["Приостановить текущую задачу/процесс"]
    CurrentTask --> LoadQA["Загрузить основную карту QA (этот файл)"]
    LoadQA --> RunQA["Запустить процесс валидации QA"]
    RunQA --> QAResults{"Результаты QA"}
    
    QAResults -->|"ПРОЙДЕНО"| ResumeFlow["Возобновить предыдущий процесс"]
    QAResults -->|"НЕ ПРОЙДЕНО"| FixIssues["Исправить выявленные проблемы"]
    FixIssues --> ReRunQA["Повторно запустить валидацию QA"]
    ReRunQA --> QAResults
    
    style UserQA fill:#f8d486,stroke:#e8b84d,color:black
    style HighPriority fill:#ff0000,stroke:#cc0000,color:white,stroke-width:3px
    style LoadQA fill:#4da6ff,stroke:#0066cc,color:white
    style RunQA fill:#4da6ff,stroke:#0066cc,color:white
    style QAResults fill:#f6546a,stroke:#c30052,color:white
```

### Правила прерывания QA

1. **Немедленный приоритет:** Команда `QA` прерывает все процессы.
2. **Загрузка и выполнение:** Загрузите эту карту (`van-qa-main.mdc`) и её компоненты (см. ниже).
3. **Приоритет исправлений:** Исправления имеют приоритет над ожидающими переключениями режимов.
4. **Возобновление:** При успешном прохождении возобновите предыдущий процесс.

```
⚠️ АКТИВИРОВАН ПЕРЕХОД НА QA
Все другие процессы приостановлены
Запущены проверки валидации QA...
Все выявленные проблемы ДОЛЖНЫ быть исправлены перед продолжением нормального процесса
```

## 🔍 ОБЗОР ТЕХНИЧЕСКОЙ ВАЛИДАЦИИ

Четырёхэтапный процесс валидации с выборочной загрузкой:

```mermaid
graph TD
    VANQA["РЕЖИМ VAN QA"] --> FourChecks["ЧЕТЫРЁХЭТАПНАЯ ВАЛИДАЦИЯ"]
    
    FourChecks --> DepCheck["1️⃣ ПРОВЕРКА ЗАВИСИМОСТЕЙ
    Load: van-qa-checks/dependency-check.mdc"]
    DepCheck --> ConfigCheck["2️⃣ ВАЛИДАЦИЯ КОНФИГУРАЦИИ
    Load: van-qa-checks/config-check.mdc"]
    ConfigCheck --> EnvCheck["3️⃣ ВАЛИДАЦИЯ СРЕДЫ
    Load: van-qa-checks/environment-check.mdc"]
    EnvCheck --> MinBuildCheck["4️⃣ ТЕСТ МИНИМАЛЬНОЙ СБОРКИ
    Load: van-qa-checks/build-test.mdc"]
    
    MinBuildCheck --> ValidationResults{"Все проверки<br>пройдены?"}
    ValidationResults -->|"Да"| SuccessReport["СФОРМИРОВАТЬ ОТЧЁТ ОБ УСПЕХЕ
    Load: van-qa-utils/reports.mdc"]
    ValidationResults -->|"Нет"| FailureReport["СФОРМИРОВАТЬ ОТЧЁТ О НЕУДАЧЕ
    Load: van-qa-utils/reports.mdc"]
    
    SuccessReport --> BUILD_Transition["Запустить режим BUILD
    Load: van-qa-utils/mode-transitions.mdc"]
    FailureReport --> FixIssues["Исправить технические проблемы
    Load: van-qa-utils/common-fixes.mdc"]
    FixIssues --> ReValidate["Повторная валидация (перезапустить VAN QA)"]
    ReValidate --> FourChecks
    
    style VANQA fill:#4da6ff,stroke:#0066cc,color:white
    style FourChecks fill:#f6546a,stroke:#c30052,color:white
    style ValidationResults fill:#f6546a,stroke:#c30052,color:white
    style BUILD_Transition fill:#10b981,stroke:#059669,color:white
    style FixIssues fill:#ff5555,stroke:#dd3333,color:white
```

## 🔄 ИНТЕГРАЦИЯ С ДИЗАЙН-РЕШЕНИЯМИ

Читает выходные данные фазы CREATIVE для информирования валидации:

```mermaid
graph TD
    Start["Чтение дизайн-решений"] --> ReadCreative["Анализ документации<br>фазы CREATIVE"]
    ReadCreative --> ExtractTech["Извлечение технологических<br>решений"]
    ExtractTech --> ExtractDeps["Извлечение необходимых<br>зависимостей"]
    ExtractDeps --> BuildValidationPlan["Создание плана<br>валидации"]
    BuildValidationPlan --> StartValidation["Запуск четырёхэтапного<br>процесса валидации"]
    
    style Start fill:#4da6ff,stroke:#0066cc,color:white
    style ExtractTech fill:#f6546a,stroke:#c30052,color:white
    style BuildValidationPlan fill:#10b981,stroke:#059669,color:white
    style StartValidation fill:#f6546a,stroke:#c30052,color:white
```

## 📋 ПОСЛЕДОВАТЕЛЬНОСТЬ ЗАГРУЗКИ КОМПОНЕНТОВ

Процесс валидации QA следует этой последовательности выборочной загрузки:

1. **Основная точка входа (этот файл)**: `van-qa-main.mdc`
2. **Проверки валидации**:
   - `van-qa-checks/dependency-check.mdc`
   - `van-qa-checks/config-check.mdc`
   - `van-qa-checks/environment-check.mdc`
   - `van-qa-checks/build-test.mdc`
3. **Утилиты (по необходимости)**:
   - `van-qa-utils/reports.mdc`
   - `van-qa-utils/common-fixes.mdc`
   - `van-qa-utils/mode-transitions.mdc`

## 📋 ФИНАЛЬНАЯ КОНТРОЛЬНАЯ ТОЧКА ВАЛИДАЦИИ QA

```
✓ КОНТРОЛЬНАЯ ТОЧКА СЕКЦИИ: ВАЛИДАЦИЯ QA
- Проверка зависимостей пройдена? [ДА/НЕТ]
- Валидация конфигурации пройдена? [ДА/НЕТ]
- Валидация среды пройдена? [ДА/НЕТ]
- Тест минимальной сборки пройден? [ДА/НЕТ]

→ Если все ДА: Готово к переходу в режим BUILD.
→ Если есть НЕТ: Исправьте выявленные проблемы и перезапустите VAN QA.
```

**Следующий шаг (при ПРОХОЖДЕНИИ):** Запустите режим BUILD (загрузите `van-qa-utils/mode-transitions.mdc`).
**Следующий шаг (при НЕУДАЧЕ):** Устраните проблемы (загрузите `van-qa-utils/common-fixes.mdc`) и перезапустите `VAN QA`.