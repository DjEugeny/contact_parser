---
description: Визуальная карта процесса для режима VAN (Точка входа/Индекс)
globs: van-mode-map.mdc
alwaysApply: false
---
# РЕЖИМ VAN: КАРТА ПРОЦЕССА ИНИЦИАЛИЗАЦИИ

🚨 ОБЯЗАТЕЛЬНЫЙ ПЕРВЫЙ ШАГ: СОЗДАНИЕ БАНКА ПАМЯТИ 🚨
НИКАКИЕ ОПЕРАЦИИ НЕ МОГУТ БЫТЬ ВЫПОЛНЕНЫ БЕЗ СТРУКТУРЫ БАНКА ПАМЯТИ

> **КРАТКОЕ СОДЕРЖАНИЕ:** Эта визуальная карта определяет процесс режима VAN для инициализации проекта, анализа задач и технической валидации. Она направляет пользователей через обнаружение платформы, проверку файлов, определение сложности и техническую валидацию для обеспечения правильной настройки перед реализацией.

## 🧭 ПОТОК ПРОЦЕССА РЕЖИМА VAN

```mermaid
graph TD
    Start["НАЧАТЬ РЕЖИМ VAN"] --> PlatformDetect["ОБНАРУЖЕНИЕ ПЛАТФОРМЫ"]
    PlatformDetect --> DetectOS["Обнаружить операционную систему"]
    DetectOS --> CheckPath["Проверить формат разделителя пути"]
    CheckPath --> AdaptCmds["Адаптировать команды при необходимости"]
    AdaptCmds --> PlatformCP["⛔ КОНТРОЛЬНАЯ ТОЧКА ПЛАТФОРМЫ"]
    
    %% Добавление критической контрольной точки банка памяти
    PlatformCP --> MemoryBankCheck{"Банк памяти<br>существует?"}
    MemoryBankCheck -->|"Нет"| CreateMemoryBank["СОЗДАТЬ БАНК ПАМЯТИ<br>[КРИТИЧЕСКИЙ ШАГ]"]
    MemoryBankCheck -->|"Да"| BasicFileVerify["БАЗОВАЯ ПРОВЕРКА ФАЙЛОВ"]
    CreateMemoryBank --> MemoryBankCP["⛔ ПРОВЕРКА БАНКА ПАМЯТИ [ОБЯЗАТЕЛЬНО]"]
    MemoryBankCP --> BasicFileVerify
    
    %% Базовая проверка файлов с контрольной точкой
    BasicFileVerify --> BatchCheck["Пакетная проверка основных компонентов"]
    BatchCheck --> BatchCreate["Пакетное создание основной структуры"]
    BatchCreate --> BasicFileCP["⛔ КОНТРОЛЬНАЯ ТОЧКА БАЗОВЫХ ФАЙЛОВ"]
    
    %% Раннее определение сложности
    BasicFileCP --> EarlyComplexity["РАННЕЕ ОПРЕДЕЛЕНИЕ СЛОЖНОСТИ"]
    EarlyComplexity --> AnalyzeTask["Анализировать требования задачи"]
    AnalyzeTask --> EarlyLevelCheck{"Уровень сложности?"}
    
    %% Пути обработки уровней
    EarlyLevelCheck -->|"Уровень 1"| ComplexityCP["⛔ КОНТРОЛЬНАЯ ТОЧКА СЛОЖНОСТИ"]
    EarlyLevelCheck -->|"Уровни 2-4"| CRITICALGATE["🚫 КРИТИЧЕСКИЙ ШЛЮЗ: ПРИНУДИТЕЛЬНЫЙ ПЕРЕХОД РЕЖИМА"]
    CRITICALGATE --> ForceExit["Перейти в режим PLAN"]
    
    %% Уровень 1 продолжается нормально
    ComplexityCP --> InitSystem["ИНИЦИАЛИЗИРОВАТЬ БАНК ПАМЯТИ"]
    InitSystem --> Complete1["ИНИЦИАЛИЗАЦИЯ УРОВНЯ 1 ЗАВЕРШЕНА"]
    
    %% Для задач уровня 2+ после режимов PLAN и CREATIVE
    ForceExit -.-> OtherModes["Режимы PLAN → CREATIVE"]
    OtherModes -.-> VANQA["РЕЖИМ VAN QA"]
    VANQA --> QAProcess["Процесс технической валидации"]
    QAProcess --> QACheck{"Все проверки пройдены?"}
    QACheck -->|"Да"| BUILD["Перейти в режим BUILD"]
    QACheck -->|"Нет"| FixIssues["Исправить технические проблемы"]
    FixIssues --> QAProcess
    
    %% Стилизация узлов
    style PlatformCP fill:#f55,stroke:#d44,color:white
    style MemoryBankCP fill:#ff0000,stroke:#990000,color:white,stroke-width:3px
    style MemoryBankCheck fill:#ff0000,stroke:#990000,color:white,stroke-width:3px
    style CreateMemoryBank fill:#ff0000,stroke:#990000,color:white,stroke-width:3px
    style BasicFileCP fill:#f55,stroke:#d44,color:white
    style ComplexityCP fill:#f55,stroke:#d44,color:white
    style CRITICALGATE fill:#ff0000,stroke:#990000,color:white,stroke-width:3px
    style ForceExit fill:#ff0000,stroke:#990000,color:white,stroke-width:2px
    style VANQA fill:#4da6ff,stroke:#0066cc,color:white,stroke-width:3px
    style QAProcess fill:#4da6ff,stroke:#0066cc,color:white
    style QACheck fill:#4da6ff,stroke:#0066cc,color:white
    style FixIssues fill:#ff5555,stroke:#dd3333,color:white
```

## 🌐 ПРОЦЕСС ОБНАРУЖЕНИЯ ПЛАТФОРМЫ

```mermaid
graph TD
    PD["Обнаружение платформы"] --> CheckOS["Обнаружить операционную систему"]
    CheckOS --> Win["Windows"]
    CheckOS --> Mac["macOS"]
    CheckOS --> Lin["Linux"]
    
    Win & Mac & Lin --> Adapt["Адаптировать команды<br>для платформы"]
    
    Win --> WinPath["Путь: Обратная косая черта (\\)"]
    Mac --> MacPath["Путь: Прямая косая черта (/)"]
    Lin --> LinPath["Путь: Прямая косая черта (/)"]
    
    Win --> WinCmd["Адаптация команд:<br>dir, icacls и т.д."]
    Mac --> MacCmd["Адаптация команд:<br>ls, chmod и т.д."]
    Lin --> LinCmd["Адаптация команд:<br>ls, chmod и т.д."]
    
    WinPath & MacPath & LinPath --> PathCP["Контрольная точка<br>разделителя пути"]
    WinCmd & MacCmd & LinCmd --> CmdCP["Контрольная точка<br>команд"]
    
    PathCP & CmdCP --> PlatformComplete["Обнаружение платформы<br>завершено"]
    
    style PD fill:#4da6ff,stroke:#0066cc,color:white
    style PlatformComplete fill:#10b981,stroke:#059669,color:white
```

## 📁 ПРОЦЕСС ПРОВЕРКИ ФАЙЛОВ

```mermaid
graph TD
    FV["Проверка файлов"] --> CheckFiles["Проверить основные файлы"]
    CheckFiles --> CheckMB["Проверить структуру<br>банка памяти"]
    CheckMB --> MBExists{"Банк памяти<br>существует?"}
    
    MBExists -->|"Да"| VerifyMB["Проверить содержимое<br>банка памяти"]
    MBExists -->|"Нет"| CreateMB["Создать структуру<br>банка памяти"]
    
    CheckFiles --> CheckDocs["Проверить файлы<br>документации"]
    CheckDocs --> DocsExist{"Документация<br>существует?"}
    
    DocsExist -->|"Да"| VerifyDocs["Проверить структуру<br>документации"]
    DocsExist -->|"Нет"| CreateDocs["Создать структуру<br>документации"]
    
    VerifyMB & CreateMB --> MBCP["Контрольная точка<br>банка памяти"]
    VerifyDocs & CreateDocs --> DocsCP["Контрольная точка<br>документации"]
    
    MBCP & DocsCP --> FileComplete["Проверка файлов<br>завершена"]
    
    style FV fill:#4da6ff,stroke:#0066cc,color:white
    style FileComplete fill:#10b981,stroke:#059669,color:white
    style MBCP fill:#f6546a,stroke:#c30052,color:white
    style DocsCP fill:#f6546a,stroke:#c30052,color:white
```

## 🧩 ПРОЦЕСС ОПРЕДЕЛЕНИЯ СЛОЖНОСТИ

```mermaid
graph TD
    CD["Определение<br>сложности"] --> AnalyzeTask["Анализировать<br>требования задачи"]
    
    AnalyzeTask --> CheckKeywords["Проверить ключевые слова<br>задачи"]
    CheckKeywords --> ScopeCheck["Оценить влияние<br>объема"]
    ScopeCheck --> RiskCheck["Оценить уровень<br>риска"]
    RiskCheck --> EffortCheck["Оценить усилия<br>для реализации"]
    
    EffortCheck --> DetermineLevel{"Определить<br>уровень сложности"}
    DetermineLevel -->|"Уровень 1"| L1["Уровень 1:<br>Быстрое исправление ошибки"]
    DetermineLevel -->|"Уровень 2"| L2["Уровень 2:<br>Простое улучшение"]
    DetermineLevel -->|"Уровень 3"| L3["Уровень 3:<br>Средняя функциональность"]
    DetermineLevel -->|"Уровень 4"| L4["Уровень 4:<br>Сложная система"]
    
    L1 --> CDComplete["Определение сложности<br>завершено"]
    L2 & L3 & L4 --> ModeSwitch["Принудительный переход<br>в режим PLAN"]
    
    style CD fill:#4da6ff,stroke:#0066cc,color:white
    style CDComplete fill:#10b981,stroke:#059669,color:white
    style ModeSwitch fill:#ff0000,stroke:#990000,color:white
    style DetermineLevel fill:#f6546a,stroke:#c30052,color:white
```

## 🔄 ПОЛНЫЙ РАБОЧИЙ ПРОЦЕСС С ВАЛИДАЦИЕЙ QA

Полный рабочий процесс включает техническую валидацию перед реализацией:

```mermaid
flowchart LR
    VAN1["РЕЖИМ VAN 
    (Начальный анализ)"] --> PLAN["РЕЖИМ PLAN
    (Планирование задачи)"]
    PLAN --> CREATIVE["РЕЖИМ CREATIVE
    (Дизайнерские решения)"]
    CREATIVE --> VANQA["РЕЖИМ VAN QA
    (Техническая валидация)"] 
    VANQA --> BUILD["РЕЖИМ BUILD
    (Реализация)"]
```

## 🔍 ОБЗОР ТЕХНИЧЕСКОЙ ВАЛИДАЦИИ

Процесс технической валидации VAN QA состоит из четырех ключевых точек валидации:

```mermaid
graph TD
    VANQA["РЕЖИМ VAN QA"] --> FourChecks["ЧЕТЫРЕХТОЧЕЧНАЯ ВАЛИДАЦИЯ"]
    
    FourChecks --> DepCheck["1️⃣ ПРОВЕРКА ЗАВИСИМОСТЕЙ<br>Проверить все необходимые пакеты"]
    DepCheck --> ConfigCheck["2️⃣ ВАЛИДАЦИЯ КОНФИГУРАЦИИ<br>Проверить формат и совместимость"]
    ConfigCheck --> EnvCheck["3️⃣ ВАЛИДАЦИЯ СРЕДЫ<br>Проверить среду сборки"]
    EnvCheck --> MinBuildCheck["4️⃣ МИНИМАЛЬНЫЙ ТЕСТ СБОРКИ<br>Проверить основную функциональность"]
    
    MinBuildCheck --> ValidationResults{"Все проверки<br>пройдены?"}
    ValidationResults -->|"Да"| SuccessReport["ГЕНЕРАЦИЯ ОТЧЕТА УСПЕХА"]
    ValidationResults -->|"Нет"| FailureReport["ГЕНЕРАЦИЯ ОТЧЕТА ПРОВАЛА"]
    
    SuccessReport --> BUILD["Перейти в режим BUILD"]
    FailureReport --> FixIssues["Исправить технические проблемы"]
    FixIssues --> ReValidate["Повторная валидация"]
    ReValidate --> ValidationResults
    
    style VANQA fill:#4da6ff,stroke:#0066cc,color:white
    style FourChecks fill:#f6546a,stroke:#c30052,color:white
    style ValidationResults fill:#f6546a,stroke:#c30052,color:white
    style BUILD fill:#10b981,stroke:#059669,color:white
    style FixIssues fill:#ff5555,stroke:#dd3333,color:white
```

## 📝 ФОРМАТ СТАТУСА ВАЛИДАЦИИ

Шаг валидации QA включает четкие индикаторы статуса:

```
╔═════════════════ 🔍 СТАТУС ВАЛИДАЦИИ QA ═════════════════╗
│ ✓ Дизайнерские решения   │ Проверено как реализуемое      │
│ ✓ Зависимости            │ Все необходимые пакеты установлены │
│ ✓ Конфигурации           │ Формат проверен для платформы  │
│ ✓ Среда                  │ Подходит для реализации        │
╚════════════════════════════════════════════════════════════╝
✅ ПРОВЕРЕНО - Можно переходить в режим BUILD
```

## 🚨 ТРИГГЕРЫ ПЕРЕХОДА РЕЖИМОВ

### Переход из VAN в PLAN
Для уровней сложности 2-4:
```
🚫 ОБНАРУЖЕНА ЗАДАЧА УРОВНЯ [2-4]
Реализация в режиме VAN ЗАБЛОКИРОВАНА
Эта задача ТРЕБУЕТ режим PLAN
Вы ДОЛЖНЫ перейти в режим PLAN для правильной документации и планирования
Введите 'PLAN' для перехода в режим планирования
```

### Переход из CREATIVE в VAN QA
После завершения режима CREATIVE:
```
⏭️ СЛЕДУЮЩИЙ РЕЖИМ: VAN QA
Для валидации технических требований перед реализацией введите 'VAN QA'
```

### Переход из VAN QA в BUILD
После успешной валидации:
```
✅ ТЕХНИЧЕСКАЯ ВАЛИДАЦИЯ ЗАВЕРШЕНА
Все предварительные условия успешно проверены
Теперь вы можете перейти в режим BUILD
Введите 'BUILD' для начала реализации
```

## 🔒 МЕХАНИЗМ ПРЕДОТВРАЩЕНИЯ ПЕРЕХОДА В РЕЖИМ BUILD

Система предотвращает переход в режим BUILD без прохождения валидации QA:

```mermaid
graph TD
    Start["Пользователь вводит: BUILD"] --> CheckQA{"Валидация QA<br>завершена?"}
    CheckQA -->|"Да и пройдена"| AllowBuild["Разрешить режим BUILD"]
    CheckQA -->|"Нет или провалена"| BlockBuild["ЗАБЛОКИРОВАТЬ РЕЖИМ BUILD"]
    BlockBuild --> Message["Показать:<br>⚠️ ТРЕБУЕТСЯ ВАЛИДАЦИЯ QA"]
    Message --> ReturnToVANQA["Запрос: Введите VAN QA"]
    
    style CheckQA fill:#f6546a,stroke:#c30052,color:white
    style BlockBuild fill:#ff0000,stroke:#990000,color:white,stroke-width:3px
    style Message fill:#ff5555,stroke:#dd3333,color:white
    style ReturnToVANQA fill:#4da6ff,stroke:#0066cc,color:white
```

## 🔄 ПРИОРИТЕТ КОМАНДЫ QA

Валидация QA может быть вызвана в любой момент процесса, и она имеет немедленный приоритет над любыми другими текущими шагами, включая принудительные переключения режимов:

```mermaid
graph TD
    UserQA["Пользователь вводит: QA"] --> HighPriority["⚠️ КОМАНДА ВЫСОКОГО ПРИОРИТЕТА"]
    HighPriority --> CurrentTask["Приостановить текущую задачу/процесс"]
    CurrentTask --> LoadQA["Загрузить карту режима QA"]
    LoadQA --> RunQA["Выполнить процесс валидации QA"]
    RunQA --> QAResults{"Результаты QA"}
    
    QAResults -->|"ПРОЙДЕНО"| ResumeFlow["Возобновить предыдущий процесс"]
    QAResults -->|"ПРОВАЛЕНО"| FixIssues["Исправить выявленные проблемы"]
    FixIssues --> ReRunQA["Повторно выполнить валидацию QA"]
    ReRunQA --> QAResults
    
    style UserQA fill:#f8d486,stroke:#e8b84d,color:black
    style HighPriority fill:#ff0000,stroke:#cc0000,color:white,stroke-width:3px
    style LoadQA fill:#4da6ff,stroke:#0066cc,color:white
    style RunQA fill:#4da6ff,stroke:#0066cc,color:white
    style QAResults fill:#f6546a,stroke:#c30052,color:white
```

### Правила прерывания QA

Когда пользователь вводит **QA** в любой момент:

1. **Команда QA ДОЛЖНА иметь немедленный приоритет** над любой текущей операцией, включая "ПРИНУДИТЕЛЬНЫЙ ПЕРЕХОД РЕЖИМА", вызванный оценкой сложности.
2. Система ДОЛЖНА:
   - Немедленно загрузить карту режима QA
   - Выполнить полный процесс валидации QA
   - Устранить любые сбои перед продолжением
3. **Требуемые шаги по исправлению имеют приоритет** над любыми ожидающими переключениями режимов или правилами сложности
4. После завершения и успешного прохождения валидации QA:
   - Возобновить ранее определенный процесс
   - Продолжить с любыми требуемыми переключениями режимов

```
⚠️ АКТИВИРОВАН ПЕРЕХОД QA
Все другие процессы приостановлены
Запущены проверки валидации QA...
Любые найденные проблемы ДОЛЖНЫ быть устранены перед продолжением нормального процесса
```

## 📋 ШАБЛОН ПРОВЕРКИ КОНТРОЛЬНОЙ ТОЧКИ

Каждая основная контрольная точка в режиме VAN использует этот формат:

```
✓ КОНТРОЛЬНАЯ ТОЧКА СЕКЦИИ: [ИМЯ СЕКЦИИ]
- Требование 1? [ДА/НЕТ]
- Требование 2? [ДА/НЕТ]
- Требование 3? [ДА/НЕТ]

→ Если все ДА: Готово к следующей секции
→ Если есть НЕТ: Исправить отсутствующие элементы перед продолжением
```

## 🚀 АКТИВАЦИЯ РЕЖИМА VAN

Когда пользователь вводит "VAN", ответить подтверждением и начать процесс:

```
Пользователь: VAN

Ответ: ОК VAN - Начало процесса инициализации
```

После завершения режима CREATIVE, когда пользователь вводит "VAN QA", ответить:

```
Пользователь: VAN QA

Ответ: ОК VAN QA - Начало технической валидации
```

Это обеспечивает четкую коммуникацию о том, какая фаза режима VAN активна.

## 🔍 ПОДРОБНЫЙ ПРОЦЕСС ВАЛИДАЦИИ QA

### 1️⃣ ПРОВЕРКА ЗАВИСИМОСТЕЙ

Этот шаг проверяет, что все необходимые пакеты установлены и совместимы:

```mermaid
graph TD
    Start["Проверка зависимостей"] --> ReadDeps["Прочитать требуемые зависимости<br>из фазы CREATIVE"]
    ReadDeps --> CheckInstalled["Проверить, установлены ли<br>зависимости"]
    CheckInstalled --> DepStatus{"Все зависимости<br>установлены?"}
    
    DepStatus -->|"Да"| VerifyVersions["Проверить версии<br>и совместимость"]
    DepStatus -->|"Нет"| InstallMissing["Установить отсутствующие<br>зависимости"]
    InstallMissing --> VerifyVersions
    
    VerifyVersions --> VersionStatus{"Версии<br>совместимы?"}
    VersionStatus -->|"Да"| DepSuccess["Зависимости проверены<br>✅ ПРОЙДЕНО"]
    VersionStatus -->|"Нет"| UpgradeVersions["Обновить/понизить<br>при необходимости"]
    UpgradeVersions --> RetryVerify["Повторная проверка"]
    RetryVerify --> VersionStatus
    
    style Start fill:#4da6ff,stroke:#0066cc,color:white
    style DepSuccess fill:#10b981,stroke:#059669,color:white
    style DepStatus fill:#f6546a,stroke:#c30052,color:white
    style VersionStatus fill:#f6546a,stroke:#c30052,color:white
```

#### Реализация для Windows (PowerShell):
```powershell
# Пример: Проверка зависимостей Node.js для проекта React
function Verify-Dependencies {
    $requiredDeps = @{
        "node" = ">=14.0.0"
        "npm" = ">=6.0.0"
    }
    
    $missingDeps = @()
    $incompatibleDeps = @()
    
    # Проверка версии Node.js
    $nodeVersion = $null
    try {
        $nodeVersion = node -v
        if ($nodeVersion -match "v(\d+)\.(\d+)\.(\d+)") {
            $major = [int]$Matches[1]
            if ($major -lt 14) {
                $incompatibleDeps += "node (найдено $nodeVersion, требуется >=14.0.0)"
            }
        }
    } catch {
        $missingDeps += "node"
    }
    
    # Проверка версии npm
    $npmVersion = $null
    try {
        $npmVersion = npm -v
        if ($npmVersion -match "(\d+)\.(\d+)\.(\d+)") {
            $major = [int]$Matches[1]
            if ($major -lt 6) {
                $incompatibleDeps += "npm (найдено $npmVersion, требуется >=6.0.0)"
            }
        }
    } catch {
        $missingDeps += "npm"
    }
    
    # Отображение результатов
    if ($missingDeps.Count -eq 0 -and $incompatibleDeps.Count -eq 0) {
        Write-Output "✅ Все зависимости проверены и совместимы"
        return $true
    } else {
        if ($missingDeps.Count -gt 0) {
            Write-Output "❌ Отсутствуют зависимости: $($missingDeps -join ', ')"
        }
        if ($incompatibleDeps.Count -gt 0) {
            Write-Output "❌ Несовместимые версии: $($incompatibleDeps -join ', ')"
        }
        return $false
    }
}
```

#### Реализация для Mac/Linux (Bash):
```bash
#!/bin/bash

# Пример: Проверка зависимостей Node.js для проекта React
verify_dependencies() {
    local missing_deps=()
    local incompatible_deps=()
    
    # Проверка версии Node.js
    if command -v node &> /dev/null; then
        local node_version=$(node -v)
        if [[ $node_version =~ v([0-9]+)\.([0-9]+)\.([0-9]+) ]]; then
            local major=${BASH_REMATCH[1]}
            if (( major < 14 )); then
                incompatible_deps+=("node (найдено $node_version, требуется >=14.0.0)")
            fi
        fi
    else
        missing_deps+=("node")
    fi
    
    # Проверка версии npm
    if command -v npm &> /dev/null; then
        local npm_version=$(npm -v)
        if [[ $npm_version =~ ([0-9]+)\.([0-9]+)\.([0-9]+) ]]; then
            local major=${BASH_REMATCH[1]}
            if (( major < 6 )); then
                incompatible_deps+=("npm (найдено $npm_version, требуется >=6.0.0)")
            fi
        fi
    else
        missing_deps+=("npm")
    fi
    
    # Отображение результатов
    if [ ${#missing_deps[@]} -eq 0 ] && [ ${#incompatible_deps[@]} -eq 0 ]; then
        echo "✅ Все зависимости проверены и совместимы"
        return 0
    else
        if [ ${#missing_deps[@]} -gt 0 ]; then
            echo "❌ Отсутствуют зависимости: ${missing_deps[*]}"
        fi
        if [ ${#incompatible_deps[@]} -gt 0 ]; then
            echo "❌ Несовместимые версии: ${incompatible_deps[*]}"
        fi
        return 1
    fi
}
```

### 2️⃣ ВАЛИДАЦИЯ КОНФИГУРАЦИИ

Этот шаг проверяет формат и совместимость конфигурационных файлов:

```mermaid
graph TD
    Start["Валидация конфигурации"] --> IdentifyConfigs["Идентифицировать файлы<br>конфигурации"]
    IdentifyConfigs --> ReadConfigs["Прочитать файлы<br>конфигурации"]
    ReadConfigs --> ValidateSyntax["Проверить синтаксис<br>и формат"]
    ValidateSyntax --> SyntaxStatus{"Синтаксис<br>валиден?"}
    
    SyntaxStatus -->|"Да"| CheckCompatibility["Проверить совместимость<br>с платформой"]
    SyntaxStatus -->|"Нет"| FixSyntax["Исправить синтаксические<br>ошибки"]
    FixSyntax --> RetryValidate["Повторная валидация"]
    RetryValidate --> SyntaxStatus
    
    CheckCompatibility --> CompatStatus{"Совместимо с<br>платформой?"}
    CompatStatus -->|"Да"| ConfigSuccess["Конфигурации валидированы<br>✅ ПРОЙДЕНО"]
    CompatStatus -->|"Нет"| AdaptConfigs["Адаптировать конфигурации<br>для платформы"]
    AdaptConfigs --> RetryCompat["Повторная проверка<br>совместимости"]
    RetryCompat --> CompatStatus
    
    style Start fill:#4da6ff,stroke:#0066cc,color:white
    style ConfigSuccess fill:#10b981,stroke:#059669,color:white
    style SyntaxStatus fill:#f6546a,stroke:#c30052,color:white
    style CompatStatus fill:#f6546a,stroke:#c30052,color:white
``` 

#### Реализация валидации конфигурации:
```powershell
# Пример: Проверка конфигурационных файлов для веб-проекта
function Validate-Configurations {
    $configFiles = @(
        "package.json",
        "tsconfig.json",
        "vite.config.js"
    )
    
    $invalidConfigs = @()
    $incompatibleConfigs = @()
    
    foreach ($configFile in $configFiles) {
        if (Test-Path $configFile) {
            # Проверка синтаксиса JSON для файлов JSON
            if ($configFile -match "\.json$") {
                try {
                    Get-Content $configFile -Raw | ConvertFrom-Json | Out-Null
                } catch {
                    $invalidConfigs += "$configFile (ошибка синтаксиса JSON: $($_.Exception.Message))"
                    continue
                }
            }
            
            # Проверка совместимости конфигурации
            if ($configFile -eq "vite.config.js") {
                $content = Get-Content $configFile -Raw
                # Проверка плагина React в конфигурации Vite
                if ($content -notmatch "react\(\)") {
                    $incompatibleConfigs += "$configFile (отсутствует плагин React для проекта React)"
                }
            }
        } else {
            $invalidConfigs += "$configFile (файл не найден)"
        }
    }
    
    # Отображение результатов
    if ($invalidConfigs.Count -eq 0 -and $incompatibleConfigs.Count -eq 0) {
        Write-Output "✅ Все конфигурации валидированы и совместимы"
        return $true
    } else {
        if ($invalidConfigs.Count -gt 0) {
            Write-Output "❌ Недействительные конфигурации: $($invalidConfigs -join ', ')"
        }
        if ($incompatibleConfigs.Count -gt 0) {
            Write-Output "❌ Несовместимые конфигурации: $($incompatibleConfigs -join ', ')"
        }
        return $false
    }
}
```

### 3️⃣ ВАЛИДАЦИЯ СРЕДЫ

Этот шаг проверяет, правильно ли настроена среда для реализации:

```mermaid
graph TD
    Start["Валидация среды"] --> CheckEnv["Проверить среду<br>сборки"]
    CheckEnv --> VerifyBuildTools["Проверить инструменты<br>сборки"]
    VerifyBuildTools --> ToolsStatus{"Инструменты<br>доступны?"}
    
    ToolsStatus -->|"Да"| CheckPerms["Проверить права<br>и доступ"]
    ToolsStatus -->|"Нет"| InstallTools["Установить требуемые<br>инструменты сборки"]
    InstallTools --> RetryTools["Повторная проверка"]
    RetryTools --> ToolsStatus
    
    CheckPerms --> PermsStatus{"Права<br>достаточны?"}
    PermsStatus -->|"Да"| EnvSuccess["Среда валидирована<br>✅ ПРОЙДЕНО"]
    PermsStatus -->|"Нет"| FixPerms["Исправить проблемы<br>с правами"]
    FixPerms --> RetryPerms["Повторная проверка<br>прав"]
    RetryPerms --> PermsStatus
    
    style Start fill:#4da6ff,stroke:#0066cc,color:white
    style EnvSuccess fill:#10b981,stroke:#059669,color:white
    style ToolsStatus fill:#f6546a,stroke:#c30052,color:white
    style PermsStatus fill:#f6546a,stroke:#c30052,color:white
```

#### Реализация валидации среды:
```powershell
# Пример: Проверка среды для веб-проекта
function Validate-Environment {
    $requiredTools = @(
        @{Name = "git"; Command = "git --version"},
        @{Name = "node"; Command = "node --version"},
        @{Name = "npm"; Command = "npm --version"}
    )
    
    $missingTools = @()
    $permissionIssues = @()
    
    # Проверка инструментов сборки
    foreach ($tool in $requiredTools) {
        try {
            Invoke-Expression $tool.Command | Out-Null
        } catch {
            $missingTools += $tool.Name
        }
    }
    
    # Проверка прав на запись в директории проекта
    try {
        $testFile = ".__permission_test"
        New-Item -Path $testFile -ItemType File -Force | Out-Null
        Remove-Item -Path $testFile -Force
    } catch {
        $permissionIssues += "Текущая директория (отказано в праве записи)"
    }
    
    # Проверка доступности порта 3000 (обычно используется для серверов разработки)
    try {
        $listener = New-Object System.Net.Sockets.TcpListener([System.Net.IPAddress]::Loopback, 3000)
        $listener.Start()
        $listener.Stop()
    } catch {
        $permissionIssues += "Порт 3000 (уже используется или доступ запрещен)"
    }
    
    # Отображение результатов
    if ($missingTools.Count -eq 0 -and $permissionIssues.Count -eq 0) {
        Write-Output "✅ Среда успешно валидирована"
        return $true
    } else {
        if ($missingTools.Count -gt 0) {
            Write-Output "❌ Отсутствуют инструменты: $($missingTools -join ', ')"
        }
        if ($permissionIssues.Count -gt 0) {
            Write-Output "❌ Проблемы с правами: $($permissionIssues -join ', ')"
        }
        return $false
    }
}
```

### 4️⃣ МИНИМАЛЬНЫЙ ТЕСТ СБОРКИ

Этот шаг выполняет минимальный тест сборки для обеспечения основной функциональности:

```mermaid
graph TD
    Start["Минимальный тест сборки"] --> CreateTest["Создать минимальный<br>тестовый проект"]
    CreateTest --> BuildTest["Попытка<br>сборки"]
    BuildTest --> BuildStatus{"Сборка<br>успешна?"}
    
    BuildStatus -->|"Да"| RunTest["Запустить базовый<br>тест функциональности"]
    BuildStatus -->|"Нет"| FixBuild["Исправить проблемы<br>сборки"]
    FixBuild --> RetryBuild["Повторная сборка"]
    RetryBuild --> BuildStatus
    
    RunTest --> TestStatus{"Тест<br>пройден?"}
    TestStatus -->|"Да"| TestSuccess["Минимальный тест сборки<br>✅ ПРОЙДЕНО"]
    TestStatus -->|"Нет"| FixTest["Исправить проблемы<br>теста"]
    FixTest --> RetryTest["Повторный тест"]
    RetryTest --> TestStatus
    
    style Start fill:#4da6ff,stroke:#0066cc,color:white
    style TestSuccess fill:#10b981,stroke:#059669,color:white
    style BuildStatus fill:#f6546a,stroke:#c30052,color:white
    style TestStatus fill:#f6546a,stroke:#c30052,color:white
```

#### Реализация минимального теста сборки:
```powershell
# Пример: Выполнение минимального теста сборки для проекта React
function Perform-MinimalBuildTest {
    $buildSuccess = $false
    $testSuccess = $false
    
    # Создание минимального тестового проекта
    $testDir = ".__build_test"
    if (Test-Path $testDir) {
        Remove-Item -Path $testDir -Recurse -Force
    }
    
    try {
        # Создание минимальной тестовой директории
        New-Item -Path $testDir -ItemType Directory | Out-Null
        Push-Location $testDir
        
        # Инициализация минимального package.json
        @"
{
  "name": "build-test",
  "version": "1.0.0",
  "description": "Минимальный тест сборки",
  "main": "index.js",
  "scripts": {
    "build": "echo Тест сборки успешен"
  }
}
"@ | Set-Content -Path "package.json"
        
        # Попытка сборки
        npm run build | Out-Null
        $buildSuccess = $true
        
        # Создание минимального тестового файла
        @"
console.log('Тест успешен');
"@ | Set-Content -Path "index.js"
        
        # Запуск базового теста
        node index.js | Out-Null
        $testSuccess = $true
        
    } catch {
        Write-Output "❌ Тест сборки провален: $($_.Exception.Message)"
    } finally {
        Pop-Location
        if (Test-Path $testDir) {
            Remove-Item -Path $testDir -Recurse -Force
        }
    }
    
    # Отображение результатов
    if ($buildSuccess -and $testSuccess) {
        Write-Output "✅ Минимальный тест сборки успешно пройден"
        return $true
    } else {
        if (-not $buildSuccess) {
            Write-Output "❌ Процесс сборки провален"
        }
        if (-not $testSuccess) {
            Write-Output "❌ Базовый тест функциональности провален"
        }
        return $false
    }
}
```

## 📋 КОМПЛЕКСНЫЙ ФОРМАТ ОТЧЕТА QA

После выполнения всех шагов валидации генерируется комплексный отчет:

```
╔═════════════════════ 🔍 ОТЧЕТ ВАЛИДАЦИИ QA ══════════════════════╗
│                                                                     │
│  ПРОЕКТ: [Название проекта]                                         │
│  МЕТКА ВРЕМЕНИ: [Текущая дата/время]                               │
│                                                                     │
│  1️⃣ ПРОВЕРКА ЗАВИСИМОСТЕЙ                                         │
│  ✓ Требуемые: [Список требуемых зависимостей]                      │
│  ✓ Установленные: [Список установленных зависимостей]              │
│  ✓ Совместимые: [Да/Нет]                                          │
│                                                                     │
│  2️⃣ ВАЛИДАЦИЯ КОНФИГУРАЦИИ                                        │
│  ✓ Файлы конфигурации: [Список файлов конфигурации]                │
│  ✓ Синтаксис валиден: [Да/Нет]                                    │
│  ✓ Совместимо с платформой: [Да/Нет]                              │
│                                                                     │
│  3️⃣ ВАЛИДАЦИЯ СРЕДЫ                                               │
│  ✓ Инструменты сборки: [Доступны/Отсутствуют]                      │
│  ✓ Права: [Достаточны/Недостаточны]                               │
│  ✓ Среда готова: [Да/Нет]                                         │
│                                                                     │
│  4️⃣ МИНИМАЛЬНЫЙ ТЕСТ СБОРКИ                                       │
│  ✓ Процесс сборки: [Успешно/Провалено]                            │
│  ✓ Тест функциональности: [Пройден/Провален]                      │
│  ✓ Готовность к сборке: [Да/Нет]                                  │
│                                                                     │
│  🚨 ИТОГОВЫЙ ВЕРДИКТ: [ПРОЙДЕНО/ПРОВАЛЕНО]                         │
│  ➡️ [Сообщение об успехе или детали ошибки]                        │
╚═════════════════════════════════════════════════════════════════════╝
```

## ❌ ФОРМАТ ОТЧЕТА О ПРОВАЛЕ

Если какой-либо шаг валидации провален, генерируется подробный отчет о провале:

```
⚠️⚠️⚠️ ВАЛИДАЦИЯ QA ПРОВАЛЕНА ⚠️⚠️⚠️

Следующие проблемы должны быть устранены перед переходом в режим BUILD:

1️⃣ ПРОБЛЕМЫ С ЗАВИСИМОСТЯМИ:
- [Подробное описание проблем с зависимостями]
- [Рекомендуемое исправление]

2️⃣ ПРОБЛЕМЫ С КОНФИГУРАЦИЕЙ:
- [Подробное описание проблем с конфигурацией]
- [Рекомендуемое исправление]

3️⃣ ПРОБЛЕМЫ СРЕДЫ:
- [Подробное описание проблем среды]
- [Рекомендуемое исправление]

4️⃣ ПРОБЛЕМЫ ТЕСТА СБОРКИ:
- [Подробное описание проблем теста сборки]
- [Рекомендуемое исправление]

⚠️ РЕЖИМ BUILD ЗАБЛОКИРОВАН, пока эти проблемы не будут устранены.
Введите 'VAN QA' после устранения проблем для повторной валидации.
```

## 🔄 ИНТЕГРАЦИЯ С ДИЗАЙНЕРСКИМИ РЕШЕНИЯМИ

Режим VAN QA считывает и валидирует дизайнерские решения из фазы CREATIVE:

```mermaid
graph TD
    Start["Прочитать дизайнерские решения"] --> ReadCreative["Разобрать документацию<br>фазы CREATIVE"]
    ReadCreative --> ExtractTech["Извлечь технологические<br>выборы"]
    ExtractTech --> ExtractDeps["Извлечь требуемые<br>зависимости"]
    ExtractDeps --> BuildValidationPlan["Создать план<br>валидации"]
    BuildValidationPlan --> StartValidation["Начать процесс<br>четырехточечной валидации"]
    
    style Start fill:#4da6ff,stroke:#0066cc,color:white
    style ExtractTech fill:#f6546a,stroke:#c30052,color:white
    style BuildValidationPlan fill:#10b981,stroke:#059669,color:white
    style StartValidation fill:#f6546a,stroke:#c30052,color:white
```

### Процесс извлечения технологий:
```powershell
# Пример: Извлечение технологических выборов из документации фазы CREATIVE
function Extract-TechnologyChoices {
    $techChoices = @{}
    
    # Чтение из systemPatterns.md
    if (Test-Path "memory-bank\systemPatterns.md") {
        $content = Get-Content "memory-bank\systemPatterns.md" -Raw
        
        # Извлечение выбора фреймворка
        if ($content -match "Framework:\s*(\w+)") {
            $techChoices["framework"] = $Matches[1]
        }
        
        # Извлечение выбора библиотеки UI
        if ($content -match "UI Library:\s*(\w+)") {
            $techChoices["ui_library"] = $Matches[1]
        }
        
        # Извлечение выбора управления состоянием
        if ($content -match "State Management:\s*([^\\n]+)") {
            $techChoices["state_management"] = $Matches[1].Trim()
        }
    }
    
    return $techChoices
}
```

## 🚨 МЕХАНИЗМ ПРЕДОТВРАЩЕНИЯ РЕАЛИЗАЦИИ

Если валидация QA провалена, система предотвращает переход в режим BUILD:

```powershell
# Пример: Проверка статуса валидации QA перед разрешением режима BUILD
function Check-QAValidationStatus {
    $qaStatusFile = "memory-bank\.qa_validation_status"
    
    if (Test-Path $qaStatusFile) {
        $status = Get-Content $qaStatusFile -Raw
        if ($status -match "PASS") {
            return $true
        }
    }
    
    # Отображение сообщения о блокировке
    Write-Output "`n`n"
    Write-Output "🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫"
    Write-Output "⛔️ РЕЖИМ BUILD ЗАБЛОКИРОВАН: ТРЕБУЕТСЯ ВАЛИДАЦИЯ QA"
    Write-Output "⛔️ Вы должны завершить валидацию QA перед переходом в режим BUILD"
    Write-Output "`n"
    Write-Output "Введите 'VAN QA' для выполнения технической валидации"
    Write-Output "`n"
    Write-Output "🚫 РЕАЛИЗАЦИЯ НЕВОЗМОЖНА БЕЗ ВАЛИДАЦИИ 🚫"
    Write-Output "🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫"
    
    return $false
}
```

## 🧪 ОБЫЧНЫЕ ИСПРАВЛЕНИЯ ДЛЯ ВАЛИДАЦИИ QA

Вот распространенные исправления для проблем, обнаруженных во время валидации QA:

### Проблемы с зависимостями:
- **Отсутствует Node.js**: Установите Node.js с https://nodejs.org/
- **Устаревший npm**: Выполните `npm install -g npm@latest` для обновления
- **Отсутствующие пакеты**: Выполните `npm install` или `npm install [package-name]`

### Проблемы с конфигурацией:
- **Недействительный JSON**: Используйте валидатор JSON для проверки синтаксиса
- **Отсутствует плагин React**: Добавьте `import react from '@vitejs/plugin-react'` и `plugins: [react()]` в vite.config.js
- **Несовместимая конфигурация TypeScript**: Обновите `tsconfig.json` с правильными настройками React

### Проблемы со средой:
- **Отказано в доступе**: Запустите терминал от имени администратора (Windows) или используйте sudo (Mac/Linux)
- **Порт уже используется**: Завершите процесс, использующий порт, или измените порт в конфигурации
- **Отсутствуют инструменты сборки**: Установите необходимые инструменты командной строки

### Проблемы с тестом сборки:
- **Сборка провалена**: Проверьте консоль на наличие конкретных сообщений об ошибках
- **Тест провален**: Убедитесь, что минимальная конфигурация верна
- **Проблемы с путями**: Убедитесь, что пути используют правильные разделители для платформы

## 🔒 ФИНАЛЬНАЯ КОНТРОЛЬНАЯ ТОЧКА ВАЛИДАЦИИ QA

```
✓ КОНТРОЛЬНАЯ ТОЧКА СЕКЦИИ: ВАЛИДАЦИЯ QA
- Проверка зависимостей пройдена? [ДА/НЕТ]
- Валидация конфигурации пройдена? [ДА/НЕТ]
- Валидация среды пройдена? [ДА/НЕТ]
- Минимальный тест сборки пройден? [ДА/НЕТ]

→ Если все ДА: Готово к режиму BUILD
→ Если есть НЕТ: Исправить выявленные проблемы перед продолжением
```