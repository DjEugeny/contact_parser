---
description: Утилита для запоминания, как вызывать правила VAN QA
globs: van-qa-utils/rule-calling-help.mdc
alwaysApply: false
---
# VAN QA: КАК ВЫЗЫВАТЬ ПРАВИЛА

> **TL;DR:** Этот файл содержит примеры и напоминания о том, как правильно вызывать правила VAN QA с помощью инструмента fetch_rules.

## 🚨 СИНТАКСИС ВЫЗОВА ПРАВИЛ

Всегда используйте инструмент `fetch_rules` с правильным синтаксисом:

```
<function_calls>
<invoke name="fetch_rules">
<parameter name="rule_names">["isolation_rules/visual-maps/rule-name"]
</invoke>
</function_calls> 