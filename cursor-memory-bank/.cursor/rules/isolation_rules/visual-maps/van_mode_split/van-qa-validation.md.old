# VAN MODE: QA ТЕХНИЧЕСКАЯ ВАЛИДАЦИЯ (Перед СБОРКОЙ)

> **Кратко:** Этот документ описывает процесс технической валидации, выполняемый *после* режима CREATIVE и *перед* режимом BUILD, запускаемый командой `VAN QA`. Он обеспечивает проверку зависимостей, конфигурации, окружения и базовой функциональности сборки.

## 🚀 АКТИВАЦИЯ РЕЖИМА VAN QA

После завершения режима CREATIVE, когда пользователь вводит "VAN QA", ответить:

```
Пользователь: VAN QA

Ответ: OK VAN QA - Начало технической валидации
Загрузка карты валидации QA...
```

## 🔄 ПРИОРИТЕТ КОМАНДЫ QA (Переопределение QA)

Валидация QA может быть вызвана в любой момент (команда `QA`) и имеет немедленный приоритет:

```mermaid
graph TD
    UserQA["Пользователь вводит: QA"] --> HighPriority["⚠️ КОМАНДА ВЫСОКОГО ПРИОРИТЕТА"]
    HighPriority --> CurrentTask["Приостановить текущую задачу/процесс"]
    CurrentTask --> LoadQA["Загрузить карту валидации QA (Этот файл)"]
    LoadQA --> RunQA["Выполнить процесс валидации QA"]
    RunQA --> QAResults{"Результаты QA"}
    
    QAResults -->|"PASS"| ResumeFlow["Возобновить предыдущий процесс"]
    QAResults -->|"FAIL"| FixIssues["Исправить выявленные проблемы"]
    FixIssues --> ReRunQA["Повторить валидацию QA"]
    ReRunQA --> QAResults
    
    style UserQA fill:#f8d486,stroke:#e8b84d,color:black
    style HighPriority fill:#ff0000,stroke:#cc0000,color:white,stroke-width:3px
    style LoadQA fill:#4da6ff,stroke:#0066cc,color:white
    style RunQA fill:#4da6ff,stroke:#0066cc,color:white
    style QAResults fill:#f6546a,stroke:#c30052,color:white
```

### Правила прерывания QA

1. **Немедленный приоритет:** Команда `QA` прерывает все процессы.
2. **Загрузка и выполнение:** Загрузить эту карту (`van-qa-validation.mdc`) и выполнить полный процесс.
3. **Приоритет исправлений:** Исправления имеют приоритет над ожидающими переключениями режимов.
4. **Возобновление:** При успешном прохождении (PASS) возобновить предыдущий процесс.

```
⚠️ ПЕРЕОПРЕДЕЛЕНИЕ QA АКТИВИРОВАНО
Все другие процессы приостановлены
Проверки валидации QA выполняются...
Все обнаруженные проблемы ДОЛЖНЫ быть устранены перед продолжением нормального процесса
```

## 🔍 ОБЗОР ТЕХНИЧЕСКОЙ ВАЛИДАЦИИ

Четырехэтапный процесс валидации:

```mermaid
graph TD
    VANQA["РЕЖИМ VAN QA"] --> FourChecks["ЧЕТЫРЕХЭТАПНАЯ ВАЛИДАЦИЯ"]
    
    FourChecks --> DepCheck["1️⃣ ПРОВЕРКА ЗАВИСИМОСТЕЙ"]
    DepCheck --> ConfigCheck["2️⃣ ВАЛИДАЦИЯ КОНФИГУРАЦИИ"]
    ConfigCheck --> EnvCheck["3️⃣ ВАЛИДАЦИЯ ОКРУЖЕНИЯ"]
    EnvCheck --> MinBuildCheck["4️⃣ ТЕСТ МИНИМАЛЬНОЙ СБОРКИ"]
    
    MinBuildCheck --> ValidationResults{"Все проверки<br>пройдены?"}
    ValidationResults -->|"Да"| SuccessReport["СФОРМИРОВАТЬ ОТЧЕТ ОБ УСПЕХЕ"]
    ValidationResults -->|"Нет"| FailureReport["СФОРМИРОВАТЬ ОТЧЕТ О НЕУДАЧЕ"]
    
    SuccessReport --> BUILD_Transition["Запустить режим BUILD"]
    FailureReport --> FixIssues["Исправить технические проблемы"]
    FixIssues --> ReValidate["Повторить валидацию (Перезапустить VAN QA)"]
    ReValidate --> FourChecks
    
    style VANQA fill:#4da6ff,stroke:#0066cc,color:white
    style FourChecks fill:#f6546a,stroke:#c30052,color:white
    style ValidationResults fill:#f6546a,stroke:#c30052,color:white
    style BUILD_Transition fill:#10b981,stroke:#059669,color:white
    style FixIssues fill:#ff5555,stroke:#dd3333,color:white
```

## 🔄 ИНТЕГРАЦИЯ С ДИЗАЙН-РЕШЕНИЯМИ

Читает выходные данные фазы CREATIVE (например, `memory-bank/systemPatterns.md`) для информирования валидации:

```mermaid
graph TD
    Start["Чтение дизайн-решений"] --> ReadCreative["Разбор документации<br>фазы CREATIVE"]
    ReadCreative --> ExtractTech["Извлечение технологических<br>решений"]
    ExtractTech --> ExtractDeps["Извлечение необходимых<br>зависимостей"]
    ExtractDeps --> BuildValidationPlan["Создание плана<br>валидации"]
    BuildValidationPlan --> StartValidation["Начало четырехэтапного<br>процесса валидации"]
    
    style Start fill:#4da6ff,stroke:#0066cc,color:white
    style ExtractTech fill:#f6546a,stroke:#c30052,color:white
    style BuildValidationPlan fill:#10b981,stroke:#059669,color:white
    style StartValidation fill:#f6546a,stroke:#c30052,color:white
```

### Пример извлечения технологий (PowerShell):
```powershell
# Пример: Извлечение технологических решений из документации фазы CREATIVE
function Extract-TechnologyChoices {
    $techChoices = @{}
    # Чтение из systemPatterns.md
    if (Test-Path "memory-bank\systemPatterns.md") {
        $content = Get-Content "memory-bank\systemPatterns.md" -Raw
        if ($content -match "Framework:\s*(\w+)") { $techChoices["framework"] = $Matches[1] }
        if ($content -match "UI Library:\s*(\w+)") { $techChoices["ui_library"] = $Matches[1] }
        if ($content -match "State Management:\s*([^\n]+)") { $techChoices["state_management"] = $Matches[1].Trim() }
    }
    return $techChoices
}
```

## 🔍 ПОДРОБНЫЕ ШАГИ ВАЛИДАЦИИ QA И СКРИПТЫ

### 1️⃣ ПРОВЕРКА ЗАВИСИМОСТЕЙ

```mermaid
# Диаграмма Mermaid для проверки зависимостей (как в оригинальном файле)
graph TD
    Start["Проверка зависимостей"] --> ReadDeps["Чтение необходимых зависимостей<br>из фазы CREATIVE"]
    ReadDeps --> CheckInstalled["Проверка установки<br>зависимостей"]
    CheckInstalled --> DepStatus{"Все зависимости<br>установлены?"}
    DepStatus -->|"Да"| VerifyVersions["Проверка версий<br>и совместимости"]
    DepStatus -->|"Нет"| InstallMissing["Установка отсутствующих<br>зависимостей"]
    InstallMissing --> VerifyVersions
    VerifyVersions --> VersionStatus{"Версии<br>совместимы?"}
    VersionStatus -->|"Да"| DepSuccess["Зависимости проверены<br>✅ УСПЕХ"]
    VersionStatus -->|"Нет"| UpgradeVersions["Обновление/понижение<br>версий при необходимости"]
    UpgradeVersions --> RetryVerify["Повторная проверка"]
    RetryVerify --> VersionStatus
    style Start fill:#4da6ff, DepSuccess fill:#10b981, DepStatus fill:#f6546a, VersionStatus fill:#f6546a
```

#### Пример реализации (PowerShell):
```powershell
# Функция Verify-Dependencies (как в оригинальном файле)
function Verify-Dependencies {
    $requiredDeps = @{ "node" = ">=14.0.0"; "npm" = ">=6.0.0" }
    $missingDeps = @(); $incompatibleDeps = @()
    try { $nodeVersion = node -v; if ($nodeVersion -match "v(\d+).*") { if ([int]$Matches[1] -lt 14) { $incompatibleDeps += "node" } } } catch { $missingDeps += "node" }
    try { $npmVersion = npm -v; if ($npmVersion -match "(\d+).*") { if ([int]$Matches[1] -lt 6) { $incompatibleDeps += "npm" } } } catch { $missingDeps += "npm" }
    if ($missingDeps.Count -eq 0 -and $incompatibleDeps.Count -eq 0) { Write-Output "✅ Зависимости в порядке"; return $true } else { Write-Output "❌ Зависимости НЕ в порядке"; return $false }
}
```

#### Пример реализации (Bash):
```bash
# Функция verify_dependencies (как в оригинальном файле)
verify_dependencies() {
    local missing_deps=(); local incompatible_deps=()
    if command -v node &> /dev/null; then node_version=$(node -v); if [[ $node_version =~ v([0-9]+) ]]; then if (( ${BASH_REMATCH[1]} < 14 )); then incompatible_deps+=("node"); fi; fi; else missing_deps+=("node"); fi
    if command -v npm &> /dev/null; then npm_version=$(npm -v); if [[ $npm_version =~ ([0-9]+) ]]; then if (( ${BASH_REMATCH[1]} < 6 )); then incompatible_deps+=("npm"); fi; fi; else missing_deps+=("npm"); fi
    if [ ${#missing_deps[@]} -eq 0 ] && [ ${#incompatible_deps[@]} -eq 0 ]; then echo "✅ Зависимости в порядке"; return 0; else echo "❌ Зависимости НЕ в порядке"; return 1; fi
}
```

### 2️⃣ ВАЛИДАЦИЯ КОНФИГУРАЦИИ

```mermaid
# Диаграмма Mermaid для валидации конфигурации (как в оригинальном файле)
graph TD
    Start["Валидация конфигурации"] --> IdentifyConfigs["Определение файлов"]
    IdentifyConfigs --> ReadConfigs["Чтение файлов"]
    ReadConfigs --> ValidateSyntax["Проверка синтаксиса"]
    ValidateSyntax --> SyntaxStatus{"Валидно?"}
    SyntaxStatus -->|"Да"| CheckCompatibility["Проверка совместимости"]
    SyntaxStatus -->|"Нет"| FixSyntax["Исправление синтаксиса"]
    FixSyntax --> RetryValidate["Повтор"]
    RetryValidate --> SyntaxStatus
    CheckCompatibility --> CompatStatus{"Совместимо?"}
    CompatStatus -->|"Да"| ConfigSuccess["Конфигурации проверены ✅ УСПЕХ"]
    CompatStatus -->|"Нет"| AdaptConfigs["Адаптация конфигураций"]
    AdaptConfigs --> RetryCompat["Повторная проверка"]
    RetryCompat --> CompatStatus
    style Start fill:#4da6ff,ConfigSuccess fill:#10b981,SyntaxStatus fill:#f6546a,CompatStatus fill:#f6546a
```

#### Пример реализации (PowerShell):
```powershell
# Функция Validate-Configurations (как в оригинальном файле)
function Validate-Configurations {
    $configFiles = @("package.json", "tsconfig.json", "vite.config.js")
    $invalidConfigs = @(); $incompatibleConfigs = @()
    foreach ($configFile in $configFiles) {
        if (Test-Path $configFile) {
            if ($configFile -match "\.json$") { try { Get-Content $configFile -Raw | ConvertFrom-Json | Out-Null } catch { $invalidConfigs += "$configFile (JSON)"; continue } }
            if ($configFile -eq "vite.config.js") { $content = Get-Content $configFile -Raw; if ($content -notmatch "react\(\)") { $incompatibleConfigs += "$configFile (React)" } }
        } else { $invalidConfigs += "$configFile (отсутствует)" }
    }
    if ($invalidConfigs.Count -eq 0 -and $incompatibleConfigs.Count -eq 0) { Write-Output "✅ Конфигурации в порядке"; return $true } else { Write-Output "❌ Конфигурации НЕ в порядке"; return $false }
}
```

### 3️⃣ ВАЛИДАЦИЯ ОКРУЖЕНИЯ

```mermaid
# Диаграмма Mermaid для валидации окружения (как в оригинальном файле)
graph TD
    Start["Валидация окружения"] --> CheckEnv["Проверка окружения"]
    CheckEnv --> VerifyBuildTools["Проверка инструментов"]
    VerifyBuildTools --> ToolsStatus{"Доступны?"}
    ToolsStatus -->|"Да"| CheckPerms["Проверка разрешений"]
    ToolsStatus -->|"Нет"| InstallTools["Установка инструментов"]
    InstallTools --> RetryTools["Повтор"]
    RetryTools --> ToolsStatus
    CheckPerms --> PermsStatus{"Достаточно?"}
    PermsStatus -->|"Да"| EnvSuccess["Окружение проверено ✅ УСПЕХ"]
    PermsStatus -->|"Нет"| FixPerms["Исправление разрешений"]
    FixPerms --> RetryPerms["Повторная проверка"]
    RetryPerms --> PermsStatus
    style Start fill:#4da6ff,EnvSuccess fill:#10b981,ToolsStatus fill:#f6546a,PermsStatus fill:#f6546a
```

#### Пример реализации (PowerShell):
```powershell
# Функция Validate-Environment (как в оригинальном файле)
function Validate-Environment {
    $requiredTools = @(@{Name='git';Cmd='git --version'},@{Name='node';Cmd='node --version'},@{Name='npm';Cmd='npm --version'})
    $missingTools = @(); $permissionIssues = @()
    foreach ($tool in $requiredTools) { try { Invoke-Expression $tool.Cmd | Out-Null } catch { $missingTools += $tool.Name } }
    try { $testFile = ".__perm_test"; New-Item $testFile -ItemType File -Force | Out-Null; Remove-Item $testFile -Force } catch { $permissionIssues += "CWD Write" }
    try { $L = New-Object Net.Sockets.TcpListener([Net.IPAddress]::Loopback, 3000); $L.Start(); $L.Stop() } catch { $permissionIssues += "Port 3000" }
    if ($missingTools.Count -eq 0 -and $permissionIssues.Count -eq 0) { Write-Output "✅ Окружение в порядке"; return $true } else { Write-Output "❌ Окружение НЕ в порядке"; return $false }
}
```

### 4️⃣ ТЕСТ МИНИМАЛЬНОЙ СБОРКИ

```mermaid
# Диаграмма Mermaid для теста минимальной сборки (как в оригинальном файле)
graph TD
    Start["Тест минимальной сборки"] --> CreateTest["Создание тестового проекта"]
    CreateTest --> BuildTest["Попытка сборки"]
    BuildTest --> BuildStatus{"Успешно?"}
    BuildStatus -->|"Да"| RunTest["Запуск базового теста"]
    BuildStatus -->|"Нет"| FixBuild["Исправление проблем сборки"]
    FixBuild --> RetryBuild["Повторная сборка"]
    RetryBuild --> BuildStatus
    RunTest --> TestStatus{"Пройдено?"}
    TestStatus -->|"Да"| TestSuccess["Тест сборки ✅ УСПЕХ"]
    TestStatus -->|"Нет"| FixTest["Исправление проблем теста"]
    FixTest --> RetryTest["Повторный тест"]
    RetryTest --> TestStatus
    style Start fill:#4da6ff,TestSuccess fill:#10b981,BuildStatus fill:#f6546a,TestStatus fill:#f6546a
```

#### Пример реализации (PowerShell):
```powershell
# Функция Perform-MinimalBuildTest (как в оригинальном файле)
function Perform-MinimalBuildTest {
    $buildSuccess = $false; $testSuccess = $false; $testDir = ".__build_test"
    if (Test-Path $testDir) { Remove-Item $testDir -Recurse -Force }
    try {
        New-Item $testDir -ItemType Directory | Out-Null; Push-Location $testDir
        '{"name": "build-test","scripts": {"build": "echo Build test successful"}}' | Set-Content package.json
        npm run build | Out-Null; $buildSuccess = $true
        'console.log("Test successful");' | Set-Content index.js
        node index.js | Out-Null; $testSuccess = $true
    } catch { Write-Output "❌ Исключение теста сборки" } finally { Pop-Location; if (Test-Path $testDir) { Remove-Item $testDir -Recurse -Force } }
    if ($buildSuccess -and $testSuccess) { Write-Output "✅ Тест сборки в порядке"; return $true } else { Write-Output "❌ Тест сборки НЕ в порядке"; return $false }
}
```

## 📝 ФОРМАТЫ ОТЧЕТОВ ВАЛИДАЦИИ

### Полный отчет об успехе:
```
╔═════════════════════ 🔍 ОТЧЕТ ВАЛИДАЦИИ QA ══════════════════════╗
│ ПРОЕКТ: [Название проекта] | ВРЕМЯ: [Текущая дата/время]          │
├─────────────────────────────────────────────────────────────────────┤
│ 1️⃣ ЗАВИСИМОСТИ: ✓ Совместимы                                      │
│ 2️⃣ КОНФИГУРАЦИЯ: ✓ Валидна и совместима                          │
│ 3️⃣ ОКРУЖЕНИЕ: ✓ Готово                                           │
│ 4️⃣ МИНИМАЛЬНАЯ СБОРКА: ✓ Успешно и пройдено                     │
├─────────────────────────────────────────────────────────────────────┤
│ 🚨 ОКОНЧАТЕЛЬНЫЙ ВЫВОД: УСПЕХ                                     │
│ ➡️ Можно переходить к режиму BUILD                                │
╚═════════════════════════════════════════════════════════════════════╝
```

### Подробный отчет о неудаче:
```
⚠️⚠️⚠️ ВАЛИДАЦИЯ QA НЕ ПРОЙДЕНА ⚠️⚠️⚠️
Проблемы должны быть устранены перед переходом в режим BUILD:

1️⃣ ПРОБЛЕМЫ ЗАВИСИМОСТЕЙ: [Детали/Исправление]
2️⃣ ПРОБЛЕМЫ КОНФИГУРАЦИИ: [Детали/Исправление]
3️⃣ ПРОБЛЕМЫ ОКРУЖЕНИЯ: [Детали/Исправление]
4️⃣ ПРОБЛЕМЫ ТЕСТА СБОРКИ: [Детали/Исправление]

⚠️ РЕЖИМ BUILD ЗАБЛОКИРОВАН. Введите 'VAN QA' после исправления для повторной валидации.
```

## 🧪 ОБЩИЕ ИСПРАВЛЕНИЯ ВАЛИДАЦИИ QA

- **Зависимости:** Установить Node/npm, выполнить `npm install`, проверить версии.
- **Конфигурация:** Проверить JSON, проверить необходимые плагины (например, React для Vite), обеспечить совместимость TSConfig.
- **Окружение:** Проверить разрешения (Admin/sudo), убедиться, что порты свободны, установить отсутствующие CLI-инструменты (git и т.д.).
- **Тест сборки:** Проверить логи на наличие ошибок, проверить минимальную конфигурацию, проверить разделители путей.

## 🔒 МЕХАНИЗМ ПРЕДОТВРАЩЕНИЯ ПЕРЕХОДА В РЕЖИМ BUILD

Логика проверки статуса QA перед разрешением перехода в режим BUILD.

```mermaid
graph TD
    Start["Пользователь вводит: BUILD"] --> CheckQA{"Валидация QA<br>пройдена?"}
    CheckQA -->|"Да"| AllowBuild["Разрешить режим BUILD"]
    CheckQA -->|"Нет"| BlockBuild["БЛОКИРОВКА РЕЖИМА BUILD"]
    BlockBuild --> Message["Отобразить:<br>⚠️ ТРЕБУЕТСЯ ВАЛИДАЦИЯ QA"]
    Message --> ReturnToVANQA["Приглашение: Введите VAN QA"]
    
    style CheckQA fill:#f6546a, BlockBuild fill:#ff0000,stroke:#990000, Message fill:#ff5555, ReturnToVANQA fill:#4da6ff
```

### Пример реализации (PowerShell):
```powershell
# Пример: Проверка статуса QA перед разрешением BUILD
function Check-QAValidationStatus {
    $qaStatusFile = "memory-bank\.qa_validation_status" # Предполагается, что статус записан здесь
    if (Test-Path $qaStatusFile) {
        if ((Get-Content $qaStatusFile -Raw) -match "PASS") { return $true }
    }
    Write-Output "🚫 РЕЖИМ BUILD ЗАБЛОКИРОВАН: ТРЕБУЕТСЯ ВАЛИДАЦИЯ QA. Введите 'VAN QA'. 🚫"
    return $false
}
```

## 🚨 ТРИГГЕРЫ ПЕРЕХОДА МЕЖДУ РЕЖИМАМИ (Относительно QA)

### Переход из CREATIVE в VAN QA:
```
⏭️ СЛЕДУЮЩИЙ РЕЖИМ: VAN QA
Для проверки технических требований перед реализацией введите 'VAN QA'
```

### Переход из VAN QA в BUILD (при успехе):
```
✅ ТЕХНИЧЕСКАЯ ВАЛИДАЦИЯ ЗАВЕРШЕНА
Все предварительные условия успешно проверены
Теперь вы можете перейти в режим BUILD
Введите 'BUILD' для начала реализации
```

## 📋 ФИНАЛЬНАЯ КОНТРОЛЬНАЯ ТОЧКА ВАЛИДАЦИИ QA

```
✓ КОНТРОЛЬНАЯ ТОЧКА СЕКЦИИ: ВАЛИДАЦИЯ QA
- Проверка зависимостей пройдена? [ДА/НЕТ]
- Валидация конфигурации пройдена? [ДА/НЕТ]
- Валидация окружения пройдена? [ДА/НЕТ]
- Тест минимальной сборки пройден? [ДА/НЕТ]

→ Если все ДА: Готово к переходу в режим BUILD.
→ Если есть НЕТ: Исправить выявленные проблемы и перезапустить VAN QA.
```

**Следующий шаг (при успехе):** Запустить режим BUILD.
**Следующий шаг (при неудаче):** Устранить проблемы и перезапустить `VAN QA`.