# План нормализации PreCleaner

## 📌 Общий статус
- **Фокус:** устранение расхождений в очистке писем (подписи, цитаты, переносы строк)
- **Дата обновления:** 2025-10-13
- **Ответственный:** AI-ассистент (Codex)

## 🚦 Статусы задач

### ✅ Выполненные
- [x] **Диагностика формата писем без переводов строк**  
  └─ Проверить сырой MIME (plain/text vs HTML) для `email_016_20250401_20250401_himlabservice_ru_06d4360a.json`; подтвердить гипотезу о потерянных переносах до PreCleaner.
- [x] **Анализ влияния базового очистителя на подписи**  
  └─ Зафиксировать точки, где `EnhancedTextCleaner` удаляет блоки `С уважением` до запуска PreCleaner.

- [x] **Восстановление переносов строк в базовом очистителе**  
  └─ Добавить конвертацию `<br>/<p>/<div>` в `\n` и отдельную обработку plain/text до удаления тегов.
- [x] **Делегирование работы с подписями PreCleaner**  
  └─ Сделать удаление подписей в `EnhancedTextCleaner` условным (только если PreCleaner отключён).
- [x] **Настройка фолдинга цитат**  
  └─ Поднять `quote_preview_context_lines` ≥ 2, увеличить `quote_preview_max_chars`, добавить правило «не схлопывать первую цитату с контактами».
- [x] **Обработка multipart-писем**  
  └─ На этапе fetching предпочитать `text/plain` часть, а HTML использовать как fallback; для чистого HTML — нормализовать переносы до PreCleaner.
- [x] **Расширение русских маркеров подписей/дисклеймеров**  
  └─ Проанализировать отчёт от 12.10 и добавить в `sig_markers`/`disclaimer_markers` безопасные формулировки без введения «юридического» профиля.
- [x] **Регрессионные тесты**  
  └─ Добавить фикстуры для писем `email_016...` и `email_023...` и убедиться, что телефоны и подписи сохраняются.
- [x] **Проверка результатов на сэмпле**  
  └─ Прогнать обновлённый конвейер на выборке за 2025‑04‑01…02, сравнить `char_count`, placeholders и логи.
- [x] **Отчёт в .kiro/specs/precleaner/**  
  └─ Описать изменения и метрики после стабилизации, приложить ссылки на тесты и примерные выводы.

- [x] Актуализирована карта задач (2025-10-12)
- [x] Зафиксированы основные проблемы по письмам `HimLabService` и `Invitro`

### 📝 Следующие шаги
- [x] **Регрессия HimLabService**  
  └─ `scripts/run_precleaner_regression.py` читает `eml_path` из JSON, проверяет placeholders и длину ~11k; прогон 2025‑04‑01 завершился без ошибок.
- [x] **Сигнатуры с латинской «c»**  
  └─ `config/precleaner.yaml` дополнен `c уважением` и `"--\u00A0"`, проверено на письме HimLabService через config-aware очиститель.
- [x] **Контроль сохранения `body_clean`**  
  └─ Email_016 переобработан (`body_clean` 11 182 символов с переносами), PreCleaner активен, сигнатура Сунцовой сохранена единично.
- [x] **Синхронизация `.eml` ↔ JSON**  
  └─ `eml_path` и `raw_size` приведены к фактическому файлу `eml/2025-04-01/70131743503110_mail.yandex.ru_205758_421727.eml`, `processed_at` обновлён.
- [x] **Сохранить текст внутри `<...>`**  
  └─ `clean_html_aggressively` разворачивает псевдо-теги с контактами; проверено на письмах 2025-04-02.
- [x] **Вернуть числовые строки таблиц**  
  └─ `extract_meaningful_content` больше не фильтрует строки `1.`/`7.` — таблицы и списки контактов сохраняются.
- [x] **Не обрезать подписи при извлечении HTML**  
  └─ `EmailParser.extract_plain_text` очищает HTML до обрезки; подпись Фетисовой и хвосты длинных писем сохранены.
- [x] **Восстановить вложения в JSON**  
  └─ `EmailProcessor` подхватывает метаданные из реестра, если attachments пусты.
- [x] **Расширить регрессию 2025-04-02**  
  └─ Добавлены кейсы (SibLabService, Samruni, med3843, Mironenko, список гостей) + проверка вложений через реестр.
- [x] **Пересохранить срез 2025-04-01…03**  
  └─ Прогнать fetcher новым кодом, убедиться что JSON содержит обновлённые `body_clean` и `attachments`.

## ⚠️ Риски и наблюдения
- Потеря переносов может быть связана с plain/text источником, а не HTML-очисткой — требуется подтверждение по сырым письмам.
- Агрессивные placeholder’ы делают LLM-этап слепым к контактам; изменения нужно тестировать на реальных данных перед отключением дедупликации.
- Профилизация «юридических» писем не планируется: акцент на лабораторной переписке и адаптации по длине/наличию цитат без тематических классификаторов.

## 🔗 Полезные материалы
- `config/precleaner.yaml` — текущие настройки
- `src/fetcher/utils/enhanced_text_cleaner.py` — базовый очиститель
- `src/fetcher/utils/precleaner_core.py` — логика фолдинга цитат и подписей
- `scripts/test_precleaner_improvements.py`, `scripts/test_precleaner_simple.py` — стартовые точки для регрессионных тестов
- `run_new_fetcher.py` — стартовая точка для запуска нового Fetcher
