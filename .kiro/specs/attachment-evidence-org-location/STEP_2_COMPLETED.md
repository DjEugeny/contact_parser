# ✅ Шаг 2 завершен: Защита от мусора

## Что сделано

### 1. Обнаружена реальная проблема

**Анализ RAW ответа LLM показал:**
- LLM правильно вернул `address: null` для МИЛЛАБ и ФБУЗ ✅
- Адрес "117587" добавлялся **постобработкой** ❌

**Файл:** `data/llm_results/2025-07-29/raw/email_016_20250729_20250729_dna_technology_ru_6360137e_20251005_105436_105546_raw.json`

```json
{
  "name": "МИЛЛАБ",
  "city": "Москва",
  "address": null  ← LLM правильно!
}
```

### 2. Добавлена защита в постобработку

#### A. Файл: `src/postprocessing/attachment_evidence_extractor.py`

**Метод:** `_find_location_near_organization`

**Добавлено:**
```python
# 🛡️ ЗАЩИТА: Фильтр "только индекс"
# Если адрес состоит только из 6 цифр (индекс) - это не полный адрес
if address and re.match(r'^\d{6}$', address.strip()):
    self.logger.debug(f"Отклонен адрес-индекс: {address}")
    address = None

# 🛡️ ЗАЩИТА: Фильтр "слишком короткий адрес"
# Если адрес короче 20 символов и нет города - скорее всего это мусор
if address and len(address) < 20 and not city:
    self.logger.debug(f"Отклонен короткий адрес без города: {address}")
    address = None
```

#### B. Файл: `src/postprocessing/org_location_enrichment.py`

**Метод:** `_apply_enrichment`

**Добавлено:**
```python
# 🛡️ ЗАЩИТА: Фильтр "только индекс"
# Если адрес состоит только из 6 цифр (индекс) - это не полный адрес
import re
if re.match(r'^\d{6}$', address_value.strip()):
    self.logger.debug(f"Отклонен адрес-индекс при обогащении: {address_value}")
    address_value = None

# 🛡️ ЗАЩИТА: Фильтр "слишком короткий адрес"
# Если адрес короче 20 символов и нет города - скорее всего это мусор
if address_value and len(address_value) < 20 and not enriched_org.get('city'):
    self.logger.debug(f"Отклонен короткий адрес без города при обогащении: {address_value}")
    address_value = None

# Применяем только если адрес прошел фильтры
if address_value:
    enriched_org['address'] = address_value
    metadata.address = address_value
    metadata.applied = True
```

## Результаты тестирования

### Тест на письме 016

**Команда:**
```bash
python src/api_pipeline_validator.py --mode first10 \
  --start "email_016_20250729_20250729_dna-technology_ru_6360137e.json" \
  --count 1
```

**Результат файла:**
`data/llm_results/2025-07-29/email_016_20250729_20250729_dna_technology_ru_6360137e_20251005_105950_110115_processed.json`

### ✅ Фактический результат (ПРАВИЛЬНЫЙ!)

```
ДНК-Технология:
  city: Москва ✅
  address: 117587, г. Москва, вн. тер. г. муниципальный округ Чертаново Северное, Варшавское шоссе, д. 125Ж, корп. 5, этаж 1, пом. 12 ✅

МИЛЛАБ:
  city: Москва ✅
  address: None ✅ (было "117587")

ФБУЗ "Центр Гигиены и Эпидемиологии в Республике Хакасия":
  city: Абакан ✅
  address: None ✅ (было "117587")
```

## Что работает

### ✅ Фаза 1: Улучшение промпта
1. **Город извлекается правильно:**
   - МИЛЛАБ: city="Москва" (из "для Москва Компания МИЛЛАБ")
   - ФБУЗ: city="Абакан" (из "для Абакан ЦГиЭ")
   - LLM понимает инструкции про город

2. **Адрес НЕ копируется:**
   - LLM правильно возвращает address=null для МИЛЛАБ и ФБУЗ
   - Инструкции про "НЕ копируй адрес" работают!

### ✅ Фаза 2: Защита от мусора
1. **Фильтр "только индекс" работает:**
   - Адрес "117587" (только индекс) отклоняется
   - МИЛЛАБ и ФБУЗ получают address=None вместо "117587"

2. **Постобработка не добавляет мусор:**
   - Даже если постобработка находит "117587", он фильтруется
   - Защита работает на двух уровнях (extraction + enrichment)

## Ключевые выводы

### 1. Промпт работает!
- LLM правильно понимает инструкции
- Город извлекается из контекста ("для г. Москва, Компания МИЛЛАБ")
- Адрес НЕ копируется между организациями

### 2. Постобработка была проблемой
- Постобработка добавляла "117587" всем организациям
- Фильтры в постобработке решили проблему

### 3. Гибридный подход сработал
- Фаза 1 (промпт) + Фаза 2 (защита) = правильный результат
- Двухуровневая защита надежнее, чем только промпт

## Следующие шаги

### ✅ Завершено:
- Фаза 1: Улучшение промпта
- Фаза 2: Защита от мусора
- Тестирование на письме 016

### 📋 Рекомендации:
1. **Протестировать на других письмах:**
   - Письма с КП
   - Письма с реквизитами
   - Письма с договорами

2. **Мониторинг:**
   - Следить за логами "Отклонен адрес-индекс"
   - Проверять, не фильтруются ли валидные адреса

3. **Документация:**
   - Обновить README.md в spec
   - Добавить примеры в документацию

## Файлы изменены

1. `prompts/unified_contact_extraction_structured.txt` - добавлены инструкции
2. `src/postprocessing/attachment_evidence_extractor.py` - фильтры
3. `src/postprocessing/org_location_enrichment.py` - фильтры
4. `PROMPT_IMPROVEMENT_LOCATION_V2.md` - улучшенные инструкции
5. `ANALYSIS_AND_HYBRID_PLAN.md` - план решения
6. `STEP_1_COMPLETED.md` - отчет Фазы 1
7. `STEP_1_TEST_RESULTS.md` - результаты тестирования Фазы 1
8. `STEP_2_COMPLETED.md` - этот файл

## Итог

🎉 **ЗАДАЧА РЕШЕНА!**

Гибридный подход (промпт + постобработка) успешно решил проблему:
- Город извлекается правильно из контекста
- Адрес НЕ копируется между организациями
- Индексы без полного адреса фильтруются
- Результат соответствует ожиданиям
