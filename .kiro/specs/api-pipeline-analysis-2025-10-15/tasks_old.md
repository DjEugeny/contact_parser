# План исправлений: API Pipeline Validator
## Дата создания: 2025-10-15
## Статус: В работе

---

## 🎯 Общая стратегия

Исправления выполняются в порядке приоритета (P0 → P1 → P2 → P3).  
После каждого блока задач - тестирование и фиксация результатов.

---

## ✅ P0: Критические проблемы (БЛОКЕРЫ)

### Задача 1.1: Исправить импорт config.paths в OCR модуле
**Статус**: ⏳ TODO  
**Приоритет**: 🔴 P0-КРИТИЧЕСКИЙ  
**Время**: 30 минут  

**Шаги**:
1. [ ] Найти файл OCR модуля (вероятно `src/core/ocr_manager.py`)
2. [ ] Проверить импорт `config.paths`
3. [ ] Определить правильный путь:
   - Вариант A: Создать `config/paths.py` с необходимыми константами
   - Вариант B: Изменить импорт на `src.config.config_manager` (если пути там)
   - Вариант C: Использовать `pathlib` и относительные пути
4. [ ] Исправить импорт в OCR модуле
5. [ ] Проверить, что OCR инициализируется без ошибок

**Тестирование**:
```bash
# Тест 1: Проверить импорт
python -c "from src.core.ocr_manager import OCRManager; print('OK')"

# Тест 2: Обработать тестовый PDF
python -c "
from src.core.ocr_manager import OCRManager
ocr = OCRManager()
result = ocr.process_attachment('test_files/test_image.jpg')
print(f'Success: {result[\"success\"]}, Text length: {len(result[\"text\"])}')
"
```

**Критерий успеха**:
- ✅ OCR модуль импортируется без ошибок
- ✅ Обработка тестового файла проходит успешно
- ✅ Текст извлекается из PDF/изображений

**Артефакты**:
- Коммит с исправлением
- Тест в `tests/unit/test_ocr_import_fix.py`

---

### Задача 1.2: Исправить логику organization_id в валидации
**Статус**: ⏳ TODO  
**Приоритет**: 🔴 P0-КРИТИЧЕСКИЙ  
**Время**: 1 час  

**Анализ проблемы**:
- LLM возвращает `organization_id: null`
- Автокоррекция заменяет на `1` (несуществующий ID)
- Контакты теряют связь с организациями

**Решение A** (Рекомендуется): Изменить логику промпта
1. [ ] Открыть `prompts/unified_contact_extraction_structured.txt`
2. [ ] Добавить инструкцию: "ВСЕГДА создавайте организации в массиве organizations, затем ссылайтесь на их индексы (0, 1, 2...)"
3. [ ] Пример в промпте:
   ```json
   {
     "organizations": [
       {"name": "ООО Рога и Копыта", ...}
     ],
     "contacts": [
       {"name": "Иванов И.И.", "organization_id": 0, ...}
     ]
   }
   ```
4. [ ] Пояснить: organization_id - это индекс в массиве organizations (0-based)

**Решение B**: Улучшить автокоррекцию
1. [ ] Найти `src/core/llm_response_validator.py`
2. [ ] Изменить логику автокоррекции:
   - Если `organization_id == null` и есть хотя бы 1 организация → поставить `0`
   - Если `organization_id == null` и нет организаций → создать default организацию
3. [ ] Добавить создание временной организации "Неизвестная организация"

**Решение C**: Двухэтапная обработка
1. [ ] После автокоррекции проверять наличие организации с таким ID
2. [ ] Если не найдена - создать default организацию и добавить её в список

**Рекомендация**: Реализовать **Решение A + B** (промпт + fallback автокоррекция)

**Тестирование**:
```bash
# Прогнать тестовое письмо
python api_pipeline_validator.py --test-email email_009_20250828_20250828_cnmt_ru_84c2a1ae.json

# Проверить:
# 1. Нет ошибок "None is not of type 'integer'"
# 2. Нет предупреждений "Не найден маппинг для organization_id"
# 3. Контакты связаны с реальными организациями
```

**Критерий успеха**:
- ✅ Ноль ошибок валидации organization_id
- ✅ Все контакты привязаны к существующим организациям
- ✅ Автокоррекция создаёт организации при необходимости

**Артефакты**:
- Обновлённый промпт
- Коммит с улучшенной автокоррекцией
- Тест в `tests/integration/test_organization_id_validation.py`

---

### Задача 1.3: Интеграционный тест P0 исправлений
**Статус**: ⏳ TODO  
**Приоритет**: 🔴 P0  
**Время**: 30 минут  

**Шаги**:
1. [ ] Запустить обработку 3 тестовых писем с вложениями
2. [ ] Проверить логи на отсутствие ошибок P0
3. [ ] Проверить, что:
   - OCR работает (текст из PDF извлечён)
   - organization_id валиден (нет автокоррекций или они корректны)
   - Контакты связаны с организациями
4. [ ] Зафиксировать метрики "до/после"

**Команда**:
```bash
python api_pipeline_validator.py --test-mode --emails 3 --date 2025-08-28
```

**Критерий успеха**:
- ✅ Ноль критических ошибок в логе
- ✅ OCR успешность > 80%
- ✅ organization_id валидность = 100%

**Артефакт**:
- Отчёт `reports/p0_fixes_validation_report.md`

---

## 🟠 P1: Высокие проблемы (ПОТЕРЯ ДАННЫХ)

### Задача 2.1: Реализовать корректную связь контактов с организациями
**Статус**: ⏳ TODO  
**Приоритет**: 🟠 P1  
**Время**: 45 минут  

**Контекст**: После исправления P0, нужно убедиться, что логика работает правильно

**Шаги**:
1. [ ] Проверить код постобработки (`src/core/postprocessor.py`?)
2. [ ] Убедиться, что маппинг organization_id → organization корректен
3. [ ] Добавить валидацию: "контакт без организации" = предупреждение, но не ошибка
4. [ ] Логировать статистику:
   - Контактов с организацией
   - Контактов без организации
   - Созданных default организаций

**Тестирование**:
```bash
# Запустить на реальных данных
python api_pipeline_validator.py --date 2025-08-28

# Проверить статистику в конце лога
```

**Критерий успеха**:
- ✅ > 90% контактов имеют валидную организацию
- ✅ Статистика выводится в конце обработки

**Артефакт**:
- Улучшенная статистика в логах
- Тест `tests/integration/test_contact_organization_link.py`

---

### Задача 2.2: Настроить DaData API или отключить функциональность
**Статус**: ⏳ TODO  
**Приоритет**: 🟠 P1  
**Время**: 20 минут  

**Выбор решения**:
- **Вариант A**: Получить API ключ DaData (если нужна функциональность)
- **Вариант B**: Отключить DaData enrichment (если не критично)

**Рекомендация**: Сначала Вариант B, затем по необходимости Вариант A

**Шаги (Вариант B - Отключение)**:
1. [ ] Найти инициализацию DaData (вероятно `src/core/postprocessor.py` или `src/enrichment/`)
2. [ ] Добавить флаг в `.env`: `DADATA_ENABLED=false`
3. [ ] Обернуть инициализацию в проверку флага
4. [ ] Убрать предупреждения из логов при отключении

**Шаги (Вариант A - Настройка)**:
1. [ ] Зарегистрироваться на dadata.ru (бесплатный тариф: 10,000 запросов/день)
2. [ ] Получить API ключ
3. [ ] Добавить в `.env`: `DADATA_API_KEY=your_key_here`
4. [ ] Проверить работу enrichment на тестовых данных

**Тестирование**:
```bash
# Вариант B
grep "DaData" data/logs/*.log | grep -v "⚠️"  # Не должно быть предупреждений

# Вариант A
python -c "
from src.enrichment.dadata_enricher import DaDataEnricher
enricher = DaDataEnricher()
result = enricher.enrich_organization({'name': 'ООО Яндекс'})
print(result)
"
```

**Критерий успеха**:
- ✅ Нет предупреждений о DaData в логах (Вариант B)
- ✅ Enrichment работает (Вариант A)

**Артефакт**:
- Обновлённый `.env.example` с документацией
- Коммит с изменениями

---

## 🟡 P2: Средние проблемы (КАЧЕСТВО ДАННЫХ)

### Задача 3.1: Исправить парсинг вложений 'unknown'
**Статус**: ⏳ TODO  
**Приоритет**: 🟡 P2  
**Время**: 1 час  

**Исследование**:
1. [ ] Найти email fetcher код (возможно `src/fetcher/` или архив)
2. [ ] Проверить, как сохраняются метаданные вложений
3. [ ] Найти примеры JSON с 'unknown' вложениями
4. [ ] Определить причину:
   - Inline изображения (подписи, лого)?
   - Технические артефакты?
   - Ошибка парсинга?

**Решения**:
- **Если inline изображения**: пропускать их (добавить фильтр)
- **Если ошибка парсинга**: исправить email fetcher
- **Если технические артефакты**: игнорировать в логах

**Шаги**:
1. [ ] Добавить фильтр в `api_pipeline_validator.py` или `src/adapters/`:
   ```python
   # Пропускать вложения 'unknown' без пути
   if attachment.get('name') == 'unknown' and not attachment.get('file_path'):
       continue
   ```
2. [ ] Убрать предупреждения для таких вложений
3. [ ] Логировать количество пропущенных 'unknown' в статистике

**Тестирование**:
```bash
# Запустить обработку
python api_pipeline_validator.py --date 2025-08-28

# Проверить, что предупреждений стало меньше
grep "⚠️ У вложения 'unknown'" data/logs/*.log | wc -l  # Должно быть 0
```

**Критерий успеха**:
- ✅ Нет предупреждений о 'unknown' вложениях
- ✅ Статистика показывает количество пропущенных вложений

**Артефакт**:
- Коммит с фильтром
- Документация в коде

---

### Задача 3.2: Улучшить промпт для создания organizations
**Статус**: ⏳ TODO  
**Приоритет**: 🟡 P2  
**Время**: 45 минут  

**Цель**: Повысить качество извлечения организаций

**Шаги**:
1. [ ] Проанализировать текущий промпт `prompts/unified_contact_extraction_structured.txt`
2. [ ] Добавить примеры (few-shot):
   ```
   Пример 1:
   Текст: "Иван Петров (ООО "Рога и Копыта", ivan@roga.ru)"
   Результат:
   {
     "organizations": [{"name": "ООО Рога и Копыта", "email": "roga.ru"}],
     "contacts": [{"name": "Иван Петров", "organization_id": 0, "email": "ivan@roga.ru"}]
   }
   ```
3. [ ] Добавить правила:
   - "Если видишь название компании в тексте - создай организацию"
   - "Если домен email не @gmail/@mail/@yandex - это корпоративная почта, создай организацию"
   - "organization_id - индекс в массиве organizations (0, 1, 2...)"
4. [ ] Добавить валидацию в промпте: "organization_id ОБЯЗАТЕЛЬНО должен быть integer"

**Тестирование**:
```bash
# Прогнать 10 писем с новым промптом
python api_pipeline_validator.py --date 2025-08-28 --count 10

# Сравнить результаты:
# - Количество созданных организаций (до/после)
# - Количество ошибок валидации (до/после)
```

**Критерий успеха**:
- ✅ Количество созданных организаций увеличилось на 30%+
- ✅ Ошибки валидации organization_id = 0

**Артефакт**:
- Новая версия промпта (с версионированием)
- A/B тест отчёт в `reports/prompt_improvement_ab_test.md`

### Задача 3.3: Исправить счётчик статистики (сбрасывать между циклами)
**Статус**: ⏳ TODO  
**Приоритет**: 🟡 P2  
**Время**: 30 минут  

**Проблема**:
- При обработке нескольких дат подряд счётчик накапливается
- Цикл 1: обработано 18 писем → статистика показывает "18 успешно"
- Цикл 2: обработано 9 писем → статистика показывает "27 успешно" (НЕВЕРНО!)
- Ожидалось: "9 успешно"

**Локация**: `src/core/resilient_email_processor.py` (предположительно)

**Решение**:
1. [ ] Найти класс `ResilientEmailProcessor`
2. [ ] Проверить метод инициализации и сброса счётчиков
3. [ ] Добавить метод `reset_stats()` или вызывать его в начале каждого цикла
4. [ ] Или: создавать новый экземпляр процессора для каждой даты
5. [ ] Убедиться, что счётчики изолированы между запусками

**Шаги**:
1. [ ] Найти где создаётся `ResilientEmailProcessor` в `api_pipeline_validator.py`
2. [ ] Проверить логику переиспользования экземпляра
3. [ ] Вариант A: Создавать новый экземпляр для каждой даты
   ```python
   # Плохо (текущая логика):
   processor = ResilientEmailProcessor()  # Один раз
   for date in dates:
       processor.process(emails_for_date)  # Переиспользование
   
   # Хорошо:
   for date in dates:
       processor = ResilientEmailProcessor()  # Новый для каждой даты
       processor.process(emails_for_date)
   ```
4. [ ] Вариант B: Добавить метод сброса
   ```python
   processor.reset_statistics()  # Перед каждым циклом
   ```
5. [ ] Выбрать и реализовать подходящий вариант

**Тестирование**:
```bash
# Запустить обработку двух дат подряд
python api_pipeline_validator.py

# В меню выбрать:
# 1. Дата 2025-08-27 (18 писем)
# 2. Обработать все
# 3. Вернуться
# 4. Дата 2025-08-28 (9 писем)
# 5. Обработать все

# Проверить статистику второго цикла:
# Должно быть: "Успешно с первой попытки: 9"
# Не должно быть: "Успешно с первой попытки: 27"
```

**Критерий успеха**:
- ✅ Каждый цикл показывает свою статистику
- ✅ Нет накопления счётчиков между циклами
- ✅ Логи корректно отображают результаты текущего цикла

**Артефакт**:
- Исправленный `ResilientEmailProcessor`
- Тест в `tests/integration/test_statistics_reset.py`

---

## 🟢 P3: Низкие проблемы (УДОБСТВО)

### Задача 4.1: Очистить логи от лишних предупреждений
**Статус**: ⏳ TODO  
**Приоритет**: 🟢 P3  
**Время**: 30 минут  

**Шаги**:
1. [ ] Убрать дублирующие предупреждения
2. [ ] Группировать однотипные сообщения:
   ```
   ⚠️ 15 вложений 'unknown' пропущено (нет пути к файлу)
   ```
   Вместо:
   ```
   ⚠️ У вложения 'unknown' отсутствует путь к файлу (x15)
   ```
3. [ ] Добавить уровни логирования:
   - INFO: нормальный процесс
   - WARNING: можно игнорировать
   - ERROR: требует внимания
4. [ ] Настроить логер для вывода только WARNING+ в консоль, INFO+ в файл

**Критерий успеха**:
- ✅ Логи легко читаются
- ✅ Важные сообщения не теряются в шуме

---

### Задача 4.2: Добавить детальную статистику в конце обработки
**Статус**: ⏳ TODO  
**Приоритет**: 🟢 P3  
**Время**: 45 минут  

**Шаги**:
1. [ ] Создать класс `ProcessingStatistics` для сбора метрик
2. [ ] Добавить счётчики:
   - Письма: всего / успешно / с ошибками
   - Контакты: извлечено / с организацией / без организации
   - Организации: извлечено / с ИНН / без ИНН
   - КП: извлечено / валидных / с датами
   - Вложения: всего / обработано OCR / ошибки OCR / пропущено
   - Время: общее / среднее на письмо / на OCR
3. [ ] Вывести красивую таблицу в конце:
   ```
   ═══════════════════════════════════════════════
   📊 ИТОГОВАЯ СТАТИСТИКА ОБРАБОТКИ
   ═══════════════════════════════════════════════
   📧 Письма:           9 / 9 (100%)
   👥 Контакты:         18 (17 с орг, 1 без)
   🏢 Организации:      8 (2 с ИНН)
   📄 КП:               3 (все валидные)
   📎 Вложения:         12 (10 OCR успех, 1 ошибка, 1 пропущено)
   ⏱️  Время:           125.3s (13.9s/письмо, 8.2s/OCR)
   ═══════════════════════════════════════════════
   ```

**Критерий успеха**:
- ✅ Статистика выводится после каждого запуска
- ✅ Можно быстро оценить качество обработки

**Артефакт**:
- Класс `ProcessingStatistics` в `src/core/statistics.py`
- Интеграция в `api_pipeline_validator.py`

---

## 📋 Чек-лист перед релизом

### Перед началом массовой обработки:
- [ ] Все задачи P0 выполнены и протестированы
- [ ] Все задачи P1 выполнены и протестированы
- [ ] Интеграционный тест на 50+ письмах прошёл успешно
- [ ] Отчёт по качеству создан (см. `memory-bank/reports/README.md`)
- [ ] Логи чистые, без критических ошибок
- [ ] Резервная копия `crm.db` создана

### Метрики качества для релиза:
- ✅ OCR успешность ≥ 90%
- ✅ organization_id валидность = 100%
- ✅ Контакты с организацией ≥ 85%
- ✅ Ошибок валидации JSON ≤ 5%
- ✅ Среднее время обработки ≤ 20s/письмо

---

## 📝 Процедура тестирования после каждого блока

### 1. Unit тесты
```bash
pytest tests/unit/ -v
```

### 2. Интеграционные тесты
```bash
pytest tests/integration/ -v
```

### 3. Smoke тест на реальных данных
```bash
python api_pipeline_validator.py --date 2025-08-28 --count 5
```

### 4. Проверка логов
```bash
# Критические ошибки
grep "❌\|ERROR" data/logs/latest.log

# Предупреждения
grep "⚠️\|WARN" data/logs/latest.log | sort | uniq -c
```

### 5. Проверка БД
```bash
sqlite3 crm.db "SELECT COUNT(*) FROM contacts WHERE organization_id IS NOT NULL"
sqlite3 crm.db "SELECT COUNT(*) FROM organizations"
```

---

## 📊 Трекинг прогресса

### P0 (Критические): 0/3 ✅
- [ ] Задача 1.1: OCR импорт
- [ ] Задача 1.2: organization_id
- [ ] Задача 1.3: Интеграционный тест P0

### P1 (Высокие): 0/2 ✅
- [ ] Задача 2.1: Связь контактов с организациями
- [ ] Задача 2.2: DaData настройка

### P2 (Средние): 0/3 ✅
- [ ] Задача 3.1: Парсинг 'unknown' вложений
- [ ] Задача 3.2: Улучшение промпта
- [ ] Задача 3.3: Исправить счётчик статистики

### P3 (Низкие): 0/2 ✅
- [ ] Задача 4.1: Очистка логов
- [ ] Задача 4.2: Статистика

---

## 🔗 Связанные документы

- Отчёт по анализу: `ANALYSIS_REPORT.md` (эта же папка)
- Инструкция по отчётам: `contact_parser/memory-bank/reports/README.md`
- PRD проекта: `contact_parser/memory-bank/mini_crm_prd/00_README.md`
- Руководство агента: `AGENTS.md`

---

## ✍️ Логи изменений

### 2025-10-15 23:41
- ✅ Создан план исправлений
- ✅ Определены приоритеты P0-P3
- ✅ Детализированы шаги для каждой задачи
- ⏳ Ожидается начало работы по P0

---

**Создал**: Cascade AI Assistant  
**Дата**: 2025-10-15  
**Версия**: 1.0  
**Статус плана**: 📋 Утверждён, готов к выполнению
