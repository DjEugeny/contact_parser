# Анализ незакрытых задач

## Обзор
- Источники: `.kiro/specs/**/tasks.md`, профильные отчёты в `.kiro/specs`, инструкции из `AGENTS.md`.
- Основные открытые пункты сгруппированы по ключевым инициативам, влияющим на корректность данных и стабильность пайплайна.

## Ключевые блоки с открытыми задачами

### 1. GID v2 — финализация внедрения
- Незакрытая проверка качества полной миграции: `3.3 Верификация` (`.kiro/specs/gid-deduplication-analysis-2025-10-16/TASKS.md:337`).
- Отсутствуют инструменты мониторинга качества после включения v2: `4.1 Дашборд метрик`, `4.2 Алерты` (строки 297-298).
- Риск: при отсутствии финальной верификации и мониторинга возможны скрытые дубли или деградация идентификации контактов.

### 2. Pipeline Issues Fix — контроль качества ответов LLM
- Отсутствует pre-validation для автоматического исправления частых ошибок (`7`, `7.1`, `7.2` в `.kiro/specs/pipeline-issues-analysis-2025-10-06/tasks.md:145-208`).
- Не завершено A/B тестирование новой версии промпта (`6.4`, строка 177) и блок по восстановлению путей вложений (`8`, `8.1`, `8.2`, строки 209-226).
- Риск: повторяющиеся ошибки валидации и пропуски вложений продолжают нагружать автокоррекцию и снижают качество данных.

### 3. Фильтрация персональных email в организациях
- После обновления промпта отсутствует ручная валидация результатов (`**Валидация**`, `.kiro/specs/contact-email-filter/tasks.md:19`).
- Нет отчёта о результате работы и влиянии на БД (`**Отчет**`, строка 20).
- Риск: персональные адреса могут оставаться в полях организаций, ухудшая качество CRM и отчётность.

### 4. SQLite-реестр вложений
- Не выполнена финальная пост-обработка и аудит (`tasks.md:14` в `.kiro/specs/attachments_sqlite_registry`).
- Без аудита невозможно подтвердить отсутствие пропусков и расхождений после миграции на SQLite.

### 5. OCR Naming Harmonization
- Требуется выборочный прогон для подтверждения поддержки `.odt/.ods` (`.kiro/specs/ocr-naming-harmonization/tasks.md:14`).
- Риск: новые вложения OpenDocument могут не обрабатываться корректно в боевом режиме.

### 6. OpenRouter Provider Hardening
- Не реализованы мониторинг провайдеров, fallback и стресс-тесты (`tasks.md:33-57` в `.kiro/specs/fix-openrouter-provider`).
- Риск: при сбоях OpenRouter отсутствует детальная диагностика и механизм быстрого восстановления.

## Итоговые наблюдения
- Критические пробелы сосредоточены в блоках GID v2 и основной цепочке обработки писем.
- Несколько задач (аудит вложений, OCR ручная проверка) требуют выделения времени на контроль качества после уже выполненных изменений.
- Для части инициатив (OpenRouter, мониторинг GID) необходимы эксплуатационные инструменты, без которых трудно поддерживать стабильность после релиза.
