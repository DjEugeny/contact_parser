---
description: Optimized file verification
globs: file-verification.mdc
alwaysApply: false
---
# ОПТИМИЗИРОВАННАЯ СИСТЕМА ПРОВЕРКИ ФАЙЛОВ

> **Кратко**: Эта система эффективно проверяет и создает необходимые структуры файлов Memory Bank с использованием пакетных операций и команд, оптимизированных для платформы.

## 🔍 ОПТИМИЗИРОВАННЫЙ РАБОЧИЙ ПРОЦЕСС ПРОВЕРКИ ФАЙЛОВ

```mermaid
graph TD
    Start["Начало проверки<br>файлов"] --> VerifyAll["Проверка всех<br>требуемых компонентов"]
    VerifyAll --> MissingCheck{"Отсутствуют<br>компоненты?"}
    MissingCheck -->|"Да"| BatchCreate["Пакетное создание<br>всех отсутствующих элементов"]
    MissingCheck -->|"Нет"| Complete["Проверка<br>завершена"]
    BatchCreate --> Report["Создать отчет<br>о проверке"]
    Report --> Complete
```

## 📋 ОПТИМИЗИРОВАННОЕ СОЗДАНИЕ ДИРЕКТОРИЙ

```mermaid
graph TD
    Start["Создание<br>директорий"] --> DetectOS["Определение<br>операционной системы"]
    DetectOS -->|"Windows"| WinCmd["Пакетное создание<br>команда Windows"]
    DetectOS -->|"Mac/Linux"| UnixCmd["Пакетное создание<br>команда Unix"]
    WinCmd & UnixCmd --> Verify["Проверка<br>успеха создания"]
    Verify --> Complete["Настройка директорий<br>завершена"]
```

### Команды, специфичные для платформы

#### Windows (PowerShell)
```powershell
# Создание всех директорий одной командой
mkdir memory-bank, docs, docs\archive -ErrorAction SilentlyContinue

# Создание всех требуемых файлов
$files = @(".cursorrules", "tasks.md", 
           "memory-bank\projectbrief.md", 
           "memory-bank\productContext.md",
           "memory-bank\systemPatterns.md",
           "memory-bank\techContext.md",
           "memory-bank\activeContext.md",
           "memory-bank\progress.md")

foreach ($file in $files) {
    if (-not (Test-Path $file)) {
        New-Item -Path $file -ItemType File -Force
    }
}
```

#### Mac/Linux (Bash)
```bash
# Создание всех директорий одной командой
mkdir -p memory-bank docs/archive

# Создание всех требуемых файлов
touch .cursorrules tasks.md \
      memory-bank/projectbrief.md \
      memory-bank/productContext.md \
      memory-bank/systemPatterns.md \
      memory-bank/techContext.md \
      memory-bank/activeContext.md \
      memory-bank/progress.md
```

## 📝 УПРОЩЕННЫЙ ПРОЦЕСС ПРОВЕРКИ

Вместо проверки каждого компонента отдельно выполняйте пакетную проверку:

```powershell
# Windows - PowerShell
$requiredDirs = @("memory-bank", "docs", "docs\archive")
$requiredFiles = @(".cursorrules", "tasks.md")
$mbFiles = @("projectbrief.md", "productContext.md", "systemPatterns.md", 
             "techContext.md", "activeContext.md", "progress.md")

$missingDirs = $requiredDirs | Where-Object { -not (Test-Path $_ -PathType Container) }
$missingFiles = $requiredFiles | Where-Object { -not (Test-Path $_ -PathType Leaf) }
$missingMBFiles = $mbFiles | ForEach-Object { "memory-bank\$_" } | 
                  Where-Object { -not (Test-Path $_) -or (Test-Path $_ -PathType Container) }

if ($missingDirs.Count -eq 0 -and $missingFiles.Count -eq 0 -and $missingMBFiles.Count -eq 0) {
    Write-Output "✓ Все требуемые компоненты проверены"
} else {
    # Создание всех отсутствующих элементов сразу
    if ($missingDirs.Count -gt 0) {
        $missingDirs | ForEach-Object { mkdir $_ -Force }
    }
    if ($missingFiles.Count -gt 0 -or $missingMBFiles.Count -gt 0) {
        $allMissingFiles = $missingFiles + $missingMBFiles
        $allMissingFiles | ForEach-Object { New-Item -Path $_ -ItemType File -Force }
    }
}
```

## 📝 ИНИЦИАЛИЗАЦИЯ ШАБЛОНОВ

Оптимизируйте создание шаблонов с помощью одного скрипта:

```powershell
# Windows - PowerShell
$templates = @{
    "tasks.md" = @"
# Memory Bank: Задачи

## Текущая задача
[Задача еще не определена]

## Статус
- [ ] Определение задачи
- [ ] План реализации
- [ ] Выполнение
- [ ] Документация

## Требования
[Требования еще не определены]
"@

    "memory-bank\activeContext.md" = @"
# Memory Bank: Активный контекст

## Текущий фокус
[Активный фокус еще не определен]

## Статус
[Статус еще не определен]

## Последние изменения
[Изменения еще не записаны]
"@

    # Добавить другие шаблоны здесь
}

foreach ($file in $templates.Keys) {
    if (Test-Path $file) {
        Set-Content -Path $file -Value $templates[$file]
    }
}
```

## 🔍 ЛУЧШИЕ ПРАКТИКИ ОПТИМИЗАЦИИ ПРОИЗВОДИТЕЛЬНОСТИ

1. **Пакетные операции**: Всегда используйте пакетные операции вместо отдельных команд
   ```
   # ХОРОШО: Создание всех директорий сразу
   mkdir memory-bank docs docs\archive
   
   # ПЛОХО: Создание директорий по одной
   mkdir memory-bank
   mkdir docs
   mkdir docs\archive
   ```

2. **Оптимизация предварительной проверки**: Сначала проверьте, что отсутствует, затем создайте только недостающее
   ```
   # Сначала проверьте, что отсутствует
   $missingItems = ...
   
   # Затем создайте только то, что отсутствует
   if ($missingItems) { ... }
   ```

3. **Обработка ошибок**: Включайте обработку ошибок во все команды
   ```
   mkdir memory-bank, docs, docs\archive -ErrorAction SilentlyContinue
   ```

4. **Адаптация к платформе**: Автоматически определяйте платформу и используйте соответствующие команды
   ```
   if ($IsWindows) {
       # Команды Windows
   } else {
       # Команды Unix
   }
   ```

5. **Однопроходная проверка**: Проверяйте структуру директорий за один проход
   ```
   $requiredPaths = @("memory-bank", "docs", "docs\archive", ".cursorrules", "tasks.md")
   $missingPaths = $requiredPaths | Where-Object { -not (Test-Path $_) }
   ```

## 📝 ФОРМАТ ОТЧЕТА О ПРОВЕРКЕ

```
✅ ПРОВЕРКА ЗАВЕРШЕНА
- Созданные директории: [список]
- Созданные файлы: [список]
- Все компоненты проверены

Система Memory Bank готова к использованию.
```