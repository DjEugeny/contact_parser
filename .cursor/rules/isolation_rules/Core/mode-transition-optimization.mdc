---
description: Протокол оптимизированных переходов между режимами
globs: "**/mode-transition*/**", "**/context-preservation*/**"
alwaysApply: false
---

# ОПТИМИЗАЦИЯ ПЕРЕХОДОВ МЕЖДУ РЕЖИМАМИ

> **Кратко**: Этот файл реализует оптимизированные переходы между режимами для эффективного сохранения контекста между различными фазами системы Memory Bank.

## 🔄 УНИФИЦИРОВАННЫЙ ПРОТОКОЛ ПЕРЕДАЧИ КОНТЕКСТА

```mermaid
graph TD
    Start["Режим A"] --> Create["Создать документ<br>сводки контекста"]
    Create --> Store["Сохранить критически<br>важные данные контекста"]
    Store --> Transition["Переход<br>к режиму B"]
    Transition --> Verify["Проверить<br>доступность контекста"]
    Verify --> Load["Загрузить релевантные<br>данные контекста"]
    Load --> Continue["Продолжить в<br>режиме B"]
    
    style Start fill:#4da6ff,stroke:#0066cc,color:white
    style Create fill:#ffa64d,stroke:#cc7a30,color:white
    style Store fill:#4dbb5f,stroke:#36873f,color:white
    style Transition fill:#d94dbb,stroke:#a3378a,color:white
    style Verify fill:#4dbbbb,stroke:#368787,color:white
    style Load fill:#d971ff,stroke:#a33bc2,color:white
    style Continue fill:#ff71c2,stroke:#c23b8a,color:white
```

## 📊 ДОКУМЕНТ ПЕРЕХОДА КОНТЕКСТА

Создайте стандартизированный документ перехода при смене режимов:

```markdown
# ПЕРЕХОД МЕЖДУ РЕЖИМАМИ: [Исходный режим] → [Целевой режим]

## Сводка контекста
- Задача: [Название/описание задачи]
- Сложность: Уровень [1-4]
- Текущая фаза: [Название фазы]
- Прогресс: [Процент или статус]

## Ключевые решения
- [Решение 1]: [Краткое описание]
- [Решение 2]: [Краткое описание]
- [Решение 3]: [Краткое описание]

## Критически важный контекст
- [Элемент контекста 1]: [Значение/статус]
- [Элемент контекста 2]: [Значение/статус]
- [Элемент контекста 3]: [Значение/статус]

## Следующие шаги
1. [Следующий шаг 1]
2. [Следующий шаг 2]
3. [Следующий шаг 3]

## Указатели на ресурсы
- [Ресурс 1]: [Расположение]
- [Ресурс 2]: [Расположение]
- [Ресурс 3]: [Расположение]
```

## 🔍 ОБРАБОТЧИКИ ПЕРЕХОДОВ ДЛЯ КОНКРЕТНЫХ РЕЖИМОВ

### Переход VAN → PLAN

```markdown
### VAN → PLAN
- Сохранённый контекст: Уровень сложности, определение платформы, структура файлов
- Передаваемые файлы: tasks.md (инициализирован), activeContext.md (инициализирован)
- Оптимизация правил: Предварительная загрузка правил планирования на основе уровня сложности
```

### Переход PLAN → CREATIVE

```markdown
### PLAN → CREATIVE
- Сохранённый контекст: Требования к задаче, список компонентов, флаги творческой фазы
- Передаваемые файлы: tasks.md (обновлён с планом), список компонентов творческой фазы
- Оптимизация правил: Загрузка только шаблонов творческой фазы для идентифицированных компонентов
```

### Переход CREATIVE → IMPLEMENT

```markdown
### CREATIVE → IMPLEMENT
- Сохранённый контекст: Дизайнерские решения, рекомендации по реализации, требования
- Передаваемые файлы: tasks.md, документы дизайна, контрольный список реализации
- Оптимизация правил: Предварительная загрузка шаблонов реализации на основе дизайнерских решений
```

### Переход IMPLEMENT → REFLECT

```markdown
### IMPLEMENT → REFLECT
- Сохранённый контекст: Статус реализации, возникшие проблемы, решения
- Передаваемые файлы: tasks.md, progress.md, заметки по реализации
- Оптимизация правил: Загрузка шаблонов рефлексии на основе статуса завершения
```

## 🧠 ИЕРАРХИЧЕСКОЕ КЭШИРОВАНИЕ ПРАВИЛ

Реализуйте кэширование правил для избежания повторной загрузки:

```javascript
// Псевдокод для кэширования правил
const ruleCache = {
  core: {}, // Основные правила, общие для всех режимов
  van: {},
  plan: {},
  creative: {},
  implement: {},
  reflect: {},
  archive: {}
};

// Проверка кэша перед загрузкой
function loadRule(rulePath) {
  const cacheKey = getCacheKey(rulePath);
  const category = getCategoryFromPath(rulePath);
  
  if (ruleCache[category][cacheKey]) {
    return ruleCache[category][cacheKey];
  }
  
  const ruleContent = readRuleFromFile(rulePath);
  ruleCache[category][cacheKey] = ruleContent;
  
  return ruleContent;
}

// Инвалидация только определённых правил при необходимости
function invalidateRule(rulePath) {
  const cacheKey = getCacheKey(rulePath);
  const category = getCategoryFromPath(rulePath);
  
  if (ruleCache[category][cacheKey]) {
    delete ruleCache[category][cacheKey];
  }
}
```

## ⚡ ДИФФЕРЕНЦИАЛЬНЫЕ ОБНОВЛЕНИЯ MEMORY BANK

```mermaid
graph TD
    Start["Запрос на обновление<br>Memory Bank"] --> Check{"Файл<br>изменился?"}
    Check -->|"Нет"| Skip["Пропустить обновление<br>(без изменений)"]
    Check -->|"Да"| Changed{"Изменилась ли<br>конкретная секция?"}
    Changed -->|"Нет"| Full["Полное обновление<br>файла"]
    Changed -->|"Да"| Partial["Частичное<br>обновление"]
    
    style Start fill:#4da6ff,stroke:#0066cc,color:white
    style Check fill:#ffa64d,stroke:#cc7a30,color:white
    style Skip fill:#4dbb5f,stroke:#36873f,color:white
    style Changed fill:#d94dbb,stroke:#a3378a,color:white
    style Full fill:#4dbbbb,stroke:#368787,color:white
    style Partial fill:#d971ff,stroke:#a33bc2,color:white
```

Реализуйте более эффективный механизм обновления:

```javascript
// Псевдокод для дифференциальных обновлений
function updateMemoryBankFile(filePath, newContent) {
  // Чтение текущего содержимого
  const currentContent = readFile(filePath);
  
  // Пропуск, если изменений нет
  if (currentContent === newContent) {
    return "Изменений не обнаружено, обновление пропущено";
  }
  
  // Проверка возможности частичного обновления
  const sections = parseIntoSections(currentContent);
  const newSections = parseIntoSections(newContent);
  
  let updatedContent = currentContent;
  let updatedSections = 0;
  
  // Обновление только изменённых секций
  for (const [sectionName, sectionContent] of Object.entries(newSections)) {
    if (!sections[sectionName] || sections[sectionName] !== sectionContent) {
      updatedContent = replaceSection(updatedContent, sectionName, sectionContent);
      updatedSections++;
    }
  }
  
  // Запись обновлённого содержимого
  writeFile(filePath, updatedContent);
  
  return `Обновлено ${updatedSections} секций в ${filePath}`;
}
```

## 🔗 МОСТ CREATIVE → IMPLEMENT

Особая обработка критически важного перехода CREATIVE → IMPLEMENT:

```markdown
## МОСТ CREATIVE → IMPLEMENT

### Сводка дизайнерских решений
Автоматически сгенерированная сводка всех решений творческой фазы:

```json
{
  "components": [
    {
      "name": "КомпонентA",
      "decision": "Выбран подход X",
      "rationale": "Лучшие характеристики производительности",
      "implementation_notes": [
        "Использовать библиотеку X",
        "Реализовать кэширование",
        "Добавить обработку ошибок"
      ]
    },
    {
      "name": "КомпонентB",
      "decision": "Кастомное решение",
      "rationale": "Уникальные требования",
      "implementation_notes": [
        "Создать с нуля",
        "Модульная архитектура",
        "Требуются модульные тесты"
      ]
    }
  ]
}
```

### Контрольный список готовности к реализации
Автоматически сгенерированный контрольный список проверки:

```markdown
# Контрольный список готовности к реализации

- [ ] Дизайнерские решения доступны для всех компонентов
- [ ] Заметки по реализации предоставлены для каждого решения
- [ ] Зависимости чётко определены
- [ ] Порядок реализации определён
- [ ] Необходимые библиотеки/фреймворки задокументированы
- [ ] Потенциальные проблемы идентифицированы
```

## 🚀 АДАПТИВНАЯ ЗАГРУЗКА РЕЖИМОВ

Реализуйте прогрессивную загрузку режимов для оптимизации контекста:

```javascript
// Псевдокод для адаптивной загрузки режимов
function loadMode(modeName, taskComplexity) {
  // Всегда загружать основные правила
  loadCoreRules();
  
  // Загрузка правил, соответствующих уровню сложности
  loadComplexityRules(taskComplexity);
  
  // Загрузка основных правил, специфичных для режима
  loadModeEssentialRules(modeName);
  
  // Загрузка специализированных правил только по необходимости
  registerLazyLoadHandlers(modeName, taskComplexity);
}

function registerLazyLoadHandlers(modeName, taskComplexity) {
  // Регистрация обработчиков для загрузки дополнительных правил только при необходимости
  if (modeName === "CREATIVE") {
    registerHandler("architecture", () => loadRule("creative-phase-architecture.mdc"));
    registerHandler("algorithm", () => loadRule("creative-phase-algorithm.mdc"));
    registerHandler("uiux", () => loadRule("creative-phase-uiux.mdc"));
  }
  
  // Аналогичные шаблоны для других типов специализированных правил
}
```

## ✅ ПРИМЕРЫ ПЕРЕХОДОВ МЕЖДУ РЕЖИМАМИ

### Пример: Переход PLAN → CREATIVE

При переходе из режима PLAN в CREATIVE:

```markdown
# ПЕРЕХОД МЕЖДУ РЕЖИМАМИ: PLAN → CREATIVE

## Сводка контекста
- Задача: Реализация системы аутентификации пользователей
- Сложность: Уровень 3
- Текущая фаза: Планирование завершено
- Прогресс: 35% (Планирование: 100%, Творческая фаза: 0%, Реализация: 0%)

## Ключевые решения
- Аутентификация: Требуется изучение вариантов (JWT против сессий)
- Управление пользователями: Будет использоваться существующая схема базы данных
- Авторизация: Выбрано управление доступом на основе ролей

## Критически важный контекст
- Компоненты для творческой фазы: Механизм аутентификации, Управление сессиями
- Зависимости: База данных пользователей, Система авторизации
- Ограничения: Должна поддерживать SSO, Требования к производительности

## Следующие шаги
1. Изучить варианты аутентификации (JWT, сессии, OAuth)
2. Спроектировать подход к управлению сессиями
3. Задокументировать рекомендации по реализации

## Указатели на ресурсы
- Документ планирования: tasks.md (раздел 3)
- Требования: activeContext.md
- Референтная архитектура: docs/system-architecture.md
```

### Пример: Переход CREATIVE → IMPLEMENT

При переходе из режима CREATIVE в IMPLEMENT:

```markdown
# ПЕРЕХОД МЕЖДУ РЕЖИМАМИ: CREATIVE → IMPLEMENT

## Сводка контекста
- Задача: Реализация системы аутентификации пользователей
- Сложность: Уровень 3
- Текущая фаза: Творческая фаза завершена
- Прогресс: 70% (Планирование: 100%, Творческая фаза: 100%, Реализация: 0%)

## Ключевые решения
- Аутентификация: Выбран подход на основе JWT
- Хранение токенов: Безопасные HttpOnly cookies с защитой от CSRF
- Стратегия обновления: Тихое обновление с плавающим сроком действия

## Критически важный контекст
- Порядок реализации: Конечные точки API аутентификации, Middleware, Интеграция на стороне клиента
- Требования к тестированию: Модульные тесты для проверки JWT, Интеграционные тесты для потока аутентификации
- Соображения безопасности: Защита от XSS, Защита от CSRF, Ограничение частоты запросов

## Следующие шаги
1. Реализовать генерацию и проверку JWT
2. Создать middleware аутентификации
3. Создать конечные точки входа/выхода пользователей
4. Реализовать интеграцию аутентификации на стороне клиента

## Указатели на ресурсы
- Документ творческой фазы: creative-auth-decisions.md
- Спецификации API: api-spec.yaml
- Требования безопасности: security-policy.md
```

## 🔄 ПРЕИМУЩЕСТВА РЕАЛИЗАЦИИ

Эта оптимизация обеспечивает:

1. Снижение использования токенов во время переходов между режимами (~40% сокращение)
2. Улучшенное сохранение контекста между режимами
3. Повышенная эффективность за счёт кэширования правил
4. Целевая загрузка только необходимых правил
5. Оптимизированные обновления Memory Bank
6. Чёткие документы переходов, сохраняющие критически важный контекст