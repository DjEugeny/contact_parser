---
description: Рекомендации по выполнению команд для Memory Bank с фокусом на изоляцию
globs: command-execution.mdc
alwaysApply: false
---

# СИСТЕМА ВЫПОЛНЕНИЯ КОМАНД

> **Кратко**: Эта система предоставляет рекомендации для эффективного выполнения команд, балансируя между ясностью и оптимизацией токенов за счёт подходящего объединения команд, с надлежащей документацией команд и результатов.

## 🔍 РАБОЧИЙ ПРОЦЕСС ЭФФЕКТИВНОСТИ КОМАНД

```mermaid
graph TD
    Start["Планирование<br>команды"] --> Analyze["Анализ требований<br>команды"]
    Analyze --> Balance["Баланс между<br>ясностью и эффективностью"]
    Balance --> Complexity{"Сложность<br>команды?"}
    
    Complexity -->|"Простая"| Single["Выполнить<br>одну команду"]
    Complexity -->|"Умеренная"| Chain["Использовать эффективное<br>объединение команд"]
    Complexity -->|"Сложная"| Group["Сгруппировать в<br>логические шаги"]
    
    Single & Chain & Group --> Verify["Проверить<br>результаты"]
    Verify --> Document["Задокументировать<br>команду и результат"]
    Document --> Next["Следующая<br>команда"]
```

## 📋 РЕКОМЕНДАЦИИ ПО ОБЪЕДИНЕНИЮ КОМАНД

```mermaid
graph TD
    Command["Выполнение<br>команды"] --> ChainApprop{"Подходит ли<br>объединение?"}
    
    ChainApprop -->|"Да"| ChainTypes["Типы<br>объединения"]
    ChainApprop -->|"Нет"| SingleCmd["Использовать отдельные<br>команды"]
    
    ChainTypes --> Sequential["Последовательные операции<br>cmd1 && cmd2"]
    ChainTypes --> Conditional["Условные операции<br>cmd1 || cmd2"]
    ChainTypes --> Piping["Конвейерная передача<br>cmd1 | cmd2"]
    ChainTypes --> Grouping["Группировка команд<br>(cmd1; cmd2)"]
    
    Sequential & Conditional & Piping & Grouping --> Doc["Задокументировать<br>команды и результаты"]
```

## 🚦 РАБОЧИЙ ПРОЦЕСС ПРОВЕРКИ ДИРЕКТОРИИ

```mermaid
graph TD
    Command["Выполнение<br>команды"] --> DirCheck["Проверка текущей<br>директории"]
    DirCheck --> ProjectRoot{"В корне<br>проекта?"}
    
    ProjectRoot -->|"Да"| Execute["Выполнить<br>команду"]
    ProjectRoot -->|"Нет"| Locate["Найти корень<br>проекта"]
    
    Locate --> Found{"Корень проекта<br>найден?"}
    Found -->|"Да"| Navigate["Перейти в<br>корень проекта"]
    Found -->|"Нет"| Error["Ошибка: корень<br>проекта не найден"]
    
    Navigate --> Execute
    Execute --> Verify["Проверить<br>результаты"]
```

## 📋 КОНТРОЛЬНЫЙ СПИСОК ПРОВЕРКИ ДИРЕКТОРИИ

Перед выполнением любой команды npm или сборки:

| Шаг | Windows (PowerShell) | Unix/Linux/Mac | Цель |
|-----|----------------------|----------------|------|
| **Проверка package.json** | `Test-Path package.json` | `ls package.json` | Убедиться, что текущая директория — корень проекта |
| **Проверка родительской директории** | `Test-Path "*/package.json"` | `find . -maxdepth 2 -name package.json` | Найти потенциальные директории проекта |
| **Переход в корень проекта** | `cd [project-dir]` | `cd [project-dir]` | Перейти в правильную директорию перед выполнением команд |

## 📋 РЕКОМЕНДАЦИИ ПО КОМАНДАМ ДЛЯ REACT

Для приложений React следуйте этим строгим рекомендациям:

| Команда | Правильное использование | Неправильное использование | Примечания |
|---------|-------------------------|---------------------------|------------|
| **npm start** | `cd [project-root] && npm start` | `npm start` (из родительской директории) | Должна выполняться из директории с package.json |
| **npm run build** | `cd [project-root] && npm run build` | `cd [parent-dir] && npm run build` | Должна выполняться из директории с package.json |
| **npm install** | `cd [project-root] && npm install` | `npm install` (из неправильной директории) | Должна выполняться из директории с package.json |
| **npm create** | `npm create vite@latest my-app -- --template react` | Ручная настройка webpack | Используйте стандартные инструменты для создания проекта |

## 🔄 ШАБЛОНЫ ОБЪЕДИНЕНИЯ КОМАНД

Эффективные шаблоны объединения команд включают:

| Шаблон | Формат | Примеры | Случай использования |
|---------|--------|----------|---------------------|
| **Последовательный** | `cmd1 && cmd2` | `mkdir dir && cd dir` | Команды, которые должны выполняться последовательно, вторая только если первая успешна |
| **Условный** | `cmd1 || cmd2` | `test -f file.txt || touch file.txt` | Резервные команды, вторая выполняется только если первая не удалась |
| **Конвейерный** | `cmd1 \| cmd2` | `grep "pattern" file.txt \| wc -l` | Передача вывода первой команды в качестве ввода для второй |
| **Фоновый** | `cmd &` | `npm start &` | Выполнение команды в фоновом режиме |
| **Группировка** | `(cmd1; cmd2)` | `(echo "Start"; npm test; echo "End")` | Группировка команд для выполнения как единого целого |

## 📋 ШАБЛОН ДОКУМЕНТАЦИИ КОМАНД

```
## Выполнение команды: [Цель]

### Команда
```
[фактическая команда или цепочка]
```

### Результат
```
[вывод команды]
```

### Эффект
[Краткое описание того, что изменилось в системе]

### Следующие шаги
[Что нужно сделать дальше]
```

## 🔍 УЧЕТ ОСОБЕННОСТЕЙ ПЛАТФОРМЫ

```mermaid
graph TD
    Platform["Обнаружение<br>платформы"] --> Windows["Команды<br>Windows"]
    Platform --> Unix["Команды<br>Unix/Linux/Mac"]
    
    Windows --> WinAdapt["Адаптация команд<br>Windows"]
    Unix --> UnixAdapt["Адаптация команд<br>Unix"]
    
    WinAdapt --> WinChain["Объединение в Windows:<br>Команды разделены &"]
    UnixAdapt --> UnixChain["Объединение в Unix:<br>Команды разделены ;"]
    
    WinChain & UnixChain --> Execute["Выполнение<br>платформозависимых<br>команд"]
```

## 📋 ПРИМЕРЫ ЭФФЕКТИВНОСТИ КОМАНД

Примеры эффективного использования команд:

| Неэффективно | Эффективно | Объяснение |
|-------------|-----------|-------------|
| `mkdir dir`<br>`cd dir`<br>`npm init -y` | `mkdir dir && cd dir && npm init -y` | Объединяет связанные последовательные операции |
| `ls`<br>`grep "\.js$"` | `ls \| grep "\.js$"` | Передаёт вывод первой команды во вторую |
| `test -f file.txt`<br>`if not exists, touch file.txt` | `test -f file.txt \|\| touch file.txt` | Создаёт файл только если он не существует |
| `mkdir dir1`<br>`mkdir dir2`<br>`mkdir dir3` | `mkdir dir1 dir2 dir3` | Использует возможность команды принимать несколько аргументов |
| `npm install pkg1`<br>`npm install pkg2` | `npm install pkg1 pkg2` | Устанавливает несколько пакетов одной командой |

## 📋 СТАНДАРТЫ ИНИЦИАЛИЗАЦИИ ПРОЕКТОВ REACT

Всегда используйте эти стандартные подходы для создания проектов React:

| Подход | Команда | Преимущества | Избегает |
|--------|---------|--------------|----------|
| **Create React App** | `npx create-react-app my-app` | Преднастроенные webpack и babel | Ошибок ручной конфигурации |
| **Create React App с TypeScript** | `npx create-react-app my-app --template typescript` | Типобезопасность + преднастройка | Несогласованных модульных систем |
| **Vite** | `npm create vite@latest my-app -- --template react` | Более быстрые сборки | Сложных настроек webpack |
| **Next.js** | `npx create-next-app@latest my-app` | Поддержка SSR | Конфликтов модульных систем |

## ⚠️ РАБОЧИЙ ПРОЦЕСС ОБРАБОТКИ ОШИБОК

```mermaid
sequenceDiagram
    participant User as Пользователь
    participant AI as ИИ
    participant System as Система
    
    AI->>System: Выполнить команду
    System->>AI: Вернуть результат
    
    alt Успех
        AI->>AI: Проверить ожидаемый результат
        AI->>User: Сообщить об успехе
    else Ошибка
        AI->>AI: Проанализировать сообщение об ошибке
        AI->>AI: Определить вероятную причину
        AI->>User: Объяснить ошибку и причину
        AI->>User: Предложить корректирующее действие
        User->>AI: Одобрить исправление
        AI->>System: Выполнить исправленную команду
    end
```

## 📋 ПРОВЕРКА РЕЗУЛЬТАТОВ ВЫПОЛНЕНИЯ КОМАНД

После выполнения команды проверьте:

```mermaid
graph TD
    Execute["Выполнить<br>команду"] --> Check{"Проверить<br>результат"}
    
    Check -->|"Успех"| Verify["Проверить ожидаемый<br>результат"]
    Check -->|"Ошибка"| Analyze["Проанализировать<br>ошибку"]
    
    Verify -->|"Ожидаемый"| Document["Задокументировать<br>успех"]
    Verify -->|"Неожиданный"| Investigate["Исследовать<br>неожиданный результат"]
    
    Analyze --> Diagnose["Диагностировать<br>причину ошибки"]
    Diagnose --> Correct["Предложить<br>исправление"]
    
    Document & Investigate & Correct --> Next["Следующий шаг<br>в процессе"]
```

## 📝 КОНТРОЛЬНЫЙ СПИСОК ВЫПОЛНЕНИЯ КОМАНД

```
✓ КОНТРОЛЬНЫЙ СПИСОК ВЫПОЛНЕНИЯ КОМАНД
- Цель команды четко определена? [ДА/НЕТ]
- Достигнут подходящий баланс между ясностью и эффективностью? [ДА/НЕТ]
- Учтены особенности платформы? [ДА/НЕТ]
- Команда задокументирована с результатами? [ДА/НЕТ]
- Результат проверен на соответствие ожиданиям? [ДА/НЕТ]
- Ошибки (если есть) корректно обработаны? [ДА/НЕТ/Н/П]
- Для npm/команд сборки: Выполнено из корня проекта? [ДА/НЕТ/Н/П]
- Для проектов React: Использованы стандартные инструменты? [ДА/НЕТ/Н/П]

→ Если все ДА: Выполнение команды завершено
→ Если есть НЕТ: Устраните недостающие элементы
```

## 🚨 ПРЕДУПРЕЖДЕНИЯ ПО ВЫПОЛНЕНИЮ КОМАНД

Избегайте этих распространенных проблем с командами:

```mermaid
graph TD
    Warning["Предупреждения<br>по командам"] --> W1["Избыточная<br>многословность"]
    Warning --> W2["Недостаточная<br>обработка ошибок"]
    Warning --> W3["Ненужная<br>сложность"]
    Warning --> W4["Деструктивные<br>операции без<br>подтверждения"]
    Warning --> W5["Выполнение в<br>неправильной директории"]
    
    W1 --> S1["Используйте флаги для<br>уменьшения лишнего вывода"]
    W2 --> S2["Включите обработку ошибок<br>в цепочки команд"]
    W3 --> S3["Предпочитайте встроенные<br>возможности команд"]
    W4 --> S4["Показывайте подтверждение<br>перед деструктивными действиями"]
    W5 --> S5["Проверяйте директорию перед<br>npm/командами сборки"]
```