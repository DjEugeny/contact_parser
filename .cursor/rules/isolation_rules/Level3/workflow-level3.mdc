---
description: Определяет стандартный рабочий процесс для задач уровня 3 (промежуточные функции), включая тщательное планирование, целенаправленный творческий дизайн, структурированную реализацию, подробное размышление и архивацию, специфичную для функции.
globs: workflow-level3.mdc
alwaysApply: false
---

# РАБОЧИЙ ПРОЦЕСС УРОВНЯ 3: РАЗРАБОТКА ПРОМЕЖУТОЧНЫХ ФУНКЦИЙ

> **Краткое содержание:** Этот документ описывает структурированный рабочий процесс для задач уровня 3 (промежуточные функции). Эти задачи включают разработку значительных новых функций, которые могут охватывать несколько компонентов, требуя тщательного планирования, часто с целенаправленными фазами творческого дизайна, за которыми следует систематическая реализация, глубокое размышление и архивация, специфичная для функции. Этот рабочий процесс балансирует между детальным процессом и эффективностью для функций средней сложности.

## 🔍 ОБЗОР РАБОЧЕГО ПРОЦЕССА УРОВНЯ 3

Задачи уровня 3 представляют собой значительные усилия по разработке, созданию полноценной функции. Рабочий процесс обеспечивает адекватное планирование, дизайн ключевых аспектов и методичное выполнение.

```mermaid
graph LR
    Init["1. ИНИЦИАЛИЗАЦИЯ<br>(VAN Mode Output)"] -->
    DocSetup["2. НАСТРОЙКА ДОКУМЕНТАЦИИ"] -->
    Plan["3. ПЛАНИРОВАНИЕ ФУНКЦИИ (PLAN Mode)"] -->
    Creative["4. ТВОРЧЕСКИЕ ФАЗЫ (CREATIVE Mode)"] -->
    Impl["5. РЕАЛИЗАЦИЯ (IMPLEMENT Mode)"] -->
    Reflect["6. РАЗМЫШЛЕНИЕ (REFLECT Mode)"] -->
    Archive["7. АРХИВАЦИЯ (ARCHIVE Mode)"]

    %% Связи документов для каждой фазы (концептуальные ссылки на руководство по режимам)
    Init -.-> InitDocs["Основные правила и подтверждение L3"]
    DocSetup -.-> DocSetupDocs["Настройка банка памяти для L3"]
    Plan -.-> PlanDocs["Комплексный план функции"]
    Creative -.-> CreativeDocs["Целенаправленные документы дизайна"]
    Impl -.-> ImplDocs["Реализация и тестирование функции"]
    Reflect -.-> ReflectDocs["Глубокое размышление о функции"]
    Archive -.-> ArchiveDocs["Пакет архивации функции"]

    style Init fill:#a1c4fd,stroke:#669df6
    style DocSetup fill:#b3e5fc,stroke:#81d4fa
    style Plan fill:#c8e6c9,stroke:#a5d6a7
    style Creative fill:#ffd8b2,stroke:#ffcc80
    style Impl fill:#ffcdd2,stroke:#ef9a9a
    style Reflect fill:#d1c4e9,stroke:#b39ddb
    style Archive fill:#cfd8dc,stroke:#b0bec5
```

Задачи уровня 3 обычно включают создание новой, отдельной функции или значительные изменения существующей функции, которые затрагивают несколько частей приложения.

## 🔄 ОБРАБОТКА ПЕРЕХОДА УРОВНЕЙ (в рамках рабочего процесса уровня 3)

```mermaid
graph TD
    L3["Задача уровня 3 в процессе"] --> Assess["Непрерывная оценка<br>во время PLAN или раннего IMPLEMENT"]

    Assess --> Up["Повысить до<br>уровня 4?"]
    Assess --> Down["Понизить до<br>уровня 2?"]
    Assess --> MaintainL3["Сохранить<br>уровень 3"]

    Up --> L4Trigger["Триггеры:<br>- Непредвиденное воздействие на всю систему<br>- Требуются глубокие архитектурные изменения<br>- Объем значительно больше запланированного"]
    Down --> L2Trigger["Триггеры:<br>- Функция проще, чем предполагалось<br>- Очень ограниченное взаимодействие компонентов<br>- Отсутствуют сложные дизайнерские решения"]

    L4Trigger --> L4Switch["Остановить рабочий процесс L3.<br>Переинициализировать задачу как уровень 4 (VAN).<br>Сохранить существующие документы как входные данные."]
    L2Trigger --> L2Switch["Адаптировать рабочий процесс L3:<br>Упростить оставшиеся фазы,<br>использовать правила отражения/архивации L2."]
    
    style Assess fill:#ffe082,stroke:#ffca28
    style Up fill:#ef9a9a,stroke:#e57373
    style Down fill:#a5d6a7,stroke:#81c784
    style MaintainL3 fill:#b3e5fc,stroke:#81d4fa
```

## 📋 ФАЗЫ РАБОЧЕГО ПРОЦЕССА

### Фаза 1: ИНИЦИАЛИЗАЦИЯ (Выход из режима VAN)

Эта фаза в основном завершается в режиме VAN, который определяет задачу как уровень 3.

  * **Входные данные:** Запрос пользователя, ведущий к классификации "Промежуточная функция".
  * **Ключевые существующие файлы (из VAN):**
      * `memory-bank/tasks.md`: Создана запись, сложность установлена на уровень 3.
      * `memory-bank/activeContext.md`: Установлен начальный контекст.
      * Загружены соответствующие основные правила (например, `Core/memory-bank-paths.mdc`, `Core/main-optimized.mdc`).
  * **Шаги в рамках этого файла рабочего процесса (Подтверждение):**
    1. Подтвердить, что задача относится к уровню 3, проверив `memory-bank/tasks.md`.
    2. Убедиться, что структура банка памяти и пути известны (ИИ должен был усвоить из правила `main`).
  * **Контрольная точка этапа:**
    ```
    ✓ ПОДТВЕРЖДЕНИЕ ИНИЦИАЛИЗАЦИИ (L3)
    - Задача правильно определена как уровень 3 в tasks.md? [ДА/НЕТ]
    - Основные файлы банка памяти (tasks.md, activeContext.md) доступны через канонические пути? [ДА/НЕТ]

    → Если все ДА: Перейти к настройке документации для L3.
    → Если есть НЕТ: Вернуться в режим VAN или настроить основные файлы.
    ```

### Фаза 2: НАСТРОЙКА ДОКУМЕНТАЦИИ (Специфично для L3)

Подготовка банка памяти для функции уровня 3.

```mermaid
graph TD
    StartDoc["Начало настройки<br>документации L3"] --> LoadL3PlanTrack["Загрузка правил планирования и отслеживания L3<br>Level3/planning-comprehensive.mdc<br>Level3/task-tracking-intermediate.mdc"]
    LoadL3PlanTrack --> UpdateBrief["Проверка/обновление `projectbrief.md`<br>Убедиться, что функция соответствует общим целям проекта"]
    UpdateBrief --> UpdateActiveCtx["Обновление `activeContext.md`<br>Установить фокус на планирование функции L3"]
    UpdateActiveCtx --> PrepTaskFile["Подготовка `tasks.md` для<br>разделов комплексного плана функции"]
    PrepTaskFile --> DocSetupComplete["Настройка документации<br>L3 завершена"]

    style StartDoc fill:#b3e5fc,stroke:#81d4fa
    style DocSetupComplete fill:#81d4fa,stroke:#4fc3f7
```

  * **Шаги:**
    1. Загрузить специфичные для уровня 3 правила планирования (`Level3/planning-comprehensive.mdc`) и отслеживания задач (`Level3/task-tracking-intermediate.mdc`).
    2. Проверить `memory-bank/projectbrief.md`: кратко отметить новую функцию, если она влияет на общий бриф.
    3. Обновить `memory-bank/activeContext.md`: установить текущий фокус на "Планирование функции уровня 3: [Название функции]".
    4. Убедиться, что `memory-bank/tasks.md` готов для подробных разделов планирования, описанных в `Level3/planning-comprehensive.mdc`.
  * **Контрольная точка этапа:**
    ```
    ✓ КОНТРОЛЬНАЯ ТОЧКА НАСТРОЙКИ ДОКУМЕНТАЦИИ L3
    - Правила планирования и отслеживания L3 загружены? [ДА/НЕТ]
    - projectbrief.md проверен/обновлен для контекста функции? [ДА/НЕТ]
    - activeContext.md отражает фокус на планирование функции L3? [ДА/НЕТ]
    - tasks.md подготовлен для детального плана L3? [ДА/НЕТ]

    → Если все ДА: Перейти к планированию функции.
    → Если есть НЕТ: Завершить шаги настройки документации.
    ```

### Фаза 3: ПЛАНИРОВАНИЕ ФУНКЦИИ (Режим PLAN)

Руководствуется `visual-maps/plan-mode-map.mdc` и использует `Level3/planning-comprehensive.mdc` и `Level3/task-tracking-intermediate.mdc`.

```mermaid
graph TD
    StartPlan["Начало планирования<br>функции L3"] --> ReqDef["Определение подробных<br>требований (функциональных и нефункциональных)"]
    ReqDef --> CompAnalysis["Анализ компонентов<br>(новые и затронутые компоненты, взаимодействия)"]
    CompAnalysis --> ImplStrategy["Разработка стратегии реализации<br>и высокоуровневых шагов"]
    ImplStrategy --> DepRiskMgmt["Определение зависимостей,<br>рисков и мер смягчения"]
    DepRiskMgmt --> CreativeFlag["Отметка аспектов для<br>режима CREATIVE (UI, архитектура, алгоритмы)"]
    CreativeFlag --> UpdateTasks["Обновление `tasks.md` с<br>полным планом функции L3"]
    UpdateTasks --> PlanComplete["Планирование функции<br>L3 завершено"]

    style StartPlan fill:#c8e6c9,stroke:#a5d6a7
    style PlanComplete fill:#a5d6a7,stroke:#81c784
```

  * **Шаги:**
    1. Определить подробные функциональные и нефункциональные требования для функции.
    2. Провести анализ компонентов: определить новые компоненты для создания и существующие, которые будут изменены. Составить карту их взаимодействий.
    3. Разработать стратегию реализации: описать основные шаги или этапы создания функции.
    4. Определить зависимости (технические, данные, другие функции) и потенциальные риски, а также идеи по их смягчению.
    5. **Критично для L3:** Явно определить и отметить части функции, требующие режима CREATIVE (например, специфические задачи UI/UX, новые архитектурные шаблоны для функции, сложные алгоритмы).
    6. Задокументировать полный план (требования, компоненты, стратегия, зависимости, риски, флаги для творческого режима) в `memory-bank/tasks.md` в записи задачи функции уровня 3.
  * **Контрольная точка этапа:**
    ```
    ✓ КОНТРОЛЬНАЯ ТОЧКА ПЛАНИРОВАНИЯ ФУНКЦИИ L3
    - Подробные требования задокументированы в tasks.md? [ДА/НЕТ]
    - Анализ компонентов (новых/затронутых, взаимодействий) завершен? [ДА/НЕТ]
    - Стратегия реализации описана? [ДА/НЕТ]
    - Зависимости и риски задокументированы? [ДА/НЕТ]
    - Аспекты, требующие режима CREATIVE, явно отмечены в tasks.md? [ДА/НЕТ]
    - tasks.md полностью обновлен с планом функции? [ДА/НЕТ]

    → Если все ДА: Перейти к творческим фазам (если отмечены) или к РЕАЛИЗАЦИИ.
    → Если есть НЕТ: Завершить шаги планирования.
    ```

### Фаза 4: ТВОРЧЕСКИЕ ФАЗЫ (Режим CREATIVE)

Запускается, если аспекты были отмечены на фазе PLAN. Руководствуется `visual-maps/creative-mode-map.mdc` и правилами `Phases/CreativePhase/*.mdc`.

```mermaid
graph TD
    StartCreative["Начало творческих<br>фаз L3 (при необходимости)"] --> SelectAspect["Выбор отмеченного аспекта<br>из `tasks.md`"]
    SelectAspect --> DesignExplore["Исследование вариантов дизайна/архитектуры<br>(использование соответствующих правил creative-phase-*.mdc)"]
    DesignExplore --> DecideDocument["Принятие и документирование решения<br>в `creative-[aspect_name].md`"]
    DecideDocument --> UpdateTasksCreative["Обновление `tasks.md` с<br>кратким описанием решения и ссылкой"]
    UpdateTasksCreative --> MoreAspects{"Есть еще<br>отмеченные аспекты?"}
    MoreAspects -- Да --> SelectAspect
    MoreAspects -- Нет --> CreativeComplete["Творческие фазы<br>L3 завершены"]

    style StartCreative fill:#ffd8b2,stroke:#ffcc80
    style CreativeComplete fill:#ffcc80,stroke:#ffb74d
```

  * **Шаги:**
    1. Для каждого аспекта, отмеченного в `tasks.md` для творческого исследования:
        a. Загрузить соответствующее правило `creative-phase-*.mdc` (например, UI/UX, архитектура).
        b. Определить проблему, исследовать варианты, проанализировать компромиссы.
        c. Принять дизайнерское решение и задокументировать его с обоснованием в новом файле `memory-bank/creative-[aspect_name].md`.
        d. Обновить `tasks.md`: отметить творческую подзадачу как завершенную и указать ссылку на документ с решением.
  * **Контрольная точка этапа:**
    ```
    ✓ КОНТРОЛЬНАЯ ТОЧКА ТВОРЧЕСКИХ ФАЗ L3
    - Все отмеченные аспекты из фазы PLAN рассмотрены? [ДА/НЕТ]
    - Решения по дизайну задокументированы в соответствующих файлах `memory-bank/creative-*.md`? [ДА/НЕТ]
    - Обоснование решений четко изложено? [ДА/НЕТ]
    - tasks.md обновлен с указанием завершения творческих подзадач и ссылок на документы с решениями? [ДА/НЕТ]

    → Если все ДА: Перейти к реализации.
    → Если есть НЕТ: Завершить работу творческой фазы.
    ```

### Фаза 5: РЕАЛИЗАЦИЯ (Режим IMPLEMENT)

Руководствуется `visual-maps/implement-mode-map.mdc` и `Level3/implementation-L3.mdc`.

```mermaid
graph TD
    StartImpl["Начало реализации<br>функции L3"] --> ReviewPlanDesign["Проверка плана (`tasks.md`)<br>и документов дизайна (`creative-*.md`)"]
    ReviewPlanDesign --> SetupDevEnv["Настройка среды разработки<br>(ветка, зависимости, инструменты)"]
    SetupDevEnv --> BuildModules["Реализация модулей/компонентов функции<br>итеративно или последовательно"]
    BuildModules --> UnitIntegrationTests["Проведение модульных и интеграционных тестов<br>для каждого модуля/части функции"]
    UnitIntegrationTests --> StyleAdherence["Обеспечение соответствия<br>`memory-bank/style-guide.md`"]
    StyleAdherence --> UpdateProgressDocs["Регулярное обновление `tasks.md` (подзадачи)<br>и `progress.md` (этапы)"]
    UpdateProgressDocs --> E2EFeatureTest["Сквозное тестирование функции<br>в соответствии с требованиями"]
    E2EFeatureTest --> ImplComplete["Реализация функции<br>L3 завершена"]

    style StartImpl fill:#ffcdd2,stroke:#ef9a9a
    style ImplComplete fill:#ef9a9a,stroke:#e57373
```

  * **Шаги:**
    1. Тщательно проверить план функции в `memory-bank/tasks.md` и все соответствующие документы решений `memory-bank/creative-*.md`.
    2. Настроить среду разработки (новая ветка, установка новых зависимостей, настройка инструментов).
    3. Реализовать функцию, создавая модули/компоненты согласно плану. Приоритет — чистый код и соответствие спецификациям дизайна.
    4. Выполнить модульные тесты для новой логики и интеграционные тесты по мере сборки компонентов.
    5. Убедиться, что все элементы пользовательского интерфейса строго соответствуют `memory-bank/style-guide.md`.
    6. Обновить `memory-bank/tasks.md` с прогрессом по подзадачам и `memory-bank/progress.md` с деталями реализованных частей, использованными командами и значимыми находками.
    7. Провести сквозное тестирование завершенной функции в соответствии с ее требованиями.
  * **Контрольная точка этапа:**
    ```
    ✓ КОНТРОЛЬНАЯ ТОЧКА РЕАЛИЗАЦИИ L3
    - Функция полностью реализована согласно плану и творческим дизайнам? [ДА/НЕТ]
    - Все элементы UI соответствуют `memory-bank/style-guide.md`? [ДА/НЕТ]
    - Модульные и интеграционные тесты выполнены и пройдены? [ДА/НЕТ]
    - Сквозное тестирование функции успешно? [ДА/НЕТ]
    - `tasks.md` и `progress.md` обновлены с состоянием реализации? [ДА/НЕТ]

    → Если все ДА: Перейти к размышлению.
    → Если есть НЕТ: Завершить реализацию и тестирование.
    ```

### Фаза 6: РАЗМЫШЛЕНИЕ (Режим REFLECT)

Руководствуется `visual-maps/reflect-mode-map.mdc` и `Level3/reflection-L3.mdc`.

```mermaid
graph TD
    StartReflect["Начало размышления<br>о функции L3"] --> ReviewCompleted["Проверка завершенной функции<br>(код, план, документы дизайна, результаты тестов)"]
    ReviewCompleted --> AnalyzeProcess["Анализ процесса разработки<br>(успехи, проблемы, отклонения)"]
    AnalyzeProcess --> DocumentLessons["Документирование ключевых уроков<br>(технические и процессные)"]
    DocumentLessons --> AssessDesignChoices["Оценка эффективности<br>решений творческой фазы"]
    AssessDesignChoices --> CreateReflectDoc["Создание `reflection-[feature_id].md`"]
    CreateReflectDoc --> UpdateTasksReflect["Обновление `tasks.md` (размышление завершено)"]
    UpdateTasksReflect --> ReflectComplete["Размышление о функции<br>L3 завершено"]

    style StartReflect fill:#d1c4e9,stroke:#b39ddb
    style ReflectComplete fill:#b39ddb,stroke:#9575cd
```

  * **Шаги:**
    1. Проверить весь жизненный цикл разработки функции: начальные требования, план, творческие дизайны, реализацию и результаты тестирования.
    2. Проанализировать, что прошло успешно, что было сложным и какие отклонения произошли от первоначального плана или дизайна.
    3. Задокументировать ключевые уроки, касающиеся технологий, архитектуры, процесса или командной работы, относящиеся к этой функции.
    4. Специально оценить, насколько эффективными были решения творческой фазы во время реализации.
    5. Создать документ `memory-bank/reflection-[feature_id].md`.
    6. Обновить `memory-bank/tasks.md`, чтобы отметить завершение этапа размышления для функции.
  * **Контрольная точка этапа:**
    ```
    ✓ КОНТРОЛЬНАЯ ТОЧКА РАЗМЫШЛЕНИЯ L3
    - Жизненный цикл разработки функции тщательно рассмотрен? [ДА/НЕТ]
    - Успехи, проблемы и уроки задокументированы в `reflection-[feature_id].md`? [ДА/НЕТ]
    - Эффективность решений дизайна/творчества оценена? [ДА/НЕТ]
    - `tasks.md` обновлен с указанием завершения размышления? [ДА/НЕТ]

    → Если все ДА: Перейти к архивации.
    → Если есть НЕТ: Завершить документацию размышления.
    ```

### Фаза 7: АРХИВАЦИЯ (Режим ARCHIVE - настоятельно рекомендуется для L3)

Руководствуется `visual-maps/archive-mode-map.mdc` и `Level3/archive-L3.mdc`.

```mermaid
graph TD
    StartArchive["Начало архивации<br>функции L3"] --> ConsolidateDocs["Консолидация всех документов функции<br>(план, творческие документы, размышления, ключевые заметки о прогрессе)"]
    ConsolidateDocs --> CreateArchiveSummary["Создание документа-итога архивации<br>`archive/feature-[feature_id]_YYYYMMDD.md`"]
    CreateArchiveSummary --> LinkDocs["Ссылка на подробные документы<br>в итоговом документе архивации"]
    LinkDocs --> FinalUpdateTasks["Финальное обновление `tasks.md`<br>(отметить функцию как ЗАВЕРШЕННУЮ и АРХИВИРОВАННУЮ)"]
    FinalUpdateTasks --> ResetActiveCtx["Очистка `activeContext.md`<br>Подготовка к следующей задаче"]
    ResetActiveCtx --> ArchiveComplete["Архивация функции<br>L3 завершена"]

    style StartArchive fill:#cfd8dc,stroke:#b0bec5
    style ArchiveComplete fill:#b0bec5,stroke:#90a4ae
```

  * **Шаги:**
    1. Консолидировать всю документацию, связанную с функцией: раздел плана из `tasks.md`, все файлы `creative-*.md`, файл `reflection-*.md` и соответствующие итоги из `progress.md`.
    2. Создать специальный итоговый документ архивации функции в `memory-bank/archive/feature-[feature_id]_YYYYMMDD.md`. Этот итог должен кратко описывать функцию, ее цель, ключевые решения и содержать ссылки на более подробные документы.
    3. Обновить `memory-bank/tasks.md`, чтобы отметить всю задачу функции уровня 3 как "ЗАВЕРШЕННУЮ" и "АРХИВИРОВАННУЮ", указав ссылку на новый итоговый документ архивации.
    4. Обновить `memory-bank/activeContext.md`, чтобы очистить информацию, связанную с завершенной функцией, подготовив к следующей задаче.
  * **Контрольная точка этапа:**
    ```
    ✓ КОНТРОЛЬНАЯ ТОЧКА АРХИВАЦИИ L3
    - Итоговый документ архивации создан в `memory-bank/archive/`? [ДА/НЕТ]
    - Итоговый документ архивации ссылается на все соответствующие документы планирования, творчества и размышления? [ДА/НЕТ]
    - `tasks.md` показывает функцию как ЗАВЕРШЕННУЮ и АРХИВИРОВАННУЮ с ссылкой на архив? [ДА/НЕТ]
    - `activeContext.md` очищен и готов к новой задаче? [ДА/НЕТ]

    → Если все ДА: Задача уровня 3 полностью завершена. Предложить режим VAN для следующей задачи.
    → Если есть НЕТ: Завершить шаги архивации.
    ```

## 🚨 ПРИНЦИП УПРАВЛЕНИЯ УРОВНЯ 3

Помните:

```
┌─────────────────────────────────────────────────────┐
│ Задачи уровня 3 создают значительные функции.       │
│ Балансируйте между детальным планированием и        │
│ целенаправленным дизайном с эффективной реализацией.│
│ Документируйте ключевые решения и результаты, чтобы │
│ функция была понятной и поддерживаемой.             │
└─────────────────────────────────────────────────────┘
```

Это гарантирует, что промежуточные функции разрабатываются с соответствующим уровнем строгости, соединяя простые улучшения и полномасштабную разработку системы.

```
```