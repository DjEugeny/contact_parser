---
description: Визуальная карта процесса для верификации файлов в режиме VAN
globs: van-file-verification.mdc
alwaysApply: false
---
# ОПТИМИЗИРОВАННАЯ СИСТЕМА ВЕРИФИКАЦИИ ФАЙЛОВ

🚨 КРИТИЧНО: ТРЕБУЕТСЯ ВЕРИФИКАЦИЯ БАНКА ПАМЯТИ 🚨
Структура Банка Памяти ДОЛЖНА существовать до любых операций с файлами
Эта проверка ДОЛЖНА быть выполнена первой во всех процессах верификации

> **Кратко:** Эта система предоставляет структурированный подход к проверке целостности структуры файлов перед выполнением задач, с акцентом на эффективные проверки и четкую отчетность о статусе.

## 🔍 ПРОЦЕСС ВЕРИФИКАЦИИ ФАЙЛОВ

```mermaid
graph TD
    %% Критическая проверка Банка Памяти - ДОЛЖНА быть первой
    Start["Начало верификации файлов"] --> MemBankCheck{"Банк Памяти<br>Существует?"}
    MemBankCheck -->|"Нет"| CreateMemBank["СОЗДАТЬ БАНК ПАМЯТИ<br>[КРИТИЧНО]"]
    MemBankCheck -->|"Да"| VerifyMemBankComplete["Проверить полноту<br>структуры Банка Памяти"]
    CreateMemBank --> VerifyMemBankComplete
    
    VerifyMemBankComplete --> PassCheck{"Все критические<br>проверки пройдены?"}
    PassCheck -->|"Нет"| AbortAll["⛔ ПРЕРВАТЬ ВСЕ ОПЕРАЦИИ<br>Сначала исправить Банк Памяти"]
    PassCheck -->|"Да"| MainVerification

    %% Обычный процесс верификации продолжается здесь
    MainVerification["Начать полную<br>верификацию файлов"] --> BatchVerify["Пакетная верификация<br>с использованием шаблонов"]
    BatchVerify --> BrokenLinks["Проверка на<br>битые ссылки"]
    BrokenLinks --> DirectoryStructure["Проверка структуры<br>каталогов"]
    DirectoryStructure --> Status{"Все проверки<br>успешны?"}
    
    Status -->|"Да"| Complete["Верификация<br>завершена ✓"]
    Status -->|"Нет"| Diagnose["Диагностика<br>проблем"]
    Diagnose --> Attempt{"Попытаться<br>автоматически исправить?"}
    
    Attempt -->|"Да"| AutoFix["Автоматическое<br>исправление проблем"]
    Attempt -->|"Нет"| ReportIssue["Сообщить о проблемах<br>пользователю"]
    
    AutoFix --> Recheck{"Проблемы<br>решены?"}
    Recheck -->|"Да"| ReportSuccess["Сообщить об успехе<br>пользователю"]
    Recheck -->|"Нет"| ReportIssue
    
    ReportSuccess --> Complete
    ReportIssue --> UserAction["Ожидать<br>действий пользователя"]
    UserAction --> ReVerify["Повторная верификация<br>после действий пользователя"]
    ReVerify --> Status
    
    style MemBankCheck fill:#ff0000,stroke:#990000,color:white,stroke-width:3px
    style CreateMemBank fill:#ff0000,stroke:#990000,color:white,stroke-width:3px
    style VerifyMemBankComplete fill:#ff0000,stroke:#990000,color:white,stroke-width:3px
    style PassCheck fill:#ff0000,stroke:#990000,color:white,stroke-width:3px
    style AbortAll fill:#ff0000,stroke:#990000,color:white,stroke-width:3px
    style Status fill:#f6546a,stroke:#c30052,color:white
    style Complete fill:#10b981,stroke:#059669,color:white
```

## 🧩 ВЕРИФИКАЦИЯ БАНКА ПАМЯТИ - КРИТИЧЕСКИЙ КОМПОНЕНТ

Верификация Банка Памяти ДОЛЖНА быть выполнена первой в любом процессе верификации файлов:

```javascript
function verifyMemoryBank() {
  // Проверка существования Банка Памяти
  const memoryBankExists = checkDirectoryExists("memory-bank");
  if (!memoryBankExists) {
    console.error("⛔ КРИТИЧЕСКАЯ ОШИБКА: Банк Памяти не существует");
    createMemoryBankStructure();
    return verifyMemoryBankCreation();
  }
  
  // Проверка необходимых подкаталогов
  const requiredDirs = [
    "memory-bank/active-context",
    "memory-bank/system-patterns",
    "memory-bank/creative-phase",
    "memory-bank/implementation"
  ];
  
  const missingDirs = requiredDirs.filter(dir => !checkDirectoryExists(dir));
  if (missingDirs.length > 0) {
    console.error(`⛔ КРИТИЧЕСКАЯ ОШИБКА: Отсутствуют каталоги Банка Памяти: ${missingDirs.join(", ")}`);
    createMissingDirectories(missingDirs);
    return verifyMemoryBankCreation();
  }
  
  // Проверка критических файлов
  const criticalFiles = [
    "memory-bank/active-context/activeContext.md",
    "memory-bank/system-patterns/systemPatterns.md"
  ];
  
  const missingFiles = criticalFiles.filter(file => !checkFileExists(file));
  if (missingFiles.length > 0) {
    console.error(`⛔ КРИТИЧЕСКАЯ ОШИБКА: Отсутствуют критические файлы: ${missingFiles.join(", ")}`);
    createMissingFiles(missingFiles);
    return verifyMemoryBankCreation();
  }
  
  return true; // Верификация Банка Памяти успешна
}

// ОБЯЗАТЕЛЬНО: Это должно быть вызвано перед любой другой верификацией
const memoryBankVerified = verifyMemoryBank();
if (!memoryBankVerified) {
  throw new Error("⛔ ВЕРИФИКАЦИЯ БАНКА ПАМЯТИ ПРОВАЛЕНА - НЕВОЗМОЖНО ПРОДОЛЖИТЬ");
}
```

## 📋 КОНТРОЛЬНЫЙ СПИСОК ВЕРИФИКАЦИИ БАНКА ПАМЯТИ

```
✓ КОНТРОЛЬНЫЙ СПИСОК ВЕРИФИКАЦИИ БАНКА ПАМЯТИ
- Каталог Банка Памяти существует? [ДА/НЕТ]
- Необходимые подкаталоги существуют? [ДА/НЕТ]
- Критические файлы существуют? [ДА/НЕТ]
- Содержимое файлов валидно? [ДА/НЕТ]

→ Если ВСЕ ДА: Верификация Банка Памяти пройдена - Продолжить верификацию файлов
→ Если ХОТЯ БЫ ОДИН НЕТ: ОСТАНОВИТЬ ВСЕ ОБРАБОТКИ и ИСПРАВИТЬ БАНК ПАМЯТИ
```

## 🔍 ПРОЦЕСС ПАКЕТНОЙ ВЕРИФИКАЦИИ

## 📋 ОПТИМИЗИРОВАННОЕ СОЗДАНИЕ КАТАЛОГОВ

```mermaid
graph TD
    Start["Создание<br>каталогов"] --> DetectOS["Определить<br>операционную систему"]
    DetectOS -->|"Windows"| WinCmd["Пакетное создание<br>команда Windows"]
    DetectOS -->|"Mac/Linux"| UnixCmd["Пакетное создание<br>команда Unix"]
    WinCmd & UnixCmd --> Verify["Проверить<br>успешность создания"]
    Verify --> Complete["Настройка каталогов<br>завершена"]
```

### Команды для конкретных платформ

#### Windows (PowerShell)
```powershell
# Создать все каталоги одной командой
mkdir memory-bank, docs, docs\archive -ErrorAction SilentlyContinue

# Создать все необходимые файлы
$files = @(".cursorrules", "tasks.md", 
           "memory-bank\projectbrief.md", 
           "memory-bank\productContext.md",
           "memory-bank\systemPatterns.md",
           "memory-bank\techContext.md",
           "memory-bank\activeContext.md",
           "memory-bank\progress.md")

foreach ($file in $files) {
    if (-not (Test-Path $file)) {
        New-Item -Path $file -ItemType File -Force
    }
}
```

#### Mac/Linux (Bash)
```bash
# Создать все каталоги одной командой
mkdir -p memory-bank docs/archive

# Создать все необходимые файлы
touch .cursorrules tasks.md \
      memory-bank/projectbrief.md \
      memory-bank/productContext.md \
      memory-bank/systemPatterns.md \
      memory-bank/techContext.md \
      memory-bank/activeContext.md \
      memory-bank/progress.md
```

## 📝 УПРОЩЕННЫЙ ПРОЦЕСС ВЕРИФИКАЦИИ

Вместо проверки каждого компонента отдельно, выполняйте пакетную верификацию:

```powershell
# Windows - PowerShell
$requiredDirs = @("memory-bank", "docs", "docs\archive")
$requiredFiles = @(".cursorrules", "tasks.md")
$mbFiles = @("projectbrief.md", "productContext.md", "systemPatterns.md", 
             "techContext.md", "activeContext.md", "progress.md")

$missingDirs = $requiredDirs | Where-Object { -not (Test-Path $_) -or -not (Test-Path $_ -PathType Container) }
$missingFiles = $requiredFiles | Where-Object { -not (Test-Path $_) -or (Test-Path $_ -PathType Container) }
$missingMBFiles = $mbFiles | ForEach-Object { "memory-bank\$_" } | 
                  Where-Object { -not (Test-Path $_) -or (Test-Path $_ -PathType Container) }

if ($missingDirs.Count -eq 0 -and $missingFiles.Count -eq 0 -and $missingMBFiles.Count -eq 0) {
    Write-Output "✓ Все необходимые компоненты проверены"
} else {
    # Создать все отсутствующие элементы сразу
    if ($missingDirs.Count -gt 0) {
        $missingDirs | ForEach-Object { mkdir $_ -Force }
    }
    if ($missingFiles.Count -gt 0 -or $missingMBFiles.Count -gt 0) {
        $allMissingFiles = $missingFiles + $missingMBFiles
        $allMissingFiles | ForEach-Object { New-Item -Path $_ -ItemType File -Force }
    }
}
```

## 📝 ИНИЦИАЛИЗАЦИЯ ШАБЛОНОВ

Оптимизируйте создание шаблонов с помощью одного скрипта:

```powershell
# Windows - PowerShell
$templates = @{
    "tasks.md" = @"
# Банк Памяти: Задачи

## Текущая задача
[Задача еще не определена]

## Статус
- [ ] Определение задачи
- [ ] План реализации
- [ ] Выполнение
- [ ] Документация

## Требования
[Требования еще не определены]
"@

    "memory-bank\activeContext.md" = @"
# Банк Памяти: Активный контекст

## Текущий фокус
[Активный фокус не определен]

## Статус
[Статус не определен]

## Последние изменения
[Изменения не записаны]
"@

    # Добавить другие шаблоны здесь
}

foreach ($file in $templates.Keys) {
    if (Test-Path $file) {
        Set-Content -Path $file -Value $templates[$file]
    }
}
```

## 🔍 ЛУЧШИЕ ПРАКТИКИ ОПТИМИЗАЦИИ ПРОИЗВОДИТЕЛЬНОСТИ

1. **Пакетные операции**: Всегда используйте пакетные операции вместо отдельных команд
   ```
   # ХОРОШО: Создать все каталоги сразу
   mkdir memory-bank docs docs\archive
   
   # ПЛОХО: Создавать каталоги по одному
   mkdir memory-bank
   mkdir docs
   mkdir docs\archive
   ```

2. **Оптимизация предварительной проверки**: Сначала проверьте все требования, затем создавайте только то, что отсутствует
   ```
   # Сначала проверьте, что отсутствует
   $missingItems = ...
   
   # Затем создавайте только то, что отсутствует
   if ($missingItems) { ... }
   ```

3. **Обработка ошибок**: Включайте обработку ошибок во все команды
   ```
   mkdir memory-bank, docs, docs\archive -ErrorAction SilentlyContinue
   ```

4. **Адаптация к платформе**: Автоматически определяйте платформу и используйте соответствующие команды
   ```
   if ($IsWindows) {
       # Команды Windows
   } else {
       # Команды Unix
   }
   ```

5. **Однопроходная верификация**: Проверяйте структуру каталогов за один проход
   ```
   $requiredPaths = @("memory-bank", "docs", "docs\archive", ".cursorrules", "tasks.md")
   $missingPaths = $requiredPaths | Where-Object { -not (Test-Path $_) }
   ```

## 📝 ФОРМАТ ОТЧЕТА О ВЕРИФИКАЦИИ

```
✅ ВЕРИФИКАЦИЯ ЗАВЕРШЕНА
- Созданные каталоги: [список]
- Созданные файлы: [список]
- Все компоненты проверены

Система Банка Памяти готова к использованию.
```