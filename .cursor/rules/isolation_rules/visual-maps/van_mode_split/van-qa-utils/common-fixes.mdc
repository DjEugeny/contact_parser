---
description: Утилита для исправления распространенных проблем валидации VAN QA
globs: van-qa-utils/common-fixes.mdc
alwaysApply: false
---
# VAN QA: РАСПРОСТРАНЕННЫЕ ИСПРАВЛЕНИЯ ОШИБОК ВАЛИДАЦИИ

> **Кратко:** Этот компонент предоставляет распространенные исправления для проблем, которые могут возникнуть в процессе валидации QA.

## 🧪 РАСПРОСТРАНЕННЫЕ ИСПРАВЛЕНИЯ ОШИБОК ВАЛИДАЦИИ ПО КАТЕГОРИЯМ

### Проблемы с зависимостями

| Проблема | Исправление |
|-------|-----|
| **Отсутствует Node.js** | Download and install Node.js from https://nodejs.org/ |
| **Устаревший npm** | Run `npm install -g npm@latest` to update |
| **Отсутствуют пакеты** | Run `npm install` or `npm install [package-name]` |
| **Конфликты версий пакетов** | Adjust versions in package.json and run `npm install` |
| **Проблемы с разрешением зависимостей** | Run `npm cache clean -f` and try installing again |

### Проблемы с конфигурацией

| Проблема | Исправление |
|-------|-----|
| **Недействительный JSON** | Use a JSON validator (e.g., jsonlint) to check syntax |
| **Отсутствует плагин React** | Add `import react from '@vitejs/plugin-react'` and `plugins: [react()]` to vite.config.js |
| **Несовместимая конфигурация TypeScript** | Update `tsconfig.json` with correct React settings |
| **Несоответствие версий в ссылках** | Ensure consistent versions across configuration files |
| **Отсутствующие записи в конфигурационных файлах** | Add required fields to configuration files |

### Проблемы с окружением

| Проблема | Исправление |
|-------|-----|
| **Отказано в доступе** | Run terminal as administrator (Windows) or use sudo (Mac/Linux) |
| **Порт уже используется** | Kill process using the port: `netstat -ano \| findstr :PORT` then `taskkill /F /PID PID` (Windows) or `lsof -i :PORT` then `kill -9 PID` (Mac/Linux) |
| **Отсутствуют инструменты сборки** | Install required command-line tools (git, node, etc.) |
| **Проблемы с переменными окружения** | Set required environment variables: `$env:VAR_NAME = "value"` (PowerShell) or `export VAR_NAME="value"` (Bash) |
| **Недостаток места на диске** | Free up disk space, clean npm/package cache files |

### Проблемы со сборкой и тестированием

| Проблема | Исправление |
|-------|-----|
| **Сборка не удалась** | Check console for specific error messages |
| **Тест не пройден** | Verify minimal configuration is correct |
| **Проблемы с путями** | Ensure paths use correct separators for the platform (`\` for Windows, `/` for Mac/Linux) |
| **Отсутствуют зависимости** | Make sure all required dependencies are installed |
| **Права доступа к скриптам** | Ensure script files have execution permissions (chmod +x on Unix) |

## 📝 ПРОЦЕДУРЫ ДИАГНОСТИКИ ПРОБЛЕМ

### 1. Диагностика зависимостей
```powershell
# Найти конфликтующие зависимости
npm ls [package-name]

# Проверить устаревшие пакеты
npm outdated

# Проверить уязвимости
npm audit
```

### 2. Диагностика конфигурации
```powershell
# Список всех конфигурационных файлов
Get-ChildItem -Recurse -Include "*.json","*.config.js" | Select-Object FullName

# Найти отсутствующие ссылки в tsconfig.json
if (Test-Path "tsconfig.json") { 
    $tsconfig = Get-Content "tsconfig.json" -Raw | ConvertFrom-Json
    if (-not $tsconfig.compilerOptions.jsx) {
        Write-Output "Missing jsx setting in tsconfig.json"
    }
}
```

### 3. Диагностика окружения
```powershell
# Проверить процесс, использующий порт (Windows)
netstat -ano | findstr ":3000"

# Список переменных окружения
Get-ChildItem Env:

# Проверить место на диске
Get-PSDrive C | Select-Object Used,Free
```

**Следующий шаг:** Вернитесь к процессу валидации или следуйте конкретным рекомендациям по исправлению, указанным выше.