---
description: Карта процесса для минимального теста сборки VAN QA
globs: van-qa-checks/build-test.mdc
alwaysApply: false
---
# VAN QA: МИНИМАЛЬНЫЙ ТЕСТ СБОРКИ

> **Кратко:** Этот компонент выполняет минимальный тест сборки для проверки корректной работы основных функций сборки.

## 4️⃣ ПРОЦЕСС МИНИМАЛЬНОГО ТЕСТА СБОРКИ

```mermaid
graph TD
    Start["Минимальный тест сборки"] --> CreateTest["Создать минимальный<br>тестовый проект"]
    CreateTest --> BuildTest["Попытка<br>сборки"]
    BuildTest --> BuildStatus{"Сборка<br>успешна?"}
    
    BuildStatus -->|"Да"| RunTest["Запуск базового<br>функционального теста"]
    BuildStatus -->|"Нет"| FixBuild["Исправить проблемы<br>сборки"]
    FixBuild --> RetryBuild["Повторить сборку"]
    RetryBuild --> BuildStatus
    
    RunTest --> TestStatus{"Тест<br>пройден?"}
    TestStatus -->|"Да"| TestSuccess["Минимальный тест сборки<br>✅ УСПЕХ"]
    TestStatus -->|"Нет"| FixTest["Исправить проблемы<br>теста"]
    FixTest --> RetryTest["Повторить тест"]
    RetryTest --> TestStatus
    
    style Start fill:#4da6ff,stroke:#0066cc,color:white
    style TestSuccess fill:#10b981,stroke:#059669,color:white
    style BuildStatus fill:#f6546a,stroke:#c30052,color:white
    style TestStatus fill:#f6546a,stroke:#c30052,color:white
```

### Реализация минимального теста сборки:
```powershell
# Пример: Выполнение минимального теста сборки для проекта React
function Perform-MinimalBuildTest {
    $buildSuccess = $false
    $testSuccess = $false
    
    # Создание минимального тестового проекта
    $testDir = ".__build_test"
    if (Test-Path $testDir) {
        Remove-Item -Path $testDir -Recurse -Force
    }
    
    try {
        # Создание минимальной тестовой директории
        New-Item -Path $testDir -ItemType Directory | Out-Null
        Push-Location $testDir
        
        # Инициализация минимального package.json
        @"
{
  "name": "build-test",
  "version": "1.0.0",
  "description": "Минимальный тест сборки",
  "main": "index.js",
  "scripts": {
    "build": "echo Тест сборки успешен"
  }
}
"@ | Set-Content -Path "package.json"
        
        # Попытка сборки
        npm run build | Out-Null
        $buildSuccess = $true
        
        # Создание минимального тестового файла
        @"
console.log('Тест успешен');
"@ | Set-Content -Path "index.js"
        
        # Выполнение базового теста
        node index.js | Out-Null
        $testSuccess = $true
        
    } catch {
        Write-Output "❌ Тест сборки не пройден: $($_.Exception.Message)"
    } finally {
        Pop-Location
        if (Test-Path $testDir) {
            Remove-Item -Path $testDir -Recurse -Force
        }
    }
    
    # Отображение результатов
    if ($buildSuccess -and $testSuccess) {
        Write-Output "✅ Минимальный тест сборки успешно пройден"
        return $true
    } else {
        if (-not $buildSuccess) {
            Write-Output "❌ Процесс сборки не удался"
        }
        if (-not $testSuccess) {
            Write-Output "❌ Базовый функциональный тест не пройден"
        }
        return $false
    }
}
```

## 📋 КОНТРОЛЬНАЯ ТОЧКА МИНИМАЛЬНОГО ТЕСТА СБОРКИ

```
✓ КОНТРОЛЬНАЯ ТОЧКА: МИНИМАЛЬНЫЙ ТЕСТ СБОРКИ
- Создание тестового проекта успешно? [ДА/НЕТ]
- Процесс сборки завершен успешно? [ДА/НЕТ]
- Базовый функциональный тест пройден? [ДА/НЕТ]

→ Если все ДА: Валидация QA завершена, перейти к генерации отчета об успехе.
→ Если есть НЕТ: Исправить проблемы сборки перед продолжением.
```

**Следующий шаг (при УСПЕХЕ):** Загрузить `van-qa-utils/reports.mdc` для генерации отчета об успехе.
**Следующий шаг (при НЕУДАЧЕ):** Проверить `van-qa-utils/common-fixes.mdc` для исправления проблем теста сборки.