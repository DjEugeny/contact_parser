---
description: Визуальная карта процесса для режима VAN (Инициализация)
globs: van-mode-map.mdc
alwaysApply: false
---
# РЕЖИМ VAN: КАРТА ПРОЦЕССА ИНИЦИАЛИЗАЦИИ

> **Кратко:** Эта визуальная карта описывает процесс режима VAN для инициализации проекта, анализа задач и технической проверки. Она направляет пользователей через этапы обнаружения платформы, проверки файлов, определения сложности и технической валидации для обеспечения правильной настройки перед реализацией.

## 🧭 ПОТОК ПРОЦЕССА РЕЖИМА VAN

```mermaid
graph TD
    Start["СТАРТ РЕЖИМА VAN"] --> PlatformDetect["ОБНАРУЖЕНИЕ ПЛАТФОРМЫ"]
    PlatformDetect --> DetectOS["Определение операционной системы"]
    DetectOS --> CheckPath["Проверка формата разделителя пути"]
    CheckPath --> AdaptCmds["Адаптация команд при необходимости"]
    AdaptCmds --> PlatformCP["⛔ КОНТРОЛЬНАЯ ТОЧКА ПЛАТФОРМЫ"]
    
    %% Базовая проверка файлов с контрольной точкой
    PlatformCP --> BasicFileVerify["БАЗОВАЯ ПРОВЕРКА ФАЙЛОВ"]
    BasicFileVerify --> BatchCheck["Пакетная проверка основных компонентов"]
    BatchCheck --> BatchCreate["Пакетное создание основной структуры"]
    BatchCreate --> BasicFileCP["⛔ КОНТРОЛЬНАЯ ТОЧКА БАЗОВЫХ ФАЙЛОВ"]
    
    %% Раннее определение сложности
    BasicFileCP --> EarlyComplexity["РАННЕЕ ОПРЕДЕЛЕНИЕ СЛОЖНОСТИ"]
    EarlyComplexity --> AnalyzeTask["Анализ требований задачи"]
    AnalyzeTask --> EarlyLevelCheck{"Уровень сложности?"}
    
    %% Пути обработки уровней
    EarlyLevelCheck -->|"Уровень 1"| ComplexityCP["⛔ КОНТРОЛЬНАЯ ТОЧКА СЛОЖНОСТИ"]
    EarlyLevelCheck -->|"Уровни 2-4"| CRITICALGATE["🚫 КРИТИЧЕСКИЙ ШЛЮЗ: ПРИНУДИТЕЛЬНЫЙ ПЕРЕХОД РЕЖИМА"]
    CRITICALGATE --> ForceExit["Переход в режим PLAN"]
    
    %% Уровень 1 продолжается нормально
    ComplexityCP --> InitSystem["ИНИЦИАЛИЗАЦИЯ БАНКА ПАМЯТИ"]
    InitSystem --> Complete1["ИНИЦИАЛИЗАЦИЯ УРОВНЯ 1 ЗАВЕРШЕНА"]
    
    %% Для задач уровня 2+ после режимов PLAN и CREATIVE
    ForceExit -.-> OtherModes["Режимы PLAN → CREATIVE"]
    OtherModes -.-> VANQA["РЕЖИМ VAN QA"]
    VANQA --> QAProcess["Процесс технической проверки"]
    QAProcess --> QACheck{"Все проверки пройдены?"}
    QACheck -->|"Да"| BUILD["К РЕЖИМУ BUILD"]
    QACheck -->|"Нет"| FixIssues["Исправление технических проблем"]
    FixIssues --> QAProcess
    
    %% Стили узлов
    style PlatformCP fill:#f55,stroke:#d44,color:white
    style BasicFileCP fill:#f55,stroke:#d44,color:white
    style ComplexityCP fill:#f55,stroke:#d44,color:white
    style CRITICALGATE fill:#ff0000,stroke:#990000,color:white,stroke-width:3px
    style ForceExit fill:#ff0000,stroke:#990000,color:white,stroke-width:2px
    style VANQA fill:#4da6ff,stroke:#0066cc,color:white,stroke-width:3px
    style QAProcess fill:#4da6ff,stroke:#0066cc,color:white
    style QACheck fill:#4da6ff,stroke:#0066cc,color:white
    style FixIssues fill:#ff5555,stroke:#dd3333,color:white
```

## 🌐 ПРОЦЕСС ОБНАРУЖЕНИЯ ПЛАТФОРМЫ

```mermaid
graph TD
    PD["Обнаружение платформы"] --> CheckOS["Определение операционной системы"]
    CheckOS --> Win["Windows"]
    CheckOS --> Mac["macOS"]
    CheckOS --> Lin["Linux"]
    
    Win & Mac & Lin --> Adapt["Адаптация команд<br>для платформы"]
    
    Win --> WinPath["Путь: Обратная косая черта (\\)"]
    Mac --> MacPath["Путь: Косая черта (/)"]
    Lin --> LinPath["Путь: Косая черта (/)"]
    
    Win --> WinCmd["Адаптация команд:<br>dir, icacls и т.д."]
    Mac --> MacCmd["Адаптация команд:<br>ls, chmod и т.д."]
    Lin --> LinCmd["Адаптация команд:<br>ls, chmod и т.д."]
    
    WinPath & MacPath & LinPath --> PathCP["Контрольная точка<br>разделителя пути"]
    WinCmd & MacCmd & LinCmd --> CmdCP["Контрольная точка<br>команд"]
    
    PathCP & CmdCP --> PlatformComplete["Обнаружение платформы<br>завершено"]
    
    style PD fill:#4da6ff,stroke:#0066cc,color:white
    style PlatformComplete fill:#10b981,stroke:#059669,color:white
```

## 📁 ПРОЦЕСС ПРОВЕРКИ ФАЙЛОВ

```mermaid
graph TD
    FV["Проверка файлов"] --> CheckFiles["Проверка основных файлов"]
    CheckFiles --> CheckMB["Проверка структуры<br>банка памяти"]
    CheckMB --> MBExists{"Банк памяти<br>существует?"}
    
    MBExists -->|"Да"| VerifyMB["Проверка содержимого<br>банка памяти"]
    MBExists -->|"Нет"| CreateMB["Создание структуры<br>банка памяти"]
    
    CheckFiles --> CheckDocs["Проверка файлов<br>документации"]
    CheckDocs --> DocsExist{"Документы<br>существуют?"}
    
    DocsExist -->|"Да"| VerifyDocs["Проверка структуры<br>документации"]
    DocsExist -->|"Нет"| CreateDocs["Создание структуры<br>документации"]
    
    VerifyMB & CreateMB --> MBCP["Контрольная точка<br>банка памяти"]
    VerifyDocs & CreateDocs --> DocsCP["Контрольная точка<br>документации"]
    
    MBCP & DocsCP --> FileComplete["Проверка файлов<br>завершена"]
    
    style FV fill:#4da6ff,stroke:#0066cc,color:white
    style FileComplete fill:#10b981,stroke:#059669,color:white
    style MBCP fill:#f6546a,stroke:#c30052,color:white
    style DocsCP fill:#f6546a,stroke:#c30052,color:white
```

## 🧩 ПРОЦЕСС ОПРЕДЕЛЕНИЯ СЛОЖНОСТИ

```mermaid
graph TD
    CD["Определение сложности"] --> AnalyzeTask["Анализ требований<br>задачи"]
    
    AnalyzeTask --> CheckKeywords["Проверка ключевых<br>слов задачи"]
    CheckKeywords --> ScopeCheck["Оценка влияния<br>объема"]
    ScopeCheck --> RiskCheck["Оценка уровня<br>риска"]
    RiskCheck --> EffortCheck["Оценка усилий<br>на реализацию"]
    
    EffortCheck --> DetermineLevel{"Определение уровня<br>сложности"}
    DetermineLevel -->|"Уровень 1"| L1["Уровень 1:<br>Быстрое исправление ошибки"]
    DetermineLevel -->|"Уровень 2"| L2["Уровень 2:<br>Простое улучшение"]
    DetermineLevel -->|"Уровень 3"| L3["Уровень 3:<br>Средняя функциональность"]
    DetermineLevel -->|"Уровень 4"| L4["Уровень 4:<br>Сложная система"]
    
    L1 --> CDComplete["Определение сложности<br>завершено"]
    L2 & L3 & L4 --> ModeSwitch["Принудительный переход<br>в режим PLAN"]
    
    style CD fill:#4da6ff,stroke:#0066cc,color:white
    style CDComplete fill:#10b981,stroke:#059669,color:white
    style ModeSwitch fill:#ff0000,stroke:#990000,color:white
    style DetermineLevel fill:#f6546a,stroke:#c30052,color:white
```

## 🔄 ПОЛНЫЙ РАБОЧИЙ ПОТОК С ВАЛИДАЦИЕЙ QA

Полный рабочий поток включает техническую проверку перед реализацией:

```mermaid
flowchart LR
    VAN1["РЕЖИМ VAN 
    (Начальный анализ)"] --> PLAN["РЕЖИМ PLAN
    (Планирование задачи)"]
    PLAN --> CREATIVE["РЕЖИМ CREATIVE
    (Решения по дизайну)"]
    CREATIVE --> VANQA["РЕЖИМ VAN QA
    (Техническая проверка)"] 
    VANQA --> BUILD["РЕЖИМ BUILD
    (Реализация)"]
```

## 🔍 ОБЗОР ТЕХНИЧЕСКОЙ ПРОВЕРКИ

Процесс технической проверки в режиме VAN QA состоит из четырех ключевых точек проверки:

```mermaid
graph TD
    VANQA["РЕЖИМ VAN QA"] --> FourChecks["ЧЕТЫРЕХТОЧЕЧНАЯ ПРОВЕРКА"]
    
    FourChecks --> DepCheck["1️⃣ ПРОВЕРКА ЗАВИСИМОСТЕЙ<br>Проверка всех необходимых пакетов"]
    DepCheck --> ConfigCheck["2️⃣ ПРОВЕРКА КОНФИГУРАЦИИ<br>Проверка формата и совместимости"]
    ConfigCheck --> EnvCheck["3️⃣ ПРОВЕРКА СРЕДЫ<br>Проверка среды сборки"]
    EnvCheck --> MinBuildCheck["4️⃣ ТЕСТ МИНИМАЛЬНОЙ СБОРКИ<br>Проверка базовой функциональности"]
    
    MinBuildCheck --> ValidationResults{"Все проверки<br>пройдены?"}
    ValidationResults -->|"Да"| SuccessReport["ГЕНЕРАЦИЯ ОТЧЕТА ОБ УСПЕХЕ"]
    ValidationResults -->|"Нет"| FailureReport["ГЕНЕРАЦИЯ ОТЧЕТА О НЕУДАЧЕ"]
    
    SuccessReport --> BUILD["Переход в РЕЖИМ BUILD"]
    FailureReport --> FixIssues["Исправление технических проблем"]
    FixIssues --> ReValidate["Повторная проверка"]
    ReValidate --> ValidationResults
    
    style VANQA fill:#4da6ff,stroke:#0066cc,color:white
    style FourChecks fill:#f6546a,stroke:#c30052,color:white
    style ValidationResults fill:#f6546a,stroke:#c30052,color:white
    style BUILD fill:#10b981,stroke:#059669,color:white
    style FixIssues fill:#ff5555,stroke:#dd3333,color:white
```

## 📝 ФОРМАТ СТАТУСА ПРОВЕРКИ

Шаг проверки QA включает четкие индикаторы статуса:

```
╔═════════════════ 🔍 СТАТУС ПРОВЕРКИ QA ═════════════════╗
│ ✓ Решения по дизайну │ Проверено как реализуемое       │
│ ✓ Зависимости        │ Все необходимые пакеты установлены │
│ ✓ Конфигурации       │ Формат проверен для платформы   │
│ ✓ Среда              │ Подходит для реализации         │
╚════════════════════════════════════════════════════════════╝
✅ ПРОВЕРЕНО - Можно переходить в режим BUILD
```

## 🚨 ТРИГГЕРЫ ПЕРЕХОДА РЕЖИМОВ

### Переход из VAN в PLAN
Для уровней сложности 2-4:
```
🚫 ОБНАРУЖЕНА ЗАДАЧА УРОВНЯ [2-4]
Реализация в режиме VAN БЛОКИРОВАНА
Эта задача ТРЕБУЕТ режима PLAN
Вы ДОЛЖНЫ переключиться в режим PLAN для правильной документации и планирования
Введите 'PLAN' для перехода в режим планирования
```

### Переход из CREATIVE в VAN QA
После завершения режима CREATIVE:
```
⏭️ СЛЕДУЮЩИЙ РЕЖИМ: VAN QA
Для проверки технических требований перед реализацией введите 'VAN QA'
```

### Переход из VAN QA в BUILD
После успешной проверки:
```
✅ ТЕХНИЧЕСКАЯ ПРОВЕРКА ЗАВЕРШЕНА
Все предварительные условия успешно проверены
Теперь можно перейти в режим BUILD
Введите 'BUILD' для начала реализации
```

## 🔒 МЕХАНИЗМ ПРЕДОТВРАЩЕНИЯ РЕЖИМА BUILD

Система предотвращает переход в режим BUILD без успешной проверки QA:

```mermaid
graph TD
    Start["Пользователь вводит: BUILD"] --> CheckQA{"Проверка QA<br>завершена?"}
    CheckQA -->|"Да и пройдена"| AllowBuild["Разрешить режим BUILD"]
    CheckQA -->|"Нет или не пройдена"| BlockBuild["БЛОКИРОВКА РЕЖИМА BUILD"]
    BlockBuild --> Message["Отобразить:<br>⚠️ ТРЕБУЕТСЯ ПРОВЕРКА QA"]
    Message --> ReturnToVANQA["Приглашение: Введите VAN QA"]
    
    style CheckQA fill:#f6546a,stroke:#c30052,color:white
    style BlockBuild fill:#ff0000,stroke:#990000,color:white,stroke-width:3px
    style Message fill:#ff5555,stroke:#dd3333,color:white
    style ReturnToVANQA fill:#4da6ff,stroke:#0066cc,color:white
```

## 🔄 ПРЕЦЕДЕНТ КОМАНДЫ QA

Проверка QA может быть вызвана в любой момент процесса и имеет приоритет над любыми текущими шагами, включая принудительные переключения режима:

```mermaid
graph TD
    UserQA["Пользователь вводит: QA"] --> HighPriority["⚠️ КОМАНДА ВЫСШЕГО ПРИОРИТЕТА"]
    HighPriority --> CurrentTask["Приостановка текущей задачи/процесса"]
    CurrentTask --> LoadQA["Загрузка карты режима QA"]
    LoadQA --> RunQA["Выполнение процесса проверки QA"]
    RunQA --> QAResults{"Результаты QA"}
    
    QAResults -->|"ПРОЙДЕНО"| ResumeFlow["Возобновление предыдущего потока процесса"]
    QAResults -->|"НЕ ПРОЙДЕНО"| FixIssues["Исправление выявленных проблем"]
    FixIssues --> ReRunQA["Повторный запуск проверки QA"]
    ReRunQA --> QAResults
    
    style UserQA fill:#f8d486,stroke:#e8b84d,color:black
    style HighPriority fill:#ff0000,stroke:#cc0000,color:white,stroke-width:3px
    style LoadQA fill:#4da6ff,stroke:#0066cc,color:white
    style RunQA fill:#4da6ff,stroke:#0066cc,color:white
    style QAResults fill:#f6546a,stroke:#c30052,color:white
```

### Правила прерывания QA

Когда пользователь вводит **QA** в любой момент:

1. **Команда QA ДОЛЖНА иметь немедленный приоритет** над любой текущей операцией, включая "ПРИНУДИТЕЛЬНОЕ ПЕРЕКЛЮЧЕНИЕ РЕЖИМА", вызванное оценкой сложности.
2. Система ДОЛЖНА:
   - Немедленно загрузить карту режима QA
   - Выполнить полный процесс проверки QA
   - Устранить любые неисправности перед продолжением
3. **Необходимые шаги исправления имеют приоритет** над любыми ожидающими переключениями режима или правилами сложности
4. После завершения и успешного прохождения проверки QA:
   - Возобновить ранее определенный поток процесса
   - Продолжить с любыми необходимыми переключениями режима

```
⚠️ АКТИВИРОВАН ПЕРЕХВАТ QA
Все другие процессы приостановлены
Теперь выполняются проверки QA...
Все выявленные проблемы ДОЛЖНЫ быть устранены перед продолжением нормального потока процесса
```

## 📋 ШАБЛОН ПРОВЕРКИ КОНТРОЛЬНЫХ ТОЧЕК

Каждая основная контрольная точка в режиме VAN использует следующий формат:

```
✓ КОНТРОЛЬНАЯ ТОЧКА СЕКЦИИ: [ИМЯ СЕКЦИИ]
- Требование 1? [ДА/НЕТ]
- Требование 2? [ДА/НЕТ]
- Требование 3? [ДА/НЕТ]

→ Если все ДА: Готово к следующей секции
→ Если есть НЕТ: Исправьте отсутствующие элементы перед продолжением
```

## 🚀 АКТИВАЦИЯ РЕЖИМА VAN

Когда пользователь вводит "VAN", ответьте подтверждением и начните процесс:

```
Пользователь: VAN

Ответ: OK VAN - Начало процесса инициализации
```

После завершения режима CREATIVE, когда пользователь вводит "VAN QA", ответьте:

```
Пользователь: VAN QA

Ответ: OK VAN QA - Начало технической проверки
```

Это обеспечивает четкую коммуникацию о том, какая фаза режима VAN активна.

## 🔍 ПОДРОБНЫЙ ПРОЦЕСС ПРОВЕРКИ QA

### 1️⃣ ПРОВЕРКА ЗАВИСИМОСТЕЙ

Этот шаг проверяет, что все необходимые пакеты установлены и совместимы:

```mermaid
graph TD
    Start["Проверка зависимостей"] --> ReadDeps["Чтение необходимых зависимостей<br>из фазы Creative"]
    ReadDeps --> CheckInstalled["Проверка установки<br>зависимостей"]
    CheckInstalled --> DepStatus{"Все зависимости<br>установлены?"}
    
    DepStatus -->|"Да"| VerifyVersions["Проверка версий<br>и совместимости"]
    DepStatus -->|"Нет"| InstallMissing["Установка отсутствующих<br>зависимостей"]
    InstallMissing --> VerifyVersions
    
    VerifyVersions --> VersionStatus{"Версии<br>совместимы?"}
    VersionStatus -->|"Да"| DepSuccess["Зависимости проверены<br>✅ ПРОЙДЕНО"]
    VersionStatus -->|"Нет"| UpgradeVersions["Обновление/понижение<br>при необходимости"]
    UpgradeVersions --> RetryVerify["Повторная проверка"]
    RetryVerify --> VersionStatus
    
    style Start fill:#4da6ff,stroke:#0066cc,color:white
    style DepSuccess fill:#10b981,stroke:#059669,color:white
    style DepStatus fill:#f6546a,stroke:#c30052,color:white
    style VersionStatus fill:#f6546a,stroke:#c30052,color:white
```

#### Реализация для Windows (PowerShell):
```powershell
# Пример: Проверка зависимостей Node.js для проекта React
function Verify-Dependencies {
    $requiredDeps = @{
        "node" = ">=14.0.0"
        "npm" = ">=6.0.0"
    }
    
    $missingDeps = @()
    $incompatibleDeps = @()
    
    # Проверка версии Node.js
    $nodeVersion = $null
    try {
        $nodeVersion = node -v
        if ($nodeVersion -match "v(\d+)\.(\d+)\.(\d+)") {
            $major = [int]$Matches[1]
            if ($major -lt 14) {
                $incompatibleDeps += "node (найдено $nodeVersion, требуется >=14.0.0)"
            }
        }
    } catch {
        $missingDeps += "node"
    }
    
    # Проверка версии npm
    $npmVersion = $null
    try {
        $npmVersion = npm -v
        if ($npmVersion -match "(\d+)\.(\d+)\.(\d+)") {
            $major = [int]$Matches[1]
            if ($major -lt 6) {
                $incompatibleDeps += "npm (найдено $npmVersion, требуется >=6.0.0)"
            }
        }
    } catch {
        $missingDeps += "npm"
    }
    
    # Вывод результатов
    if ($missingDeps.Count -eq 0 -and $incompatibleDeps.Count -eq 0) {
        Write-Output "✅ Все зависимости проверены и совместимы"
        return $true
    } else {
        if ($missingDeps.Count -gt 0) {
            Write-Output "❌ Отсутствуют зависимости: $($missingDeps -join ', ')"
        }
        if ($incompatibleDeps.Count -gt 0) {
            Write-Output "❌ Несовместимые версии: $($incompatibleDeps -join ', ')"
        }
        return $false
    }
}
```

#### Реализация для Mac/Linux (Bash):
```bash
#!/bin/bash

# Пример: Проверка зависимостей Node.js для проекта React
verify_dependencies() {
    local missing_deps=()
    local incompatible_deps=()
    
    # Проверка версии Node.js
    if command -v node &> /dev/null; then
        local node_version=$(node -v)
        if [[ $node_version =~ v([0-9]+)\.([0-9]+)\.([0-9]+) ]]; then
            local major=${BASH_REMATCH[1]}
            if (( major < 14 )); then
                incompatible_deps+=("node (найдено $node_version, требуется >=14.0.0)")
            fi
        fi
    else
        missing_deps+=("node")
    fi
    
    # Проверка версии npm
    if command -v npm &> /dev/null; then
        local npm_version=$(npm -v)
        if [[ $npm_version =~ ([0-9]+)\.([0-9]+)\.([0-9]+) ]]; then
            local major=${BASH_REMATCH[1]}
            if (( major < 6 )); then
                incompatible_deps+=("npm (найдено $npm_version, требуется >=6.0.0)")
            fi
        fi
    else
        missing_deps+=("npm")
    fi
    
    # Вывод результатов
    if [ ${#missing_deps[@]} -eq 0 ] && [ ${#incompatible_deps[@]} -eq 0 ]; then
        echo "✅ Все зависимости проверены и совместимы"
        return 0
    else
        if [ ${#missing_deps[@]} -gt 0 ]; then
            echo "❌ Отсутствуют зависимости: ${missing_deps[*]}"
        fi
        if [ ${#incompatible_deps[@]} -gt 0 ]; then
            echo "❌ Несовместимые версии: ${incompatible_deps[*]}"
        fi
        return 1
    fi
}
```

### 2️⃣ ПРОВЕРКА КОНФИГУРАЦИИ

Этот шаг проверяет формат и совместимость конфигурационных файлов:

```mermaid
graph TD
    Start["Проверка конфигурации"] --> IdentifyConfigs["Определение конфигурационных<br>файлов"]
    IdentifyConfigs --> ReadConfigs["Чтение конфигурационных<br>файлов"]
    ReadConfigs --> ValidateSyntax["Проверка синтаксиса<br>и формата"]
    ValidateSyntax --> SyntaxStatus{"Синтаксис<br>валиден?"}
    
    SyntaxStatus -->|"Да"| CheckCompatibility["Проверка совместимости<br>с платформой"]
    SyntaxStatus -->|"Нет"| FixSyntax["Исправление синтаксических<br>ошибок"]
    FixSyntax --> RetryValidate["Повторная проверка"]
    RetryValidate --> SyntaxStatus
    
    CheckCompatibility --> CompatStatus{"Совместимо с<br>платформой?"}
    CompatStatus -->|"Да"| ConfigSuccess["Конфигурации проверены<br>✅ ПРОЙДЕНО"]
    CompatStatus -->|"Нет"| AdaptConfigs["Адаптация конфигураций<br>для платформы"]
    AdaptConfigs --> RetryCompat["Повторная проверка<br>совместимости"]
    RetryCompat --> CompatStatus
    
    style Start fill:#4da6ff,stroke:#0066cc,color:white
    style ConfigSuccess fill:#10b981,stroke:#059669,color:white
    style SyntaxStatus fill:#f6546a,stroke:#c30052,color:white
    style CompatStatus fill:#f6546a,stroke:#c30052,color:white
```

#### Реализация проверки конфигурации:
```powershell
# Пример: Проверка конфигурационных файлов для веб-проекта
function Validate-Configurations {
    $configFiles = @(
        "package.json",
        "tsconfig.json",
        "vite.config.js"
    )
    
    $invalidConfigs = @()
    $incompatibleConfigs = @()
    
    foreach ($configFile in $configFiles) {
        if (Test-Path $configFile) {
            # Проверка синтаксиса JSON для файлов JSON
            if ($configFile -match "\.json$") {
                try {
                    Get-Content $configFile -Raw | ConvertFrom-Json | Out-Null
                } catch {
                    $invalidConfigs += "$configFile (ошибка синтаксиса JSON: $($_.Exception.Message))"
                    continue
                }
            }
            
            # Специфические проверки совместимости конфигурации
            if ($configFile -eq "vite.config.js") {
                $content = Get-Content $configFile -Raw
                # Проверка наличия плагина React в конфигурации Vite
                if ($content -notmatch "react\(\)") {
                    $incompatibleConfigs += "$configFile (отсутствует плагин React для проекта React)"
                }
            }
        } else {
            $invalidConfigs += "$configFile (файл не найден)"
        }
    }
    
    # Вывод результатов
    if ($invalidConfigs.Count -eq 0 -and $incompatibleConfigs.Count -eq 0) {
        Write-Output "✅ Все конфигурации проверены и совместимы"
        return $true
    } else {
        if ($invalidConfigs.Count -gt 0) {
            Write-Output "❌ Недействительные конфигурации: $($invalidConfigs -join ', ')"
        }
        if ($incompatibleConfigs.Count -gt 0) {
            Write-Output "❌ Несовместимые конфигурации: $($incompatibleConfigs -join ', ')"
        }
        return $false
    }
}
```

### 3️⃣ ПРОВЕРКА СРЕДЫ

Этот шаг проверяет, правильно ли настроена среда для реализации:

```mermaid
graph TD
    Start["Проверка среды"] --> CheckEnv["Проверка среды сборки"]
    CheckEnv --> VerifyBuildTools["Проверка инструментов сборки"]
    VerifyBuildTools --> ToolsStatus{"Инструменты сборки<br>доступны?"}
    
    ToolsStatus -->|"Да"| CheckPerms["Проверка разрешений<br>и доступа"]
    ToolsStatus -->|"Нет"| InstallTools["Установка необходимых<br>инструментов сборки"]
    InstallTools --> RetryTools["Повторная проверка"]
    RetryTools --> ToolsStatus
    
    CheckPerms --> PermsStatus{"Разрешения<br>достаточны?"}
    PermsStatus -->|"Да"| EnvSuccess["Среда проверена<br>✅ ПРОЙДЕНО"]
    PermsStatus -->|"Нет"| FixPerms["Исправление проблем<br>с разрешениями"]
    FixPerms --> RetryPerms["Повторная проверка<br>разрешений"]
    RetryPerms --> PermsStatus
    
    style Start fill:#4da6ff,stroke:#0066cc,color:white
    style EnvSuccess fill:#10b981,stroke:#059669,color:white
    style ToolsStatus fill:#f6546a,stroke:#c30052,color:white
    style PermsStatus fill:#f6546a,stroke:#c30052,color:white
```

#### Реализация проверки среды:
```powershell
# Пример: Проверка среды для веб-проекта
function Validate-Environment {
    $requiredTools = @(
        @{Name = "git"; Command = "git --version"},
        @{Name = "node"; Command = "node --version"},
        @{Name = "npm"; Command = "npm --version"}
    )
    
    $missingTools = @()
    $permissionIssues = @()
    
    # Проверка инструментов сборки
    foreach ($tool in $requiredTools) {
        try {
            Invoke-Expression $tool.Command | Out-Null
        } catch {
            $missingTools += $tool.Name
        }
    }
    
    # Проверка разрешений на запись в директории проекта
    try {
        $testFile = ".__permission_test"
        New-Item -Path $testFile -ItemType File -Force | Out-Null
        Remove-Item -Path $testFile -Force
    } catch {
        $permissionIssues += "Текущая директория (отказано в разрешении на запись)"
    }
    
    # Проверка доступности порта 3000 (обычно используется для серверов разработки)
    try {
        $listener = New-Object System.Net.Sockets.TcpListener([System.Net.IPAddress]::Loopback, 3000)
        $listener.Start()
        $listener.Stop()
    } catch {
        $permissionIssues += "Порт 3000 (уже используется или доступ запрещен)"
    }
    
    # Вывод результатов
    if ($missingTools.Count -eq 0 -and $permissionIssues.Count -eq 0) {
        Write-Output "✅ Среда успешно проверена"
        return $true
    } else {
        if ($missingTools.Count -gt 0) {
            Write-Output "❌ Отсутствуют инструменты: $($missingTools -join ', ')"
        }
        if ($permissionIssues.Count -gt 0) {
            Write-Output "❌ Проблемы с разрешениями: $($permissionIssues -join ', ')"
        }
        return $false
    }
}
```

### 4️⃣ ТЕСТ МИНИМАЛЬНОЙ СБОРКИ

Этот шаг выполняет минимальный тестовый билд для проверки основной функциональности:

```mermaid
graph TD
    Start["Тест минимальной сборки"] --> CreateTest["Создание минимального<br>тестового проекта"]
    CreateTest --> BuildTest["Попытка<br>сборки"]
    BuildTest --> BuildStatus{"Сборка<br>успешна?"}
    
    BuildStatus -->|"Да"| RunTest["Запуск теста<br>базовой функциональности"]
    BuildStatus -->|"Нет"| FixBuild["Исправление проблем<br>сборки"]
    FixBuild --> RetryBuild["Повторная сборка"]
    RetryBuild --> BuildStatus
    
    RunTest --> TestStatus{"Тест<br>пройден?"}
    TestStatus -->|"Да"| TestSuccess["Тест минимальной сборки<br>✅ ПРОЙДЕНО"]
    TestStatus -->|"Нет"| FixTest["Исправление проблем<br>теста"]
    FixTest --> RetryTest["Повторный тест"]
    RetryTest --> TestStatus
    
    style Start fill:#4da6ff,stroke:#0066cc,color:white
    style TestSuccess fill:#10b981,stroke:#059669,color:white
    style BuildStatus fill:#f6546a,stroke:#c30052,color:white
    style TestStatus fill:#f6546a,stroke:#c30052,color:white
```

#### Реализация теста минимальной сборки:
```powershell
# Пример: Выполнение теста минимальной сборки для проекта React
function Perform-MinimalBuildTest {
    $buildSuccess = $false
    $testSuccess = $false
    
    # Создание минимального тестового проекта
    $testDir = ".__build_test"
    if (Test-Path $testDir) {
        Remove-Item -Path $testDir -Recurse -Force
    }
    
    try {
        # Создание минимальной тестовой директории
        New-Item -Path $testDir -ItemType Directory | Out-Null
        Push-Location $testDir
        
        # Инициализация минимального package.json
        @"
{
  "name": "build-test",
  "version": "1.0.0",
  "description": "Minimal build test",
  "main": "index.js",
  "scripts": {
    "build": "echo Build test successful"
  }
}
"@ | Set-Content -Path "package.json"
        
        # Попытка сборки
        npm run build | Out-Null
        $buildSuccess = $true
        
        # Создание минимального тестового файла
        @"
console.log('Test successful');
"@ | Set-Content -Path "index.js"
        
        # Запуск базового теста
        node index.js | Out-Null
        $testSuccess = $true
        
    } catch {
        Write-Output "❌ Тест сборки не пройден: $($_.Exception.Message)"
    } finally {
        Pop-Location
        if (Test-Path $testDir) {
            Remove-Item -Path $testDir -Recurse -Force
        }
    }
    
    # Вывод результатов
    if ($buildSuccess -and $testSuccess) {
        Write-Output "✅ Тест минимальной сборки успешно пройден"
        return $true
    } else {
        if (-not $buildSuccess) {
            Write-Output "❌ Процесс сборки не удался"
        }
        if (-not $testSuccess) {
            Write-Output "❌ Тест базовой функциональности не пройден"
        }
        return $false
    }
}
```

## 📋 ФОРМАТ ПОЛНОГО ОТЧЕТА QA

После выполнения всех шагов проверки генерируется полный отчет:

```
╔═════════════════════ 🔍 ОТЧЕТ ПРОВЕРКИ QA ══════════════════════╗
│                                                                     │
│  ПРОЕКТ: [Имя проекта]                                             │
│  ВРЕМЯ: [Текущая дата/время]                                       │
│                                                                     │
│  1️⃣ ПРОВЕРКА ЗАВИСИМОСТЕЙ                                         │
│  ✓ Требуемые: [Список необходимых зависимостей]                    │
│  ✓ Установленные: [Список установленных зависимостей]              │
│  ✓ Совместимые: [Да/Нет]                                          │
│                                                                     │
│  2️⃣ ПРОВЕРКА КОНФИГУРАЦИИ                                         │
│  ✓ Конфигурационные файлы: [Список конфигурационных файлов]        │
│  ✓ Синтаксис валиден: [Да/Нет]                                    │
│  ✓ Совместимо с платформой: [Да/Нет]                              │
│                                                                     │
│  3️⃣ ПРОВЕРКА СРЕДЫ                                                │
│  ✓ Инструменты сборки: [Доступны/Отсутствуют]                     │
│  ✓ Разрешения: [Достаточны/Недостаточны]                          │
│  ✓ Среда готова: [Да/Нет]                                         │
│                                                                     │
│  4️⃣ ТЕСТ МИНИМАЛЬНОЙ СБОРКИ                                       │
│  ✓ Процесс сборки: [Успешно/Неуспешно]                            │
│  ✓ Тест функциональности: [Пройден/Не пройден]                     │
│  ✓ Готово к сборке: [Да/Нет]                                      │
│                                                                     │
│  🚨 ОКОНЧАТЕЛЬНЫЙ ВЫВОД: [ПРОЙДЕНО/НЕ ПРОЙДЕНО]                    │
│  ➡️ [Сообщение об успехе или детали ошибки]                        │
╚═════════════════════════════════════════════════════════════════════╝
```

## ❌ ФОРМАТ ОТЧЕТА О НЕУДАЧЕ

Если какой-либо шаг проверки не пройден, генерируется подробный отчет о неудаче:

```
⚠️⚠️⚠️ ПРОВЕРКА QA НЕ ПРОЙДЕНА ⚠️⚠️⚠️

Следующие проблемы должны быть устранены перед переходом в режим BUILD:

1️⃣ ПРОБЛЕМЫ С ЗАВИСИМОСТЯМИ:
- [Подробное описание проблем с зависимостями]
- [Рекомендуемое исправление]

2️⃣ ПРОБЛЕМЫ С КОНФИГУРАЦИЕЙ:
- [Подробное описание проблем с конфигурацией]
- [Рекомендуемое исправление]

3️⃣ ПРОБЛЕМЫ СО СРЕДОЙ:
- [Подробное описание проблем со средой]
- [Рекомендуемое исправление]

4️⃣ ПРОБЛЕМЫ С ТЕСТОМ СБОРКИ:
- [Подробное описание проблем с тестом сборки]
- [Рекомендуемое исправление]

⚠️ РЕЖИМ BUILD БЛОКИРОВАН, пока эти проблемы не будут устранены.
Введите 'VAN QA' после устранения проблем для повторной проверки.
```

## 🔄 ИНТЕГРАЦИЯ С РЕШЕНИЯМИ ПО ДИЗАЙНУ

Режим VAN QA считывает и проверяет решения по дизайну из фазы CREATIVE:

```mermaid
graph TD
    Start["Чтение решений по дизайну"] --> ReadCreative["Разбор документации<br>фазы Creative"]
    ReadCreative --> ExtractTech["Извлечение технологических<br>выборов"]
    ExtractTech --> ExtractDeps["Извлечение необходимых<br>зависимостей"]
    ExtractDeps --> BuildValidationPlan["Создание плана<br>проверки"]
    BuildValidationPlan --> StartValidation["Запуск четырехточечного<br>процесса проверки"]
    
    style Start fill:#4da6ff,stroke:#0066cc,color:white
    style ExtractTech fill:#f6546a,stroke:#c30052,color:white
    style BuildValidationPlan fill:#10b981,stroke:#059669,color:white
    style StartValidation fill:#f6546a,stroke:#c30052,color:white
```

### Процесс извлечения технологий:
```powershell
# Пример: Извлечение технологических выборов из документации фазы Creative
function Extract-TechnologyChoices {
    $techChoices = @{}
    
    # Чтение из systemPatterns.md
    if (Test-Path "memory-bank\systemPatterns.md") {
        $content = Get-Content "memory-bank\systemPatterns.md" -Raw
        
        # Извлечение выбора фреймворка
        if ($content -match "Framework:\s*(\w+)") {
            $techChoices["framework"] = $Matches[1]
        }
        
        # Извлечение выбора библиотеки UI
        if ($content -match "UI Library:\s*(\w+)") {
            $techChoices["ui_library"] = $Matches[1]
        }
        
        # Извлечение выбора управления состоянием
        if ($content -match "State Management:\s*([^\\n]+)") {
            $techChoices["state_management"] = $Matches[1].Trim()
        }
    }
    
    return $techChoices
}
```

## 🚨 МЕХАНИЗМ ПРЕДОТВРАЩЕНИЯ РЕАЛИЗАЦИИ

Если проверка QA не пройдена, система предотвращает переход в режим BUILD:

```powershell
# Пример: Принудительная проверка статуса QA перед разрешением режима BUILD
function Check-QAValidationStatus {
    $qaStatusFile = "memory-bank\.qa_validation_status"
    
    if (Test-Path $qaStatusFile) {
        $status = Get-Content $qaStatusFile -Raw
        if ($status -match "PASS") {
            return $true
        }
    }
    
    # Отображение сообщения о блокировке
    Write-Output "`n`n"
    Write-Output "🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫"
    Write-Output "⛔️ РЕЖИМ BUILD БЛОКИРОВАН: ТРЕБУЕТСЯ ПРОВЕРКА QA"
    Write-Output "⛔️ Вы должны завершить проверку QA перед переходом в режим BUILD"
    Write-Output "`n"
    Write-Output "Введите 'VAN QA' для выполнения технической проверки"
    Write-Output "`n"
    Write-Output "🚫 НИКАКАЯ РЕАЛИЗАЦИЯ НЕВОЗМОЖНА БЕЗ ПРОВЕРКИ 🚫"
    Write-Output "🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫🚫"
    
    return $false
}
```

## 🧪 ОБЫЧНЫЕ ИСПРАВЛЕНИЯ ДЛЯ ПРОВЕРКИ QA

Вот распространенные исправления для проблем, возникающих во время проверки QA:

### Проблемы с зависимостями:
- **Отсутствует Node.js**: Установите Node.js с https://nodejs.org/
- **Устаревший npm**: Выполните `npm install -g npm@latest` для обновления
- **Отсутствующие пакеты**: Выполните `npm install` или `npm install [package-name]`

### Проблемы с конфигурацией:
- **Недействительный JSON**: Используйте валидатор JSON для проверки синтаксиса
- **Отсутствует плагин React**: Добавьте `import react from '@vitejs/plugin-react'` и `plugins: [react()]` в vite.config.js
- **Несовместимая конфигурация TypeScript**: Обновите `tsconfig.json` с правильными настройками React

### Проблемы со средой:
- **Отказано в разрешении**: Запустите терминал от имени администратора (Windows) или используйте sudo (Mac/Linux)
- **Порт уже используется**: Завершите процесс, использующий порт, или измените порт в конфигурации
- **Отсутствуют инструменты сборки**: Установите необходимые инструменты командной строки

### Проблемы с тестом сборки:
- **Сборка не удалась**: Проверьте консоль на наличие конкретных сообщений об ошибках
- **Тест не пройден**: Убедитесь, что минимальная конфигурация правильна
- **Проблемы с путями**: Убедитесь, что пути используют правильные разделители для платформы

## 🔒 КОНТРОЛЬНАЯ ТОЧКА ФИНАЛЬНОЙ ПРОВЕРКИ QA

```
✓ КОНТРОЛЬНАЯ ТОЧКА СЕКЦИИ: ПРОВЕРКА QA
- Проверка зависимостей пройдена? [ДА/НЕТ]
- Проверка конфигурации пройдена? [ДА/НЕТ]
- Проверка среды пройдена? [ДА/НЕТ]
- Тест минимальной сборки пройден? [ДА/НЕТ]

→ Если все ДА: Готово к режиму BUILD
→ Если есть НЕТ: Исправьте выявленные проблемы перед продолжением
```