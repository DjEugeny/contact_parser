#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
📦 ТОЧЕЧНЫЙ пакетный тестер с выбором конкретных писем
"""

import sys
from pathlib import Path

sys.path.append(str(Path(__file__).parent))
from llm_commercial_tester import CommercialOfferLLMTester

class PrecisionBatchLLMTester:
    """📦 Точечный пакетный тестер"""
    
    def __init__(self):
        self.tester = CommercialOfferLLMTester()
    
    def test_specific_emails(self, test_cases: list):
        """🎯 Точечное тестирование конкретных писем"""
        
        print(f"🎯 ТОЧЕЧНОЕ ТЕСТИРОВАНИЕ {len(test_cases)} ПИСЕМ")
        print("="*70)
        
        results = []
        
        for i, (date, email_number) in enumerate(test_cases, 1):
            print(f"\n📧 ТЕСТ {i}/{len(test_cases)}: {date}, письмо #{email_number}")
            print("-"*50)
            
            result = self.tester.test_specific_email(date, email_number)
            
            if 'error' not in result:
                # Краткий анализ результата
                llm_data = result.get('llm_analysis', {}).get('data', {})
                
                contacts_found = bool(llm_data.get('contacts', {}).get('sender_name'))
                kp_found = llm_data.get('commercial_offer', {}).get('amplifier_found', False)
                
                print(f"   ✅ Контакты: {'✓' if contacts_found else '✗'}")
                print(f"   📋 КП с амплификатором: {'✓' if kp_found else '✗'}")
                print(f"   🎯 Контекст: {llm_data.get('context', {}).get('status', 'N/A')}")
                
                results.append(result)
            else:
                print(f"   ❌ Ошибка: {result['error']}")
        
        return results


def main():
    """🚀 Главная функция точечного тестирования"""
    
    print("📦 ТОЧЕЧНЫЙ ПАКЕТНЫЙ LLM ТЕСТЕР")
    print("="*70)
    
    tester = PrecisionBatchLLMTester()
    
    print("\n🎯 РЕЖИМЫ ТОЧЕЧНОГО ТЕСТИРОВАНИЯ:")
    print("1. Тестировать список конкретных писем")
    print("2. Интерактивное добавление писем для теста")
    print("3. Выход")
    
    choice = input("Ваш выбор (1-3): ").strip()
    
    if choice == '1':
        # Предустановленный список для тестирования
        test_cases = [
            ("2025-07-28", 1),  # Письмо без вложений
            ("2025-07-28", 5),  # Письмо с 5 вложениями
            ("2025-08-05", 2),  # Другая дата
        ]
        
        print(f"\n📋 Тестируем предустановленный список:")
        for date, num in test_cases:
            print(f"   - {date}, письмо #{num}")
        
        confirm = input("Продолжить? (y/n): ").strip().lower()
        if confirm == 'y':
            results = tester.test_specific_emails(test_cases)
            print(f"\n✅ Завершено! Протестировано {len(results)} писем")
    
    elif choice == '2':
        # Интерактивное добавление
        test_cases = []
        
        available_dates = tester.tester.email_loader.get_available_date_folders()
        print(f"\n📅 Доступные даты: {', '.join(available_dates)}")
        
        while True:
            print(f"\n📧 Добавить письмо для теста:")
            print(f"   Уже добавлено: {len(test_cases)} писем")
            
            date = input("Дата (YYYY-MM-DD) или 'готово': ").strip()
            if date.lower() == 'готово':
                break
            
            if date not in available_dates:
                print(f"❌ Дата {date} не найдена!")
                continue
            
            # Показываем письма за дату
            tester.tester.show_available_emails(date)
            
            try:
                email_num = int(input("Номер письма: "))
                test_cases.append((date, email_num))
                print(f"✅ Добавлено: {date}, письмо #{email_num}")
            except ValueError:
                print("❌ Некорректный номер!")
        
        if test_cases:
            print(f"\n📋 К тестированию: {len(test_cases)} писем")
            results = tester.test_specific_emails(test_cases)
            print(f"\n✅ Завершено! Протестировано {len(results)} писем")
        else:
            print("📭 Нет писем для тестирования")
    
    elif choice == '3':
        print("👋 Выход")


if __name__ == '__main__':
    main()
